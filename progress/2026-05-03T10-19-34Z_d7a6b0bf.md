## Accomplished

Closed #2666 (Algorithm A redesign R1 bridge):

- Added private lemma `in_L_of_in_V_of_supp_bounded` to
  `EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean`,
  inserted between `polytabloidTab_in_lower_span_of_dominates` and
  `twistedPolytabloid_pigeonhole_pair`.
- The lemma packages the two existing bridges
  (`tabloidSupport_straightening` + `polytabloidTab_in_lower_span_of_dominates`)
  into a single statement: V-membership + tabloid-support bound ⇒
  membership in the L_σ target span used by `garnir_twisted_in_lower_span`.
- Proof is 4 lines: apply straightening, then `Submodule.span_le.mpr` to
  lift each generator into L_σ via the dominance bridge.
- No new sorries; lemma body is sorry-free.
- Doc comment cites `progress/algorithm-A-redesign.md` §2.2 for context.

## Current frontier

R1 (this issue) is the first piece of the Algorithm A redesign.
R2 (`twistedPolytabloid_mem_V` / `garnir_twisted_in_lower_span` closure)
is the next step — issue #2667 was filed in the planner cycle but
remains unclaimed.

## Overall project progress

- Stage 3 (Lean formalization): in progress on Chapter 5
- Sorry count in `SpechtModuleBasis.lean`: 2 (unchanged from start —
  `twistedPolytabloid_pigeonhole_pair`, `garnir_twisted_in_lower_span`)
- This addition is a pure helper; downstream consumers (R2) will use
  it to discharge `garnir_twisted_in_lower_span`.

## Next step

Worker can claim #2667 (R2: `twistedPolytabloid_mem_V` and the
`garnir_twisted_in_lower_span` closure that consumes the new bridge
lemma). #2656 (off-block vanishing of c_λ) is also unblocked now that
sub-α (#2655) has merged.

## Blockers

None.
