## Accomplished

Proved Theorem5_25_2_part3b and supporting infrastructure (PR #1690, issue #1681):

- **`principalSeries_char_diagElt`**: Character of V(χ₁,χ₂) at diagElt(c) = χ₁(c) + χ₂(c) for c ≠ 1. Uses evaluation linear equiv, trace via matrix diagonal, Pi.basisFun.
- **`pair_eq_of_sum_eq`**: If χ₁(c)+χ₂(c) = χ₁'(c)+χ₂'(c) for all c, and χ₁≠χ₂, χ₁'≠χ₂', then {χ₁,χ₂} = {χ₁',χ₂'}. Uses character orthogonality (∑μ(g) = 0 for nontrivial μ).
- **`sum_nontrivial_char_eq_zero`**: Standard character orthogonality result.
- **`Theorem5_25_2_part3b`**: V(χ₁,χ₂) ≅ V(χ₁',χ₂') ↔ {χ₁,χ₂} = {χ₁',χ₂'}. Forward direction fully proved via char_iso + char_diagElt + pair_eq_of_sum_eq. Backward direction uses principalSeries_iso_swap.
- **`principalSeries_iso_swap`**: V(χ₁,χ₂) ≅ V(χ₂,χ₁) via Corollary 4.2.4. Has 1 sorry for character symmetry proof.

Also proved `complementW_iso_implies_eq` (W_μ ≅ W_ν → μ = ν) in the previous session.

### Key technical decisions

1. Abandoned Weyl element conjugation approach for iso_swap — `wBw⁻¹ ≠ B` for non-diagonal Borel elements, so left-multiplication by w does NOT send V(χ₁,χ₂) into V(χ₂,χ₁).
2. Switched to character-theoretic approach using Corollary 4.2.4 (equal characters ⟹ isomorphic).
3. The remaining sorry requires proving full character equality (on ALL group elements, not just diagonal). This needs either the Frobenius trace formula or the intertwining operator construction.

## Current frontier

PR #1690 submitted. One sorry remains in `principalSeries_iso_swap` (character symmetry for V(χ₁,χ₂) = V(χ₂,χ₁)). This is a helper lemma; the main theorems `complementW_iso_implies_eq` and `Theorem5_25_2_part3b` are structurally complete.

## Overall project progress

- Theorem5_25_2 sorry count: was 8, now ~2 (principalSeries_iso_swap + complementW_simple may be resolved by merge)
- New fully proved lemmas: principalSeries_char_diagElt, pair_eq_of_sum_eq, sum_nontrivial_char_eq_zero, complementW_iso_implies_eq

## Next step

- Fill the sorry in `principalSeries_iso_swap` — requires proving Tr(V(χ₁,χ₂), g) = Tr(V(χ₂,χ₁), g) for all g. Best approach: compute trace on each conjugacy class type (scalar, diagonal, unipotent, non-split) and verify symmetry.
- Alternative: construct the intertwining operator M: f ↦ ∑_t f(cosetRep(some t) * ·) and show it's nonzero + equivariant.

## Blockers

The `principalSeries_iso_swap` sorry blocks full sorry-elimination for Theorem5_25_2_part3b. Requires significant infrastructure (full trace formula on all conjugacy classes).
