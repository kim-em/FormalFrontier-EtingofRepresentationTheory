## Accomplished

### F⁻ᵢ API lemmas for Proposition 6.6.6 (#1711 → PR #1715)

- Added `reversedArrow_ne_eq` to Definition6_6_4.lean: converts reversed-quiver arrow `a →_{Q̄ᵢ} i` (a ≠ i) to original `i ⟶ a`. Uses match-based pattern for clean unfolding.
- Added `reflFunctorMinus_mkQ`: canonical quotient map into F⁻ᵢ(ρ).obj i from the direct sum, resolving the Decidable.casesOn at vertex i.
- Added `reflFunctorMinus_mapLinear_ne_eq`: characterizes F⁻ᵢ map at (a ≠ i, b = i) through mkQ + lof + equivAt_ne. Proved using `rw [h_da, h_di]; intro e w; rfl` pattern.
- Removed duplicate `reversedArrow_ne_eq` from Proposition6_6_6.lean (now in Definition6_6_4.lean).
- Key fix: changed `reversedArrow_ne_eq` from `rw`-based proof to `revert e; exact match` pattern (matching `reversedArrow_eq_ne` in the Plus API) — this was the critical fix that let `reflFunctorMinus_mapLinear_ne_eq` compile.

### Blocker documented

The 2 sorrys in Proposition 6.6.6 remain. Documented the fundamental blocker in issue #1711: composing `equivAt_eq_sink` (tactic `cases` → equational Decidable.rec) with `reflFunctorMinus_mkQ` (term `match` → simple Decidable.casesOn). The equational motive from `cases` prevents `rw [h_ii]` from resolving `inst i i` in dependent type positions.

## Current frontier

- Proposition6_6_6.lean: 2 sorrys (sink naturality at ne_eq, source case)
- Both blocked on equivAt_eq_sink + Decidable.casesOn composition

## Overall project progress

- 562/583 items sorry-free (96.4%)
- 61 sorries remaining across 26 files
- Chapters 3, 4, 7, 8 fully sorry-free
- Phase 3.3 formalization work loop active

## Next step

- Refactor `equivAt_eq_sink` to extract Φ (the forward map) as a top-level definition, enabling `equivAt_eq_sink(mkQ y) = Φ y` via quotient API lemmas
- Or: change `equivAt_eq_sink`'s `cases` to produce match-compatible terms
- Other available work: Vandermonde orthogonality (#1592), CoxeterInfrastructure admissibleOrdering_exists

## Blockers

Decidable.casesOn type transport in equivAt_eq_sink composition (see issue #1711 comment).
