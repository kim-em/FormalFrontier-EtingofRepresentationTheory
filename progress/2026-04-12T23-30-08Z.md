## Accomplished
- Claimed issue #2318: "Prove single_branch_leaf_case sub-cases (4 sorries in InfiniteTypeConstructions)"
- Decomposed 4 sorry placeholders (original lines ~3992-3999) into 12 focused sub-sorries with complete case analysis skeleton
- Built out arm-tracing logic using `extract_other` for both arm2 and arm3, with full degree bookkeeping at each vertex
- Cases covered:
  - **D-type** (T(1,≥1,1) and T(1,1,≥1)): `exfalso; apply h_not_posdef; intro x hx; sorry` with QF decomposition strategy documented
  - **E₆/E₇/E₈** (T(1,3,2), T(1,4,2), T(1,5,2) and symmetric): posdef contradiction sorries
  - **T(1,2,≥6) and T(1,≥6,2)**: T(1,2,5) embedding sorries for infinite type transfer
- Verified build: no new errors introduced (pre-existing errors in `acyclic_deg_le_2_posdef` at lines 3291-3353 remain)
- Created PR #2321 with auto-merge enabled

## Current frontier
- 12 sub-sorries remain in `single_branch_leaf_case` (lines 4038-4137), each a specific:
  - Positive definiteness proof for a concrete ADE tree (D₅, E₆, E₇, E₈), or
  - T(1,2,5) subgraph embedding construction
- Pre-existing errors in `acyclic_deg_le_2_posdef` (lines 3291-3353) block some downstream work

## Overall project progress
- `single_branch_leaf_case` case analysis skeleton is complete — all tree types enumerated
- Total sorries in InfiniteTypeConstructions.lean: 17 (was ~9 before; net increase from decomposition is expected)
- The Ẽ₇ embedding (T(1,3,3) case) was proved in PR #2316
- Key infrastructure exists: `subgraph_infinite_type_transfer`, `t125_not_finite_type`, `etilde7_not_finite_type`

## Next step
- Prove the D-type positive definiteness sorries (lines 4121, 4137) — these are the most architecturally important since they close the base cases
- Strategy: use `acyclic_path_posdef_aux` on the inner path, then show QF_tree ≥ QF_path + (v₀ - leaf - a₃)² + (leaf - a₃)²
- Alternatively, construct graph isomorphisms to `DynkinType.adj (.D n)` and use `Dn_posDef` from DynkinTypes.lean

## Blockers
- Pre-existing build errors in `acyclic_deg_le_2_posdef` (lines 3291-3353) — these are from a prior PR and not caused by this work
