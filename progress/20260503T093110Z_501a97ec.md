## Accomplished

Worker session `501a97ec` — claimed and implemented #2655
(Schur-Weyl L_i C-4a-i sub-α: block factorization of `c_λ` via
A-equivariance of the bimodule iso).

Added to `EtingofRepresentationTheory/Chapter5/Theorem5_22_1.lean`:

* `youngSymElement k N lam : ↥(symGroupImage k (Fin N → k) (∑ i, lam i))`
  — the Young symmetrizer `c_λ` packaged as an element of the
  symmetric-group image subalgebra `A`. Uses
  `symGroupAlgHom_range` to witness membership.
* `@[simp] youngSymElement_val` — refl lemma identifying the
  underlying `End`-level realisation with `youngSymEndomorphism`.
* `youngSym_block_factorization` — the abstract algebraic step (1)
  in the C-4a-i decomposition. Given any `k`-linear iso
  `e : V^⊗n ≃ₗ[k] ⨁ᵢ ↥(S i) ⊗[k] (↥(S i) →ₗ[A] V^⊗n)` with the
  evaluation formula `e.symm (of i (v ⊗ₜ l)) = l v`, shows
  `e (youngSymEndomorphism (e.symm (of i (v ⊗ₜ l))))
    = of i ((youngSymElement • v) ⊗ₜ l)`.

Proof: 7 lines. Reduces via the evaluation formula to A-linearity
of `l`, then uses `Subalgebra.smul_def` and `Module.End.smul_def`
to identify `youngSymElement • (l v) = youngSymEndomorphism (l v)`
on `V^⊗n`.

Heartbeats: `2400000 / 1000000` (empirical minimum 2000000 / 800000;
20% safety buffer). The deep `Subalgebra → Subsemiring → Module`
instance chain on `Module ↥A ↥(S i)` for `S i : Submodule ↥A V^⊗n`
is the dominant cost.

Also added missing `open scoped TensorProduct` at the top of the
file (was not previously needed because all earlier `⊗[k]` uses
were inside imported scopes).

## Current frontier

The Schur-Weyl L_i C-4a-i chain now has its first sub-issue done:

* **#2655 (sub-α)** — DONE this session.
* **#2656 (sub-β)** — off-block vanishing of `f_i` (next).
* **#2657 (sub-γ)** — rank-1 scaled-projection on the special block.

These three feed into **#2644** (C-4a-ii: abstract idempotent-image
simplicity over centralizer), which is the abstract algebraic input
for the L_i simplicity proof.

## Overall project progress

Stage 3 formalization. **Sorry count: 7 leaves / 4 files**
(unchanged — the new theorem closes with no sorries, and adds 3
new declarations to the file).

Build status: full `lake build` was running at session end on
`Proposition5_22_2.lean` (downstream of `Theorem5_22_1`); the
target file `Theorem5_22_1` itself builds cleanly.

## Next step

**Workers** (priority order):
1. **#2660** (meditate, critical-path) — Algorithm A redesign (claimed).
2. **#2656** (sub-β) — off-block vanishing of `f_i`. New top of the
   Schur-Weyl L_i C-4a-i chain.
3. **#2657** (sub-γ) — rank-1 scaled-projection.
4. **#2633** — Theorem5_18_4_GL_rep_decomposition_simple wrapper.
5. **#2644** — abstract idempotent simplicity (now closer to ready,
   sub-α providing the per-block factorization data it needs).

The new helpers `youngSymElement` and `youngSym_block_factorization`
are in `Chapter5/Theorem5_22_1.lean` (around line 660-730) and
should be referenced by sub-β / sub-γ / #2644.

## Blockers

None. Clean implementation, no design changes needed.
