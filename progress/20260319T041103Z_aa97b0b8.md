## Accomplished

Refactored `reflectionFunctorPlus` in `Definition6_6_3.lean` to use consistent Decidable instances (#1165):

1. **Removed `by classical`** from the definition — `mapLinear` no longer introduces `Classical.propDecidable`
2. **Rewrote mapLinear field** to use `match inst a i, inst b i` (same instance as obj/AddCommMonoid/Module)
3. **Rewrote equivAt_ne and equivAt_eq** API lemmas to use term-mode `match` for cleaner definitional reduction
4. **Updated reversedArrow_ne_ne** in Proposition6_6_7 to use match-based definition
5. **Verified all downstream files** build correctly: Definition6_6_4, Proposition6_6_5, Proposition6_6_6, Proposition6_6_7, Proposition6_6_8

The `reflFunctorPlus_mapLinear_ne_ne` lemma still has a sorry — the instance consistency is fixed but the proof also requires rewriting `equivAt_ne` and `reversedArrow_ne_ne` to avoid simp-based casts that prevent `rfl`.

## Current frontier

The Decidable instance mismatch is fixed. The remaining sorry in `reflFunctorPlus_mapLinear_ne_ne` needs the `equivAt_ne` and `reversedArrow_ne_ne` helpers to also avoid `Eq.mpr` casts (use match-based definitions that reduce cleanly when the instance is known).

## Overall project progress

Stage 3.2 proof filling in progress. ~193/583 items sorry-free. This PR fixes a structural issue that was blocking proofs in the reflection functor chapter.

## Next step

1. Prove `reflFunctorPlus_mapLinear_ne_ne` by rewriting `reversedArrow_ne_ne` to return `e` directly (no simp-based cast) in the isFalse/isFalse branch
2. This unblocks the indecomposability proof in Proposition 6.6.7

## Blockers

None — the refactoring is complete and all downstream code builds.
