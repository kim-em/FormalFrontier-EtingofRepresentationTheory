## Accomplished

Completed issue #952: formalize §5.26-5.27 statements (3 items).

1. **Theorem 5.26.1 (Artin's theorem)** — Replaced `sorry : Prop` with a proper iff statement:
   - Hypothesis: X is a conjugation-invariant system of subgroups (closed under `H.map (MulAut.conj g).toMonoidHom`)
   - (i) Coverage: `∀ g : G, ∃ H ∈ X, g ∈ H`
   - (ii) ℚ-span: every irreducible character (`FDRep.character` with `CategoryTheory.Simple`) lies in `Submodule.span ℚ` of induced characters
   - Defined `Etingof.inducedCharacter` via the Frobenius character formula: `(1/|H|) ∑_x [x⁻¹gx ∈ H] · χ(x⁻¹gx)`

2. **Corollary 5.26.3 (characters from cyclic subgroups)** — Every irreducible character is in the ℚ-span of characters induced from `Subgroup.zpowers g` (cyclic subgroups). Uses `CategoryTheory.Simple` instance on `FDRep ℂ G`.

3. **Theorem 5.27.1 (orbit method for semidirect products)** — 4-part existential statement:
   - Existentially quantifies dual action `dualSmul`, stabilizer `stab`, and construction `V(χ, U)`
   - (i) `V(χ, U)` is simple when `U` is simple
   - (ii) Isomorphic `V(χ₁, U₁) ≅ V(χ₂, U₂)` implies same G-orbit on `A →* ℂˣ`
   - (iii) Every simple rep of `A ⋊[φ] G` arises as `V(χ, U)`
   - (iv) Character formula: `(1/|G_χ|) ∑_{h : hgh⁻¹ ∈ G_χ} χ(h(a)) · χ_U(hgh⁻¹)`

All three files build with only `sorry` warnings (no errors, no linter issues).

## Current frontier

All 3 items from §5.26-5.27 are now `statement_formalized`. No `(sorry : Prop)` placeholders remain in these files.

## Overall project progress

Stage 3.1 statement formalization continues. ~183/583 items sorry_free (~31.4%). Sections 5.26-5.27 now have proper typed statements. Chapter 5 completion rate improving.

## Next step

1. Continue Stage 3.2 proof filling — Proposition 6.6.6 (issue #919) is unblocked by Definition 6.6.4
2. Poll Aristotle submissions (issue #945) for completed proofs
3. Formalize remaining Ch5 placeholder statements

## Blockers

None.
