## Accomplished

Proved 2 key helper lemmas for Proposition 6.6.6 (reflection functor round-trip):

1. **`reversedArrow_ne_ne_is_cast`**: Shows `reversedArrow_ne_ne ha hb e = cast (ReversedAtVertexHom_ne_ne ha hb) e`. Uses the Decidable-fixing pattern: match on `inst_dec a i` / `inst_dec b i`, unfold definitions, rewrite Decidable values, close with rfl.

2. **`reversedArrow_ne_ne_twice`**: Shows double application of `reversedArrow_ne_ne` (through the ▸ transport) equals identity: `reversedArrow_ne_ne(inst)(reversedArrow_ne_ne(instR)(htwice.symm ▸ e)) = e`. Uses HEq chain: `cast_heq` for each `reversedArrow_ne_ne` (via `is_cast`), and `eqRec_heq_self` with explicit field-projection motive `(fun q _ => q.Hom a b)` to handle the ▸ transport (avoids Quiver Q instance synthesis issues).

Also added `dif_neg` simplification for the by_cases in crossIsoToIso naturality.

### Key technical discovery

The "synthesized type class instance is not definitionally equal" error when working with `Eq.rec`/`▸` on `Quiver Q` equalities can be bypassed by:
- Using `eqRec_heq_self` with `(motive := fun q _ => q.Hom a b)` (field projection) instead of `(fun q _ => @Quiver.Hom Q q a b)` (typeclass application)
- Using explicit `@` instance arguments for `reversedArrow_ne_ne_is_cast` calls to prevent instance synthesis mismatch
- Using `Subsingleton.elim _ _` instead of `match ... | .isFalse _ => rfl` for Decidable value fixing when the proof-irrelevance rfl pattern fails

## Current frontier

Proposition6_6_6.lean: 4 sorries remain across 3 declarations:

1. **`equivAt_eq_sink` (line ~591)**: Reverse direction `ker Φ ≤ range ψ`. Needs explicit preimage construction through direct sum decomposition.

2. **`Proposition6_6_6_sink` naturality (lines ~695, ~700)**: Two sorry cases:
   - ne/eq: arrow a → i with equivAt_eq_sink at target
   - ne/ne: requires composing `reflFunctorMinus_mapLinear_ne_ne` + `reflFunctorPlus_mapLinear_ne_ne` + `reversedArrow_ne_ne_twice` through deeply nested Decidable.casesOn layers

3. **`Proposition6_6_6_source` (line ~719)**: Dual of sink case (same infrastructure needed).

### Blocker analysis

All remaining sorries involve fighting `Decidable.casesOn` opacity in deeply nested terms. The ne/ne naturality case is particularly hard because `ρ_dr.mapLinear` involves TWO layers of Decidable branching (F⁻ then F⁺), and the equivAt_ne_sink involves two more. After unfolding, the goal has `Decidable.rec` terms with `inst✝³ a.fst i` for arrow vertices, which can't be fixed by the standard Decidable-matching technique.

A better approach might be: define helper lemmas that relate `equivAt_ne_sink` to the composition `reflFunctorPlus_equivAt_ne ∘ reflFunctorMinus_equivAt_ne`, then use the API lemmas without unfolding.

## Overall project progress

- ~207/583 items sorry_free in items.json (~35.5%)
- Proposition6_6_6: 4 sorries down from 5 (reversedArrow_ne_ne_twice proved)

## Next step

- Create sub-issues decomposing the remaining work:
  - equivAt_ne_sink decomposition lemma (relate to equivAt_ne composition)
  - ne/ne naturality using API lemmas + decomposition
  - equivAt_eq_sink reverse direction
  - source case (dual of sink)

## Blockers

Decidable.casesOn opacity in deeply nested reflection functor compositions.
