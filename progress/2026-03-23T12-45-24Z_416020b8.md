## Accomplished

- Proved 4 helper lemmas for Theorem 5.25.2 decomposition:
  - `detFun_mem_principalSeries`: μ∘det is in the principal series submodule for (μ,μ)
  - `augmentation_detFun_ne_zero`: aug(μ∘det) = |G| ≠ 0
  - `ker_augOnPrincipalSeries_eq`: ker(aug on principalSeries) = complementWSubmodule
  - `augOnPrincipalSeries_surjective`: augmentation restricted to principalSeries is surjective

- Proved augmentation equivariance: `augOnPrincipalSeries_equivariant`
  - aug(ρ(g)f) = μ(det g)·aug(f), using `Fintype.sum_equiv (Equiv.mulRight g)`

- Constructed 3 FDRep morphisms with full equivariance proofs:
  - `augMorphism : principalSeries p n mu mu ⟶ detChar p n mu`
  - `complementWInclusion : complementW p n mu ⟶ principalSeries p n mu mu`
  - `detCharEmbedding : detChar p n mu ⟶ principalSeries p n mu mu`

- Structured `principalSeries_decomp` proof via IsSplitMono + cokernel:
  - Maschke's theorem gives IsSplitMono for detCharEmbedding
  - `cokernelIsCokernel` + `binaryBiconeOfIsSplitMonoOfCokernel` gives V(μ,μ) ≅ ℂ_μ ⊞ cokernel(emb)
  - Remaining: identify cokernel(emb) ≅ W_μ

- Added 3 helper declarations structuring remaining work:
  - `detChar_simple`: ℂ_μ is Simple (1-dimensional rep)
  - `detCharEmbedding_ne_zero`: embedding is nonzero
  - `detCharEmbedding_mono`: embedding is mono

## Current frontier

- `principalSeries_decomp`: Has IsSplitMono structure; needs cokernel(emb) ≅ W_μ identification
- `complementW_simple`: Not started; requires ⟨χ_V,χ_V⟩ = 2 computation or finrank argument
- `detChar_simple`: Needs bridge from finrank=1 to categorical Simple in Action category
- `detCharEmbedding_ne_zero`: Needs navigating FDRep/FGModuleCat type coercions
- `detCharEmbedding_mono`: Follows from Simple + ne_zero (once those are proved)

## Overall project progress

- Theorem5_25_2.lean: 13 sorry warnings (was 10 after previous session, but 3 new structured helper declarations added)
- The 4 proved helper lemmas and 3 FDRep morphism constructions (from previous commit) are the main value
- The principalSeries_decomp proof outline shows the categorical approach works modulo cokernel identification
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free

## Next step

- Prove `detChar_simple`: look for `Action.simple_of_forget_simple` or prove via `simple_iff_mono_isZero_or_isIso` using finrank argument on underlying module
- Prove `detCharEmbedding_ne_zero`: navigate FDRep → FGModuleCat → ModuleCat coercions to show emb(1) = ⟨g ↦ μ(det g)⟩ ≠ 0
- Then `detCharEmbedding_mono` follows from Simple + ne_zero
- For cokernel(emb) ≅ W_μ: construct G-equivariant map `complementWInclusion ≫ cokernel.π emb` and show it's an iso (both have dim q)
- Alternative: use character comparison via Corollary 4.2.4

## Blockers

- Type coercion complexity: FDRep → FGModuleCat → ModuleCat layers make direct element-level proofs difficult
- No existing Mathlib API for "Action.simple_of_finrank_eq_one" — may need custom bridge lemma
