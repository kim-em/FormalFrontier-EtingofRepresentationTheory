## Accomplished

Factored the proof of `vandermonde_cauchy_diagonal` (PowerSumCauchyBilinear.lean) into two clean steps:

1. **`alternating_coeff_eq_cauchyRHS_coeff`** (new helper, sorry'd): the double alternating sum of `fullCauchyProd` coefficients equals the corresponding `cauchyRHS` coefficient. This isolates the deep algebraic step (Cauchy determinant formula in FPS form).

2. **`vandermonde_cauchy_diagonal`** (now proved): follows immediately from the helper + `cauchyRHS_coeff_bilin_of_injective`.

Created sub-issue #1747 for the remaining sorry in the helper.

## Current frontier

`alternating_coeff_eq_cauchyRHS_coeff` at line ~729 of `PowerSumCauchyBilinear.lean` — this requires either:
- Cauchy-Binet expansion at the FPS coefficient level, or
- Direct proof of `cauchyRHS = Δ_x · Δ_y · fullCauchyProd` as a formal power series identity

## Overall project progress

- Sorry count in PowerSumCauchyBilinear.lean: 2 (unchanged — `card_sigma_fiberPerm_eq_factorial_mul` + new helper replacing old sorry)
- `vandermonde_cauchy_diagonal` now has a clean proof structure instead of a bare sorry
- The proof factorization clarifies what remains: the Cauchy determinant at the coefficient level

## Next step

Prove `alternating_coeff_eq_cauchyRHS_coeff` (#1747). The recommended approach is Cauchy-Binet:
1. Factor `[x^r] fullCauchyProd = ∏_i h_{r_i}(y)` (x-variables separate by row)
2. x-alternating sum → `det(h_{α_i-j}(y))` via Leibniz
3. y-alternating sum → cauchyRHS coefficient via Cauchy-Binet for `det(cauchyMatrix)`

Difficulty is ~7, significantly harder than the original issue estimated (5).

## Blockers

None — the sub-issue #1747 captures the remaining work cleanly.
