## Accomplished

Issue #1408: Stage 3.2 Ch6 branch_classification and n=0 case for Dynkin classification.

1. **Eliminated n=0 sorry** (line 1852 → removed): Added `(hn : 1 ≤ n)` hypothesis to both `dynkin_classification_forward` and `Theorem_Dynkin_classification`. The n=0 case was genuinely unprovable because `IsDynkinDiagram 0 adj` is vacuously true (all conditions quantify over `Fin 0`) but no `DynkinType` has rank 0. The empty graph is not a Dynkin diagram mathematically, so requiring n ≥ 1 is the correct fix.

2. **Proved `arm_length_solutions`** (~30 lines): New helper lemma proving the complete enumeration of solutions to the reciprocal-sum constraint `1/(p+1) + 1/(q+1) + 1/(r+1) > 1` with `1 ≤ p ≤ q ≤ r`. The solutions are: (1,1,r) for r ≥ 1, (1,2,2), (1,2,3), (1,2,4). Uses `nlinarith` with explicit multiplication bounds.

3. **Documented branch_classification proof strategy**: Added detailed comments documenting the 4 remaining sub-steps: (a) arm extraction from tree structure, (b) positive-definiteness constraint derivation, (c) D_n isomorphism construction, (d) E₆/E₇/E₈ isomorphism construction.

4. **Fixed pre-existing build errors** in `dynkin_edge_count` (~80 lines changed):
   - Moved `adj_sum_eq_degree` before `dynkin_edge_count` (was a forward reference)
   - Fixed Mathlib API breakage: `smul_eq_mul` → explicit show blocks, `SimpleGraph.Connected` constructor, `Std.Irrefl` wrapping for `loopless`, `Reachable.refl` → `⟨.nil⟩`, `SimpleGraph.degree`/`neighborFinset` simp lemmas

**Sorry count**: 4 → 3 (eliminated n=0 sorry; remaining: 2 in path_walk_construction [issue #1407], 1 in branch_classification)

## Current frontier

`branch_classification` remains sorry'd. The `arm_length_solutions` helper lemma provides the arithmetic core. Remaining work:
- Formalize arm extraction from tree with degree-3 vertex
- Derive reciprocal-sum constraint from positive definiteness (via subgraph_contradiction with null vector on T_{p,q,r})
- Construct explicit graph isomorphisms for D_n, E₆, E₇, E₈

## Overall project progress

- ~193+/583 items sorry-free
- Theorem_Dynkin_classification.lean: 4 → 3 sorries (1 eliminated, build errors fixed)
- Pre-existing build errors in dynkin_edge_count now fixed

## Next step

- The `branch_classification` sorry needs: (1) arm extraction infrastructure, (2) null vector construction for non-Dynkin T_{p,q,r}, (3) explicit isomorphism construction for D_n, E₆, E₇, E₈. Each is a substantial sub-task. Consider splitting into separate issues.
- The `path_walk_construction` sorries (issue #1407) are independent and being worked on.

## Blockers

None.
