## Accomplished

- Merged PR #2278 (degree ≥ 4 and triangle cases for non_ade_graph_not_finite_type decomposition)
- Decomposed the single sorry in `not_posdef_infinite_type` into 3 clean sub-lemmas:
  1. `chordless_cycle_infinite_type` (PROVED) - direct corollary of `subgraph_infinite_type_transfer` + `cycle_not_finite_type`
  2. `graph_with_list_cycle_infinite_type` (sorry) - cycle in graph → infinite type
  3. `acyclic_deg_le_2_posdef` (sorry) - connected acyclic graph with all degrees ≤ 2 → positive definite
  4. `acyclic_branch_not_posdef_infinite_type` (sorry) - connected acyclic graph with branch point + not posdef → infinite type
- Main theorem `not_posdef_infinite_type` now has clean case structure:
  - Degree ≥ 4: `degree_ge_4_infinite_type` (done)
  - Has cycle: `graph_with_list_cycle_infinite_type` (sorry)
  - Acyclic, all degrees ≤ 2: `acyclic_deg_le_2_posdef` (sorry, contradiction)
  - Acyclic, has branch point: `acyclic_branch_not_posdef_infinite_type` (sorry)
- Created sub-issues for each helper: #2283, #2284, #2285
- Added dependency: #2285 depends on #2284

## Current frontier

Three helper lemmas need proving in `InfiniteTypeConstructions.lean`:
- `graph_with_list_cycle_infinite_type` (~line 2550): strong induction on cycle length, handle chords
- `acyclic_deg_le_2_posdef` (~line 2564): quadratic form decomposition + 2-regular cycle argument
- `acyclic_branch_not_posdef_infinite_type` (~line 2589): tree structure analysis + forbidden subgraph embeddings

## Overall project progress

The `not_posdef_infinite_type` sorry is now decomposed into 3 well-scoped sub-problems.
The `non_ade_graph_not_finite_type` theorem (which depends on `not_posdef_infinite_type`) will be
fully proved once these 3 helpers are closed. All representation-theoretic content is complete;
only graph-theoretic classification remains.

## Next step

Work on #2283 (cycle case) or #2284 (path case) - both are difficulty 6 and independent.
#2285 (branch case) is difficulty 8 and depends on #2284.

## Blockers

None - all three sub-issues are actionable.
