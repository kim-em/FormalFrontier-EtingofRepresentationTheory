## Accomplished

- Proved `dynkin_no_cycle` fully — cycle embedding into parent graph + null vector B(1,...,1) ≤ 0
- Created partial PR #1371 closing issue #1283 with `replan` label
- Reduced sorry count in `Theorem_Dynkin_classification.lean` from 6 to 4

## Current frontier

4 sorry's remain in `Theorem_Dynkin_classification.lean`:

1. **Line 1485: `dynkin_edge_count`** — tree has n-1 edges. Not used by anything; could be deleted.
2. **Line 1554: `path_walk_construction`** — most impactful. Needs greedy walk from endpoint in connected acyclic graph with max degree 2. Returns `Fin n ≃ Fin n` with adjacency properties. Substantial inductive construction.
3. **Line 1608: `branch_classification`** — degree-3 branch → D_n or E_{6,7,8}. Needs arm-length enumeration + explicit graph isomorphisms. Most complex remaining sorry.
4. **Line 1625: n=0 edge case** — `IsDynkinDiagram 0 adj` is vacuously true but no `DynkinType` has rank 0. Minor statement limitation.

## Overall project progress

- ~194/583 items sorry-free (33.3%), ~100 sorries across ~37 files
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- Ch6: Dynkin classification forward direction has proof skeleton with 4 sorry's (was 6)

## Next step

Recommended decomposition for the `replan` issue:
1. **`path_walk_construction`** (medium): Build walk by induction on n. For n=1 trivial. For n≥2, endpoint has one neighbor; walk to it, recurse on remaining graph. Key challenge: managing `Fin n` vs `Fin (n-1)` in Lean.
2. **`branch_classification`** (hard): Split into sub-lemmas: (a) unique degree-3 vertex, (b) arm-length extraction, (c) 1/p+1/q+1/r > 1 constraint from pos-def, (d) explicit isomorphisms for D_n, E_6, E_7, E_8.
3. **n=0 edge case** (easy): Add `hn : 1 ≤ n` hypothesis to `dynkin_classification_forward` or handle via `Fin 0 → False`.

## Blockers

None — all sorry's compile and proof strategies are documented inline.
