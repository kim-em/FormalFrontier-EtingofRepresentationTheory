## Accomplished

Closed `dTildeRep_mapLinear_transport` (Wall 2 Stage C residual,
issue #2479) using **Strategy A** from the issue: refactor
`dTildeRepMap` to replace the `▸`-cast pattern with an explicit
`LinearEquiv.funCongrLeft ∘ finCongr` reindexing (new `dTildeCast`
helper).  After the refactor each of the 8 arrow patterns reduces by
`rfl` once the correct `if`-branch of `dTildeRepMap` is selected.

Concretely:

- Introduced `dTildeCast {p q p' q'}` helper that rewraps a linear map
  between `Fin p → ℂ` and `Fin q → ℂ` through dimension equalities via
  `funCongrLeft ∘ finCongr` (val-preserving), plus a supporting
  `dTildeCast_apply` doc lemma.
- Rewrote `dTildeRepMap` all 6 non-zero branches to use `dTildeCast`
  rather than `ha ▸ hb ▸ primitive`, leaving the overall type
  signature identical.
- Added 6 branch-unfolding helper lemmas
  (`dTildeRepMap_starEmbed1_zero_two`, …,
  `dTildeRepMap_starEmbed2_right`) — one per concrete arrow family.
  Each does `unfold dTildeRepMap` plus `dif_neg`/`dif_pos` to select
  the matching branch.
- Replaced the `sorry` in `dTildeRep_mapLinear_transport` with a match
  on `a, b, e'` covering all 8 DTildeVertex arrow patterns.  Each case
  applies the corresponding helper lemma and closes with `rfl`.

**File:** `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`
(~222 lines added, 28 removed).

**Sorry count in file:** 4 → 3 (matches the issue's verification
criterion).  The three remaining sorries are the pre-existing Ẽ/T
sub-sorries at lines ~3344, ~3599, ~3826, gated on framework decision
(#2436) and outside this issue's scope.

**Verification:**

- `lake build EtingofRepresentationTheory.Chapter6.InfiniteTypeConstructions`
  succeeds.
- Full `lake build` succeeds (no errors; only pre-existing linter
  warnings unchanged by this PR).
- `dTildeRep_isIndecomposable` and `dTilde_not_finite_type` continue
  to compile.
- Sanity checks at lines ~2228–2235 (`.dim m = _ := rfl`) unaffected.

## Current frontier

Wall 2 Stage C now fully closed.  `dTildeRep_isIndecomposable`
requires no remaining sorries in its transitive dependencies (modulo
the pre-existing, unrelated Ẽ/T sub-sorries gated on #2436).

## Overall project progress

- **Wall 2 (D̃_n indecomposability):** completed via this PR.  All
  Stage A/B/C mathematical content merged; transport bridge closed.
- **Wall 3 (Garnir straightening):** still open at #2450.  Two prior
  refutations; whole-sum strategy recommended; no progress this turn.
- **Schur-Weyl chain:** #1 ✅, #4 ✅, bimodule foundation pieces
  landed (#2467, #2473, #2476); #2a (#2477) and #2b/#3 (#2458
  bimodule-assembly blocked) remain.

## Next step

- A planner should close #2479 once this PR merges and refresh
  `progress/design-walls-wave55.md` / `progress/sorry-landscape.md` to
  reflect Wall 2 closure (summarize #2469 already queued).
- The only open Wall 2-adjacent thread left is the Ẽ/T framework
  (#2436) — independent track.
- Remaining feature work gravitates around Schur-Weyl #2a/b and the
  Garnir Wall 3 proof.

## Blockers

None for this issue.
