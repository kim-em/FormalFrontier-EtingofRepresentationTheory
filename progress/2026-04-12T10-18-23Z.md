## Accomplished
- Proved `acyclic_deg_le_2_posdef` structure via strong induction on vertex count (issue #2284)
- Base case (n=1): QF = 2x², positive for nonzero x
- Inductive step: leaf identification, unique neighbor, QF decomposition at leaf, transfer to reduced graph, completing-the-square argument
- Reduced from 1 full sorry to 2 targeted sub-goals
- Created PR #2290 with auto-merge enabled

## Current frontier
Two sorries remain in `acyclic_deg_le_2_posdef`:
1. **Connectivity preservation** (`hconn'`, ~line 2737): When removing a leaf from a connected acyclic graph, the remaining graph stays connected. Mathematically clear (paths in a tree between non-leaf vertices don't use the leaf) but requires formalizing path manipulation.
2. **2-regular acyclic → contradiction** (`h_has_leaf`, ~line 2893): A connected acyclic graph where all vertices have degree 2 must contain a cycle. Needs walk construction + pigeonhole.

## Overall project progress
- Stage 3 formalization ongoing across multiple items
- `acyclic_deg_le_2_posdef` proof structure complete (320 lines added), 2 sorries remain
- This unblocks issue #2285 (`non_ade_graph_not_finite_type` completion) once sorries are filled

## Next step
- Fill the 2 remaining sorries in `acyclic_deg_le_2_posdef` (could be a follow-up issue)
- Or pick up issue #2285 which can now make progress using the partial proof

## Blockers
- None blocking PR merge; the 2 sorries are self-contained sub-problems
