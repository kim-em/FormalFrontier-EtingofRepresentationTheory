## Accomplished

- **Proved `star_adj_of_deg3_n4`**: For a Dynkin diagram on 4 vertices with a degree-3 vertex v,
  the adjacency is a star: `adj i j = if (i = v) = (j = v) then 0 else 1`. Uses edge count
  argument to show non-hub vertices have no edges between them (adding any such edge would make
  degree sum ≥ 8, but edgeCount = 3 requires sum ≤ 7).

- **Proved `branch_classification_n4`**: The n=4 base case of branch_classification. Any Dynkin
  diagram on 4 vertices with a degree-3 vertex is isomorphic to D₄. Constructs `Equiv.swap ⟨1,_⟩ v`
  as the isomorphism mapping D₄'s branch vertex (position 1) to the abstract branch vertex v.

- **Fixed pre-existing build errors**: The `star_adj_of_deg3_n4` proof had multiple issues:
  - `subst` on equalities with parameter `v` was removing `v` from scope (fixed by avoiding subst)
  - `rw` on if-conditions with dependent decidability instances caused motive errors (fixed with simp)
  - Missing `hv` (degree=3) in omega/linarith calls (fixed by explicit hypothesis threading)
  - `dynkin_edge_count hD` needed `hD` intact but it was destructured (fixed with `have` instead of `obtain`)

- **Restructured `branch_classification`**: Now handles n≥4 via case split: n=4 delegates to
  `branch_classification_n4`, n≥5 remains sorry with detailed proof outline.

## Current frontier

- `branch_classification` n≥5 case (1 sorry remaining)
- This is the last sorry in the Dynkin classification chain

## Overall project progress

~195+/583 items sorry-free (~33.5%), ~67 remaining sorries across ~28 files.
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- Ch5: ~45 sorries
- Ch6: ~34 sorries (branch_classification n≥5 is the key remaining piece for classification)
- Ch9: ~8 sorries
- Ch2: ~3 sorries

## Next step

The n≥5 case of `branch_classification` requires substantial infrastructure:
1. **Unique branch vertex**: Show v is the only degree-3 vertex (via subgraph_contradiction
   with extended Dynkin diagrams)
2. **Spider structure**: All non-v vertices have degree ≤ 2, so tree is a spider
3. **Arm length extraction**: Three neighbors of v lead to paths of lengths p, q, r
4. **Reciprocal sum constraint**: Positive-definiteness → 1/(p+1)+1/(q+1)+1/(r+1) > 1
5. **Classification**: Apply `arm_length_solutions` (already proved)
6. **Isomorphism construction**: For each of the 4 solution families (D_n, E₆, E₇, E₈),
   construct the explicit Fin permutation

Steps 1-2 could be a separate lemma. Step 3 requires path walk machinery.
Steps 4-6 are case analysis with explicit constructions.

Consider splitting into sub-issues: unique_branch_vertex, spider_arm_extraction,
spider_iso_construction.

## Blockers

- The n≥5 proof requires arm extraction infrastructure not yet in the file
- Formalizing "remove a leaf from a Dynkin diagram" or "extract spider arms" involves
  complex Fin index manipulation and submatrix positive-definiteness arguments
