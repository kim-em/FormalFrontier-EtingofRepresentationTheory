## Accomplished

Continued work on issue #1401: Proposition6_6_6 reflection functor round-trip (F⁻F⁺V ≅ V).

### Structural progress in `equivAt_eq_sink`
- Built complete first-isomorphism-theorem proof structure: (⊕F⁺(V)_j)/ker(Φ) ≅ V_i
- Forward map Φ : ⊕F⁺(V)_j → V_i constructed and proved surjective
- Source map ψ : F⁺(V)_i → ⊕F⁺(V)_j constructed
- range(ψ) ≤ ker(Φ) proved
- Quotient construction via `Submodule.quotEquivOfEq` + `quotKerEquivOfSurjective` complete
- Created `exact_of_dim` helper proving ψ.range = Φ.ker from injectivity + dimension equation

### Remaining sorries (5 total, 3 declarations)
1. **`equivAt_eq_sink`** (2 sorries): `hψ_inj` (injectivity of ψ) and `hdim` (dimension equation)
2. **`Proposition6_6_6_sink`** (2 sorries): naturality cases (a≠i,b=i) and (a≠i,b≠i) — blocked by Decidable.casesOn opacity
3. **`Proposition6_6_6_source`** (1 sorry): dual case, same infrastructure needed

### PR created
- PR #1587: https://github.com/kim-em/FormalFrontier-EtingofRepresentationTheory/pull/1587
- Auto-merge enabled
- Build passes with 3 sorry warnings (same 3 declarations as on main)

## Current frontier

The `Decidable.casesOn` opacity remains the core blocker for the naturality cases. When two Quiver instances coexist (`inst` and `instR = reversedAtVertex Q i`), rewriting Decidable equality facts fails because they appear in dependent types that can't be abstracted. This affects:
- Naturality proof in `Proposition6_6_6_sink` (a≠i, b≠i case)
- The dimension equation `hdim` in `equivAt_eq_sink`

## Overall project progress

- **Phase 3.2 (Proof Formalization)**: 560/583 items sorry-free (96.1%)
- **75 sorries** across 28 files
- Proposition6_6_6 remains at 3 declarations with sorry (same count as main, but internal structure improved)
- Chapters 3, 4, 7, 8 remain 100% sorry-free

## Next step

1. **Decidable.casesOn workaround**: The naturality (a≠i, b≠i) case needs a different approach — possibly `congr_arg` with explicit motive, or proving the equality at the level of `Quiver.Hom` before lifting to representation maps
2. **Injectivity of ψ** (`hψ_inj`): May follow from ψ being the inclusion of ker into the direct sum
3. **Dimension equation** (`hdim`): Needs `finrank(F⁺(V)_i) + finrank(V_i) = finrank(⊕F⁺(V)_j)`, which follows from the short exact sequence 0 → F⁺(V)_i → ⊕F⁺(V)_j → V_i → 0

## Blockers

- **Decidable.casesOn opacity** (#1401): Persistent across 4+ waves. The two-Quiver-instance setup produces opaque `Decidable.rec` terms that resist `rw`/`simp`/`dsimp`. This blocks naturality proofs and potentially the dimension equation.
- **SchurModule** remains the mega-blocker (~27% of remaining sorries transitively blocked)
