## Accomplished

Feature session `cbaf28df` — completed issue #2573: propagate
`Module.Finite k (V i)` (S-side / Specht-side carriers) into the
existential output of three consecutive bimodule-decomposition
theorems in the Schur-Weyl chain.

**Changes**:

1. `Chapter5/Theorem5_18_1.lean` —
   `Theorem5_18_1_bimodule_decomposition` existential now carries
   `(_ : ∀ i, Module.Finite k (V i))` (between distinctness and the
   `L` block, mirroring the existing `Module.Finite k (L i)` clause
   at the tail of the `L` block).

   The witness was already in scope via the `haveI : ∀ c, Module.Finite
   k ↥(V' c)` on line 595-597, so the discharge in the `refine` is
   `fun _ => inferInstance`.

2. `Chapter5/Theorem5_18_4.lean` —
   `Theorem5_18_4_bimodule_decomposition` existential gains
   `(_ : ∀ i, Module.Finite k (S i))` and the `obtain` / `refine`
   thread the new clause as `hS'_fin`.

3. `Chapter5/Theorem5_18_4.lean` —
   `Theorem5_18_4_GL_rep_decomposition` existential gains
   `(_ : ∀ i, Module.Finite k (S i))` and the `obtain` / `exact`
   thread the new clause.

The propagation chain is now: the proof of `Theorem5_18_1_bimodule_decomposition`
constructs `Module.Finite k ↥(V' c)` for each isotypic component (already
in scope), exposes it through the existential, and downstream theorems
re-export it. Future consumers no longer need to re-derive it from
`IsSimpleModule` + finite-dim `symGroupImage`.

## Verification

- `lake build EtingofRepresentationTheory.Chapter5.Theorem5_18_1` — pass.
- `lake build EtingofRepresentationTheory.Chapter5.Theorem5_18_4` — pass.
- `lake build EtingofRepresentationTheory.Chapter5.FormalCharacterIso`
  — pass (key consumer; uses
  `Theorem5_18_1_bimodule_decomposition_explicit` which is *not*
  modified, so this only confirms no transitive break).
- All warnings are pre-existing (style/maxHeartbeats notes,
  `SpechtModuleBasis.lean` sorry placeholders).

## Construction notes

- The existing `glTensorRep_equivariant_schurWeyl_decomposition`
  consumer (PR #2540 / commit 0cc1dc7) uses the *explicit* variant
  `Theorem5_18_1_bimodule_decomposition_explicit`, which is a
  different theorem and was not part of #2573's deliverables. Its
  re-derivation of `Module.Finite k (↥(V' i))` (lines 818-820 of
  `FormalCharacterIso.lean`) remains in place and still compiles —
  it's now slightly redundant for any future explicit consumer that
  would mirror this propagation pattern, but #2573 was scoped to the
  non-explicit chain only.

- A natural follow-up would be the same propagation for the
  `_explicit` variants, but that's not in scope here.

## Current frontier

- #2573 completed via this PR.
- Five unclaimed issues remain in the queue (#2564 Mathlib upstream
  PR, #2565 [CharZero k] hygiene, #2570 / #2571 doc-string fixes,
  #2574 heartbeat tightening) — all small hygiene items.
- #2493 (Schur-Weyl L_i part C, `schurWeyl_gl_decomposition`) is
  claimed but listed as `claimed 3 days ago` per `coordination
  orient`. May need a stale-claim release if no PR appears.

## Overall project progress

Stage 3 formalization. The bimodule-decomposition existential output
is tightened: each new universe-polymorphic theorem in the chain now
ships `Module.Finite k (S i)` directly. No new sorries; no removed
sorries; this is a signature-strengthening refactor.

## Next step

- Workers: claim any of the remaining hygiene issues (#2564, #2565,
  #2570, #2571, #2574).
- Possible follow-up issue: mirror the same `Module.Finite k (S i)`
  propagation for `Theorem5_18_1_bimodule_decomposition_explicit` and
  `Theorem5_18_4_bimodule_decomposition_explicit`, then refactor
  `glTensorRep_equivariant_schurWeyl_decomposition` to drop its
  re-derivation (lines 818-820 of `FormalCharacterIso.lean`).

## Blockers

None.
