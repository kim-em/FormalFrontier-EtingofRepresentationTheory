## Accomplished

- Proved `complementW_finrank` (dim W_μ = p^n) — main deliverable of issue #1645
- Built full coset infrastructure for GL₂(𝔽_q) / B decomposition:
  - `cosetBorel` definition (Borel component of coset decomposition)
  - `cosetBorel_mul_cosetRep` (g = cosetBorel(g) · rep(cosetIndex(g)))
  - `cosetIndex_borel_mul` (B-invariance of coset index)
  - `borelCharValue_mul` (multiplicativity on Borel subgroup)
  - `cosetBorel_borel_mul` (B acts on coset decomposition)
  - `mkCovariantFun`, `mkCovariantFun_mem`, `mkCovariantFun_eval`
  - `cosetIndex_cosetRep`, `borelCharValue_one`, `cosetBorel_cosetRep`
- Proved `principalSeries_eval_surjective` (any values on coset reps can be realized)
- Proved `complementW_eval_injective` (evaluation at "some t" reps is injective on W_μ)
- Partially proved `complementW_eval_surjective` (1 sorry remains: fiber reindexing)
- Proved `complementW_finrank` via LinearEquiv.ofBijective with evaluation map
- Created PR #1656 closing issue #1645

## Key patterns discovered

- `Fintype.sum_fiberwise` for splitting sums by fibers of a map
- `Units.val_inv_eq_inv_val` + `mul_inv_cancel₀ (Units.ne_zero _)` for unit coercion cancellation
- `mul_right_cancel` for proving coset decomposition uniqueness
- `GaloisField.card p n hn.ne'` needs `hn : 0 < n` explicitly in scope
- Section variables in Lean 4 are only auto-included when mentioned in the type, not just the proof body

## Current frontier

- PR #1656 reduces sorries in Theorem5_25_2.lean from 13 to 10
- Remaining 10 sorries:
  1. `principalSeries_simple_of_ne` (issue #1646)
  2. `complementW_eval_surjective` — 1 sorry for `∑_g v(cosetIndex g) = 0` via fiber reindexing
  3. `principalSeriesSubmodule_finrank`
  4. `augOnPrincipalSeries_surjective`
  5. `detChar_simple`, `detCharEmbedding_ne_zero`, `detCharEmbedding_mono`
  6. `principalSeries_decomp` (issue #1647)
  7. `complementW_simple` (issue #1647)
  8. `complementW_iso_implies_eq`
  9. `Theorem5_25_2_part3b`

## Overall project progress

- ~197/583 items sorry-free (~34%)
- Theorem5_25_2.lean: reduced from 13 to 10 sorries
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free

## Next step

- The 1 remaining sorry in `complementW_eval_surjective` needs an Equiv between
  GL₂(𝔽_q) and B × Option(GaloisField p n) to reindex the sum, or alternatively
  showing all cosetIndex fibers have size |B| to factor `∑_g v(cosetIndex g) = |B| * ∑_i v(i)`
- Issues #1646 and #1647 are ready for work

## Blockers

None.
