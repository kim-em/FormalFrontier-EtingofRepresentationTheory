## Accomplished

- **Proved `tree_embed_adj_eq`** (fully, no sorry): If an injective map from a connected acyclic subgraph to an acyclic host graph preserves edges, then it preserves ALL adjacencies. Key lemma for variable-size D̃ embedding.
- **Scaffolded `leaf_case` proof structure**: Extracts Nodup chain from v₀ to w, identifies side arm neighbor of v₀, extracts w's two arm neighbors, sets up D̃ embedding parameters.
- **Added sorry'd helpers**: `dTilde_nodup_path_between` (D̃ connectivity via Nodup paths), `walk_to_nodup_path` (trim walks to simple paths in trees).

## Current frontier

The `leaf_case` sorry at line 6438 needs the actual D̃ embedding construction: define φ mapping D̃ vertices to tree vertices, prove injectivity, prove edge preservation, apply `tree_embed_adj_eq` for hembed, apply `subgraph_infinite_type_transfer`.

Three sorry'd helpers need proofs:
1. `walk_to_nodup_path` — strong induction on walk length, splicing at repeated vertices
2. `dTilde_nodup_path_between` — construct explicit paths via D̃ spine structure
3. Path extraction sorry's in leaf_case (walk length ≥ 2, Nodup path length ≥ 3)

## Overall project progress

Issue #2363 partially addressed. The key theoretical contribution (`tree_embed_adj_eq`) is complete. The remaining work is mechanical: constructing the explicit D̃ embedding and proving D̃ connectivity. These are well-scoped tasks suitable for follow-up issues.

## Next step

1. Fill `dTilde_nodup_path_between` by constructing explicit paths through D̃'s spine
2. Fill `walk_to_nodup_path` by strong induction on walk length
3. Complete the D̃ embedding φ construction in leaf_case
4. Prove φ is injective (all mapped vertices are distinct in the tree)
5. Prove edge preservation (D̃ edges map to tree edges)

## Blockers

None — all remaining work is well-defined and decomposable.
