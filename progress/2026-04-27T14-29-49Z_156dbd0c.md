## Accomplished

Completed audit of PR #2504 (Schur-Weyl L_i FDRep GL_N structure) per
issue #2560.

- Verified Q1-Q6 against the actual diff and Mathlib references:
  - Q1 `centralizerModuleHom_smulCommClass` (PASS): proof-correct,
    no diamond risk (only declaration site project-wide).
  - Q2 extended `Theorem5_18_1_bimodule_decomposition` signature
    (PASS): both new clauses (`SMulCommClass`, `Module.Finite k
    (L i)`) discharge correctly via `Module.Finite.of_injective` +
    `LinearMap.restrictScalarsₗ`.
  - Q3 `Theorem5_18_4_GL_rep_decomposition` (PASS): `glHom`
    membership / `map_one'` / `map_mul'` / `Module.toModuleEnd` /
    `FDRep.of` chain all wire through; `(L i : Type u) = L' i`
    defeq via `FGModuleCat.of_carrier`.
  - Q4 downstream usability (DEFECT planning): two gaps vs.
    `glTensorRep_equivariant_schurWeyl_decomposition` (#2540):
    `Module.Finite k (S i)` not propagated, and the iso is opaque
    (no explicit-eval form to support equivariance proof).
  - Q5 doc-strings: irreducibility overclaim + stale signature
    description.
  - Q6 heartbeat budgets: minor hygiene.
- Posted consolidated review comment on #2560 (PASS-with-followups).
- Filed five follow-up issues:
  - F1 → #2570 (doc-string fix, low)
  - F2 → #2571 (doc-string update, low)
  - F3 → #2572 (`_GL_rep_decomposition_explicit` analogue, medium —
    unblocks #2540)
  - F4 → #2573 (propagate `Module.Finite k (V i)`, low)
  - F5 → #2574 (heartbeat hygiene, low)
- Audit report at
  `progress/reviews/2026-04-28-Li-FDRep-GL-N.md`.

## Current frontier

Audit complete. PR #2504 stands as-is (no code changes from this
review). Downstream chain remains blocked on #2540 (claimed) which
will likely consume #2572 (the explicit-eval analogue).

## Overall project progress

Schur-Weyl chain audit progress:
- #2547 (PRs #2538/#2539 equivariance) — done.
- #2554 (PRs #2528/#2551 plumbing) — done.
- #2557 (PR #2509 explicit bimodule decomposition) — claimed
  (in-flight).
- **#2560 (PR #2504 L_i FDRep GL_N) — done this turn.**
- #2561 (PR #2511 polyRightTransl + tgtGLAction) — unclaimed.

The remaining un-audited critical-path PR on the Schur-Weyl chain
is #2511 (audited under issue #2561). After that, the chain's
foundations are fully covered.

## Next step

Concrete recommendations:
1. **Highest impact**: #2572 (F3) — extract / land the
   `_GL_rep_decomposition_explicit` analogue. Coordinate with the
   in-flight worker on #2540, who likely needs this internally.
2. **Hygiene**: claim #2570/#2571 (doc-string fixes), #2573 (Module.Finite
   propagation), and #2574 (heartbeat tightening) in a single
   cleanup PR if a worker is available.
3. **Audit completion**: the next review session should claim #2561
   (PR #2511 foundations audit) so the Schur-Weyl chain is fully
   covered.

## Blockers

None for this audit. Downstream #2540 stays claimed; #2493 stays
blocked on #2540.
