## Accomplished

Resolved 7 of 8 sorrys in `CoxeterInfrastructure.lean`:

1. **`subsingleton_hom_reversedAtVertex`** — proved by unfolding `ReversedAtVertexHom` and case-splitting on `DecidableEq` instances
2. **`subsingleton_hom_iteratedReversed`** — proved (was already working from prior session)
3. **`reflFunctorPlus_free_ne`** — proved via `Module.Free.of_equiv` transport
4. **`reflFunctorPlus_free_eq`** — proved via `Module.Free.of_equiv` with `addCommGroupOfRing` for PID freeness
5. **`reflFunctorPlus_finite_ne`** — proved via `Module.Finite.equiv` transport
6. **`reflFunctorPlus_finite_eq`** — proved via `Module.Finite.equiv` with same pattern
7. **`simpleReflectionDimVector_eq_simpleReflection`** — the bridge lemma connecting quiver representation dimension vectors to algebraic simple reflections. Three sub-proofs:
   - `hdot`: dot product computation with Cartan matrix `(2I - adj)`, expanding via `Matrix.sub_apply`/`smul_apply`/`one_apply` and distributing
   - `hcard`: `Fintype.card(j ⟶ v) = adj j v` for Dynkin orientations, case-splitting on `adj j v ∈ {0,1}`
   - `hsum`: Sigma sum decomposition `∑_{(j,e) : ArrowsInto} d(j) = ∑_j adj(j,v) * d(j)`, requiring careful Fintype instance management with explicit `Sigma.instFintype` and `Finset.sum_congr` to bridge between the statement-level and proof-level Fintype instances

**Key technical challenges solved:**
- Fintype instance mismatch between statement-level `haveI` and proof-level instances for `ArrowsInto` sums — resolved by abstracting over instances with `∀ (inst1 inst2 : Fintype ...), ∑ = ∑`
- Matrix `2 • 1` coercion: `nsmul_eq_mul` + `Nat.cast_ofNat` creates `(2 * 1 : Matrix)` which needs careful handling vs `(2 : ℤ) * (1 : ℤ)`

## Current frontier

One sorry remains in `CoxeterInfrastructure.lean` at line 1421: `one_round_or_simpleRoot` (difficulty 9). This is a complex induction on the admissible ordering that threads type-changing quiver instances through reflection functors. The comment notes a universe constraint blocker.

## Overall project progress

- `CoxeterInfrastructure.lean`: 1 sorry (down from 8)
- All infrastructure lemmas proved: Subsingleton Hom preservation, Module.Free/Finite transport, dimension vector bridge
- `one_round_or_simpleRoot` is the last blocker before `indecomposable_reduces_to_simpleRoot` and `indecomposable_bilinearForm_eq_two` (both already proved assuming it)

## Next step

Prove `one_round_or_simpleRoot` — requires induction on admissible ordering with careful universe management. Consider decomposing into sub-lemmas: (1) single-step reflection functor application, (2) full-round iteration, (3) final quiver isomorphism.

## Blockers

`one_round_or_simpleRoot` is difficulty 9 and may need decomposition into sub-issues for tractability.
