## Accomplished

1. **Proved Proposition6_6_6_sink naturality, a≠i b≠i case** — the hardest of the three naturality sub-cases. Key insight: replaced monolithic `equivAt_ne_sink` with explicit `.trans` composition of `reflFunctorMinus_equivAt_ne` and `reflFunctorPlus_equivAt_ne`, enabling direct use of API lemmas (`reflFunctorMinus_mapLinear_ne_ne`, `reflFunctorPlus_mapLinear_ne_ne`, `reversedArrow_ne_ne_twice`).

2. **Set up a≠i, b=i case** with `subst hb` + `simp only [dite_true, dif_neg ha, LinearEquiv.trans_apply]` reducing to a clean goal.

3. **Added `reversedArrow_ne_eq` helper** — converts reversed-quiver arrow a→i (a≠i) back to i⟶a in Q, infrastructure for the ne_eq case.

4. **Reduced sorry count from 3 to 2** in Proposition6_6_6.lean.

## Current frontier

- **File:** `EtingofRepresentationTheory/Chapter6/Proposition6_6_6.lean`
- **Remaining sorries:**
  1. Line ~958: `Proposition6_6_6_sink` naturality, a≠i b=i case — involves `equivAt_eq_sink` (first isomorphism theorem equiv at sink vertex). Blocked by: Lean can't see that `(reflectionFunctorMinus ...).obj i` equals a quotient type due to `Decidable.casesOn` opacity. An API lemma `reflFunctorMinus_mapLinear_ne_eq` was attempted but its statement can't typecheck due to this type mismatch.
  2. Line ~1002: `Proposition6_6_6_source` — full sorry, dual of sink case.

## Overall project progress

- Proposition 6.6.6 part 1 (sink): 2/3 naturality cases proven, equivAt construction complete
- Proposition 6.6.6 part 2 (source): fully sorry'd, needs same infrastructure as sink case
- The `Decidable.casesOn` opacity is a systematic blocker for cases involving vertex `i` (where the representation object is defined via a case split on `DecidableEq`)

## Next step

- **ne_eq case:** Needs a `reflFunctorMinus_mapLinear_ne_eq` lemma whose statement must match the opaque `(reflectionFunctorMinus ...).obj i` type. Two possible approaches:
  1. Use `show`/`change` to cast the goal type before applying API lemmas
  2. Prove a statement using `@` with fully unfolded types, matching the Decidable.casesOn output
- **Source case:** Dual of sink — would need `equivAt_eq_source` (kernel of source map ≅ V_i when ψ injective), plus same naturality infrastructure

## Blockers

- `Decidable.casesOn` opacity prevents clean API lemma statements for the ne_eq and eq_ne cases (vertex `i` involved). The `rw` and `simp` tactics cannot rewrite through dependent `Decidable.rec` terms ("motive is not type correct").
