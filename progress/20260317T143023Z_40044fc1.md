## Accomplished

Formalized §5.19 statements for issue #941:

1. **Proposition 5.19.1** — Replaced `(sorry : Prop)` with: the linear span of `{g⊗ⁿ | g ∈ GL(V)}` equals `(diagonalActionImage k V n).toSubmodule`. Imports and reuses definitions from Theorem 5.18.4.

2. **Corollary 5.19.2** — Replaced `(sorry : Prop)` with: existential Schur-Weyl decomposition indexed by `Nat.Partition n`, asserting `TensorPower k V n ≃ₗ[k] ⊕_p (S p ⊗ L p)`.

3. **Example 5.19.3** — Replaced two `(sorry : Prop)` placeholders with:
   - Symmetric: `symInvariants k V n ≃ₗ[k] SymmetricPower k (Fin n) V` (Sₙ-invariant tensors ≅ symmetric power)
   - Exterior: `symAntisymmetric k V n ≃ₗ[k] ⋀[k]^n V` (antisymmetric tensors ≅ exterior power)
   - Defined `symInvariants` and `symAntisymmetric` as submodules of `TensorPower k V n`

All 3 files build successfully. Updated `progress/items.json`: 3 items → `statement_formalized`.

## Current frontier

§5.19 statements complete. All 3 items advanced from `scaffolded` to `statement_formalized`.

## Overall project progress

Stage 3.2 proof filling in progress. ~181/583 items sorry_free (31.0%). Ch5 advanced from 21% with 3 more items having proper statements (still need proofs).

Remaining unclaimed issues: #939 (review PR #926 conflicts), #940 (summarize wave 10).

## Next step

1. Merge this PR when CI passes
2. Pick up #939 (resolve PR #926 merge conflicts) or #940 (wave 10 summary)
3. Continue advancing Ch5 statement formalization (21% → higher)

## Blockers

None.
