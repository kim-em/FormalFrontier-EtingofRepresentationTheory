## Accomplished

Completed Stage 2.3: Assemble leanblueprint DAG from items and dependencies (issue #462).

### Deliverables

1. **`scripts/generate_blueprint.py`**: Reads `progress/items.json`, `dependencies/internal.json`, and `dependencies/external.json`, then generates `blueprint/src/content.tex` with all 583 items as leanblueprint LaTeX environments. Features:
   - Maps item types to LaTeX environments (theorem, definition, lemma, corollary, proposition, example, remark)
   - Non-theorem types (discussion, introduction, notation, preface, exercise, bibliography) mapped to remark
   - Adds `\label{Chapter:ItemName}` and `\uses{dep1, dep2, ...}` annotations
   - Converts markdown blob content to LaTeX (bold, italic, headers, lists)
   - Escapes special LaTeX characters while preserving embedded math

2. **Blueprint web build verified**: Successfully built HTML dependency graph with `plastex`. All 583 items present across 11 chapter sections with dependency edges.

3. **`blueprint/src/web.tex`**: Inlined theorem-like environment definitions (previously via `\input{macros/common}`) to work around plasTeX's subdirectory file resolution limitation.

4. **`.gitignore`**: Added `blueprint/web/` and `blueprint/print/` for generated output.

### Notes
- plasTeX 3.1 + Python 3.14 has a hashability bug with dynamically-created theorem classes. Worked around by adding `__hash__` to the amsthm newtheorem data dict (local env patch, not committed). CI should use Python <= 3.13 for blueprint builds.
- Repo restructured during session (lean/ contents moved to root, lean/blueprint/ to blueprint/). Script updated accordingly.

## Current frontier

Stage 2.3 complete. The conservative dependency DAG is now viewable as an HTML blueprint.

## Overall project progress

- Phase 1 (Source Preparation): Complete — 583 items across 11 chapters
- Phase 2 (Dependency Mapping): Complete
  - Stage 2.1 (Internal deps): Done (conservative, #458)
  - Stage 2.2 (External deps): Done (#461)
  - Stage 2.3 (Blueprint assembly): Done (#462)
- Phase 3 (Formalization): Not started

## Next step

Begin Phase 3 formalization scaffolding — create Lean files for items, set up module structure, and start formalizing definitions and theorem statements.

## Blockers

None.
