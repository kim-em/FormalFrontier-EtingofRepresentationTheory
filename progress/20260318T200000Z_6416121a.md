## Accomplished

1. Claimed issue #951 (formalize §5.22 statements: Theorem 5.22.1, Proposition 5.22.2)
2. Formalized Theorem 5.22.1 (Weyl character formula for GL(V)):
   - Defined `SchurModule k N lam` as an opaque `FDRep k (GL_N(k))` placeholder for the irreducible polynomial representation L_λ
   - Defined `formalCharacter` as an opaque function mapping `FDRep` to `MvPolynomial (Fin N) ℚ`
   - Stated: `formalCharacter k N (SchurModule k N lam) = schurPoly N lam` (character = Schur polynomial)
   - Imports `Proposition5_21_1` to reuse `schurPoly` and related infrastructure
3. Formalized Proposition 5.22.2 (determinant twist):
   - Defined `detRep k N` as an opaque `FDRep` for the determinant representation (∧^N V)
   - Stated: `SchurModule k N (fun i => lam i + 1) ≅ SchurModule k N lam ⊗ detRep k N`
   - Uses `MonoidalCategory` tensor product on `FDRep`
4. Both files build successfully (`lake build` — only `sorry` warnings)
5. Updated `progress/items.json`: both items → `statement_formalized`

## Current frontier

Issue #951 complete. PR ready to create.

## Overall project progress

Stage 3.2 proof filling in progress. ~182/583 items sorry_free (31.2%). Chapter 5 at ~21% sorry_free.
Two more §5.22 items now have formalized statements (Theorem 5.22.1 + Proposition 5.22.2).

## Next step

1. Create PR for #951 and enable auto-merge
2. Tackle remaining unclaimed issues: #952 (§5.26-5.27 statements), #945 (Aristotle poll)
3. Continue Chapter 5 statement formalization — still the lowest completion chapter

## Blockers

None.
