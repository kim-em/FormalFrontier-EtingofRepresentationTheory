## Accomplished

- Claimed #2104 (restructure straightening WF induction for Garnir expansion)
- **Deep analysis revealed fundamental flaw**: the Garnir identity at the group algebra level is a TAUTOLOGY via Lemma 5.13.1 (sandwich property). Each term `of(σ) * a_λ * of(w) * b_λ = ℓ(of(w)) • of(σ) * c_λ`, so the expansion collapses to `of(σ) * c_λ = of(σ) * c_λ`. No metric change can fix this — the approach is fundamentally wrong.
- Confirmed the issue's counterexample: partition (2,2), coset rep swap(1,2) preserves columnInvCount'
- Removed false `garnir_columnInvCount_decrease` and dependent theorems
- Replaced with direct sorry + detailed documentation of correct approaches
- Posted comprehensive analysis on issue #2104
- Created PR #2172 with auto-merge enabled

## Current frontier

- `perm_mul_youngSymmetrizer_mem_span_polytabloids` (PolytabloidBasis.lean:1384) still sorry'd
- The straightening lemma requires a fundamentally different approach than Garnir-at-group-algebra-level
- Two viable approaches documented:
  1. **Tabloid-level straightening**: prove in M^λ using tabloid dominance order, transfer via V_λ irreducibility
  2. **Dimension argument**: use n! = Σ dim(V_λ)² + Σ |SYT(λ)|² = n! + dim V_λ ≥ |SYT(λ)|

## Overall project progress

- **4 sorries** in PolytabloidBasis.lean (unchanged count, but one sorry is now correctly documented as requiring a different approach)
  - `polytabloid_mem_spechtModule` (line 193)
  - `polytabloid_linearIndependent` (line 537) — proved at tabloid level
  - `column_standard_in_span'` (line 943)
  - `perm_mul_youngSymmetrizer_mem_span_polytabloids` (line 1384) — **approach clarified**
- **1 sorry** in TabloidModule.lean (`polytabloid_syt_dominance`, line 889)
- ~9 total sorries across the project
- V_λ irreducibility proved (Theorem5_12_2_irreducible) — key for transfer approach
- polytabloidTab_linearIndependent proved in TabloidModule.lean

## Next step

1. **New issue needed**: "Prove tabloid-level straightening in M^λ" — define the tabloid projection map φ: ℂ[Sn] → M^λ, prove the Garnir relation at the tabloid level produces tabloids that are strictly lower in the dominance order, and use WF induction on dominance to prove straightening. Then transfer to V_λ via irreducibility.
2. Alternatively: pursue the dimension argument if the RSK identity Σ |SYT(λ)|² = n! is tractable.
3. The other unclaimed issue #2083 (compatible_chain_basis_decomp) is independent and available.

## Blockers

- The straightening lemma at the group algebra level CANNOT be proved via Garnir expansion (fundamental obstruction from Lemma 5.13.1). A tabloid-module or dimension-theoretic approach is required.
- The tabloid projection map φ: ℂ[Sn] → M^λ and its properties (injectivity on V_λ, image characterization) need to be formalized.
