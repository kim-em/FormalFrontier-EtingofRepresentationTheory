## Accomplished

- Claimed issue #2261 (straightening theorem: `generalizedPolytabloidTab_mem_span_polytabloidTab`)
- Extensive mathematical analysis of proof approaches (Garnir at group algebra level, tabloid level, column equivariance, dimension counting, sign-eigenspace arguments)
- Identified that the group-algebra Garnir approach is a tautology (confirmed by existing code comments in PolytabloidBasis.lean:836-856)
- Identified core difficulty: Garnir permutations in S_G do NOT normalize Q_λ, so tabloid-level Garnir expansion produces "twisted polytabloids" (antisymmetrized with conjugated column groups) rather than standard generalized polytabloids
- Decomposed the single sorry into 2 focused sub-lemmas:
  1. `exists_column_standard_mul`: column-sorting reduction (difficulty 4)
  2. `polytabloidTab_column_standard_in_span`: Garnir straightening for column-standard case (difficulty 8)
- Proved the main theorem `generalizedPolytabloidTab_mem_span_polytabloidTab` modulo these two helpers
- Made 4 definitions/theorems public in PolytabloidBasis.lean (`isColumnStandard'`, `column_standard_coset_has_syt'`, `columnInvCount'`, `exists_column_inversion`)
- Created sub-issues #2265 (column-sorting) and #2266 (Garnir straightening, depends on #2265)
- Build passes with 2 sorry warnings (the two helper lemmas)

## Current frontier

The straightening theorem proof structure is complete. Two sorry'd helpers remain:
- `exists_column_standard_mul` (line 54 of SpechtModuleBasis.lean) — construct column-sorting permutation in Q_λ
- `polytabloidTab_column_standard_in_span` (line 71) — core Garnir argument for column-standard fillings

## Overall project progress

- `finrank_spechtModule_eq_card_syt'` (dim V_λ = |SYT(λ)|) is proved modulo the straightening theorem
- The straightening theorem is proved modulo 2 focused sorries (reduced from 1 opaque sorry)
- The ≥ direction (`finrank_spechtModule_ge_card_syt`) is fully proved
- The ≤ direction chain: `generalizedPolytabloidTab_mem_span_polytabloidTab` → `tabloidProjection_spechtModule_le_span` → `finrank_spechtModule_le_card_syt` is fully connected

## Next step

1. **Easy win**: Prove `exists_column_standard_mul` (#2265) — difficulty 4, ~50-100 lines. Construct column-sorting permutation for each column using Finset.sort, show it's in Q_λ.
2. **Hard core**: Prove `polytabloidTab_column_standard_in_span` (#2266) — difficulty 8, ~150-250 lines. Requires:
   - Well-founded relation on tabloids (Tabloid is Fintype, so tabloidStrictDominates is well-founded)
   - Garnir set construction at row descents
   - Dominance decrease argument for Garnir expansion
   - Handling twisted polytabloids (key difficulty)

## Blockers

- The Garnir straightening (#2266) is genuinely difficult (difficulty 8). The core obstacle is that Garnir permutations in S_G don't normalize Q_λ, so the tabloid-level Garnir expansion produces "twisted polytabloids" rather than standard generalized polytabloids. The classical proof (James Ch. 7-8) handles this by working directly with tabloid coefficients and dominance ordering, but formalizing this requires careful bookkeeping.
