## Accomplished

- **Closed ALL sorry's in Problem6_9_1.lean** (was 4, now 0)
- Proved `IsCompl (V'₀.prod W'₀) C` for both symmetric cases of `exists_invariant_product_complement`
- Key proof structure (applied identically in both cases):
  - **Step A (hφ_aeval)**: `Module.AEval.of_aeval_smul` gives `φ((aeval X f) p) = f • φ(p)`
  - **Step B (haeval_mem)**: Polynomial induction shows `(aeval X f) p ∈ V'₀.prod W'₀`
  - **Step C (hφ_surj)**: Surjectivity via `LinearEquiv.apply_symm_apply` + `DirectSum.component.lof_self`
  - **Step D (hfr_N)**: `finrank(N j₀) = k j₀` via `Ideal.submodule_span_eq` + `finrank_quotient_span_eq_natDegree`
  - **Step E (hfr_le)**: `finrank(V'₀.prod W'₀) ≤ k j₀` via `Submodule.map_span` + `finrank_span_finset_le_card`
  - **Step F (hcodisjoint)**: Unit section: lift `φ q * ↑u⁻¹` to polynomial, evaluate at X
  - **Step G (hdisjoint)**: Dimension argument from `finrank_sup_add_finrank_inf_eq` + rank-nullity
- Fixed multiple API issues:
  - `Submodule.span_induction` now uses dependent predicate `(x : M) → x ∈ span R s → Prop`; avoided by using `Submodule.map_span` + `Submodule.span_le` instead
  - `Ideal.submodule_span_eq` (rfl) converts between Submodule.span and Ideal.span
  - `Units.inv_mul` (not `Units.val_inv_mul`) for `↑u⁻¹ * ↑u = 1`
  - `swapOp_pow_even_fst/snd` direction: gives `X^{2m} p = ...`, not `... = X^{2m} p`
  - `DecidableEq (V × W)` needed for `Finset.image`; solved with `classical`
  - `change` needed to unify `LinearMap.ker φ` and `C` for omega

## Current frontier

Problem6_9_1.lean: **0 sorry's**. All proofs complete.

## Overall project progress

- Problem6_9_1.lean fully proven (0 sorry's, was 1 monolithic sorry → 4 targeted → 0)
- `compatible_product_decomp` and `exists_invariant_product_complement` both fully proven
- `off_diagonal_nilpotent_product_decomp` (main Problem 6.9.1 result) fully proven
- Build succeeds with 0 errors, 0 sorry's
- maxHeartbeats increased to 6400000 for `exists_invariant_product_complement` (PID + AEval' manipulations are expensive)

## Next step

1. Update PR #2215 (squash or update description to reflect 0 sorry's)
2. Enable auto-merge
3. Move to next formalization item

## Blockers

None. All sorry's eliminated.
