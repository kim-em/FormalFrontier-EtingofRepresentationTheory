## Accomplished

- Reduced sorrys in `CoxeterInfrastructure.lean` from 2 to 1
- Proved `iteratedSimpleReflection_replicate_eq_iterate`: connects iterated Coxeter element c^[M] with `iteratedSimpleReflection` on M concatenated copies of the admissible ordering
- Rewrote `indecomposable_reduces_to_simpleRoot` with strengthened induction hypothesis that threads the representation through rounds:
  - Suffices now carries: nonneg dim vec + indecomposable rep ρ_M on Q with dim vec = c^[M](d)
  - Base case (M=0): use original ρ
  - Inductive step: apply `one_round_or_simpleRoot` to ρ_M, producing either simple root or next rep
  - Left branch uses `iteratedSimpleReflection_replicate_eq_iterate` to concatenate vertex lists
- Round-trip lemma `iteratedReversedAtVertices_perm_eq` and supporting hom lemmas (from prior session) remain proved

## Current frontier

Single remaining sorry: `one_round_or_simpleRoot` (line ~1212). This is the core type-changing iteration lemma. Its proof requires:
1. Induction on the vertex list σ, generalizing over Q
2. At each step: apply Prop 6.6.5 (simple or surjective at sink)
3. If surjective: apply F⁺ to get rep on reversed quiver, then IH
4. After full round: use round-trip lemma to cast back to Q

Missing infrastructure for this proof:
- Bridge lemma: `simpleReflectionDimVector` (ArrowsInto sum) = `simpleReflection` (Cartan matrix) for Dynkin orientations at a sink
- `Module.Free`/`Module.Finite` instances for `reflectionFunctorPlus` output
- `Fintype (ArrowsInto Q_k σ[k])` at each step

## Overall project progress

- `CoxeterInfrastructure.lean`: 1 sorry (was 2)
- `indecomposable_reduces_to_simpleRoot`: proof complete (depends on `one_round_or_simpleRoot`)
- `indecomposable_bilinearForm_eq_two`: proof complete (depends on `indecomposable_reduces_to_simpleRoot`)
- The downstream chain `indecomposable_titsForm_le_two` → `Corollary6_8_3` works once `one_round_or_simpleRoot` is proved

## Next step

Prove `one_round_or_simpleRoot` by induction on the vertex list with Q universally quantified. The single-step requires combining Props 6.6.5, 6.6.7, 6.6.8 with Free/Finite/Fintype instance threading. Consider creating sub-issues for missing infrastructure pieces.

## Blockers

The bridge lemma connecting `simpleReflectionDimVector` to `simpleReflection` is the key missing piece. Without it, the dimension vector tracking through reflection functors cannot be formalized.
