## Accomplished

Continued work on issue #1344: prove 3 Frobenius formula helpers (Theorem5_15_1).

1. **Proved `coeff_symmetric_eq_coeff_partition` fully** (deliverable 2/3): Filled the
   remaining sorry in `hparts_w` by adding helper lemma `card_filter_getD_eq_count`.
   - For `c ≠ 0` and `l.length ≤ n`, the number of `Fin n` indices where `getD` returns
     `c` equals `List.count c l`.
   - Proof uses `Fin.univ_succ` to decompose `Finset.univ.val` for `Fin (m+1)` into
     `{0} + map Fin.succ (univ.val for Fin m)`, then `Multiset.count_cons` for the
     inductive step, with `Multiset.count_replicate` for the base case.
   - PR #1361 created and auto-merge enabled.

2. **`sum_shifted_sub_permExponent`** (deliverable 1/3): Already proved in prior session.

3. **`alternating_kostka_eq_delta`** (deliverable 3/3): Still sorry'd. This is the deep
   algebraic identity requiring Kostka matrix upper-triangularity and cancellation.

## Current frontier

- `alternating_kostka_eq_delta` at line ~1215 is fully sorry'd — the only remaining
  deliverable for issue #1344.
- Pre-existing sorry at line 768 (unrelated to this issue).

## Overall project progress

- ~193/583 items sorry-free (~33%)
- 2 of 3 target deliverables fully proved
- 3rd deliverable (alternating Kostka) requires substantial Kostka matrix infrastructure

## Next step

- Prove `alternating_kostka_eq_delta` — requires deep Kostka matrix theory
  (`spechtMultiplicity_vanishing`, `spechtMultiplicity_diagonal`) plus dominance order.
  May need to be split into a separate issue given its complexity.
- Alternatively, move on to other unclaimed issues if this is blocked.

## Blockers

- `alternating_kostka_eq_delta` requires Kostka matrix infrastructure that may not be
  fully proved. The proof sketch involves cancellation of off-diagonal terms via
  dominance order, which is a deep combinatorial argument.
