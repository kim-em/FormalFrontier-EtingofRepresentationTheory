## Accomplished

- Added corner projection helper lemmas to BasicAlgebraExistence.lean:
  - `eCorner_smul_of_idem`: e•(e•m) = e•m
  - `toECorner`: projection m ↦ e•m into the e-corner
  - `toECorner_add`: projection is additive
  - `toECorner_of_mem`: projection is identity on eM
  - `toECorner_cornerRing_smul`: projection commutes with eAe-action
- Documented detailed proof strategy for all 3 sorrys with references to available Mathlib infrastructure
- Added import for `Mathlib.RingTheory.Idempotents` (needed for idempotent lifting)
- Verified available Mathlib infrastructure:
  - `IsArtinianRing.of_finite` (finite-dim → Artinian)
  - `IsSemiprimaryRing` instance (Artinian → semiprimary)
  - `IsSemisimpleRing.exists_algEquiv_pi_matrix_of_isAlgClosed` (Wedderburn-Artin over alg closed field)
  - `CompleteOrthogonalIdempotents.lift_of_isNilpotent_ker` (idempotent lifting)

## Current frontier

BasicAlgebraExistence.lean has 3 sorrys (unchanged from before):
1. `exists_full_idempotent_basic_corner` (line 89): Full idempotent with basic corner — needs Wedderburn-Artin decomposition of A/rad(A), extraction of diagonal idempotents, lifting to A, and proof of fullness + basicness
2. `cornerFunctor_full` (line 282): Fullness of corner functor — needs evaluation isomorphism `Ae ⊗_{eAe} eM ≅ M`
3. `cornerFunctor_essSurj` (line 292): Essential surjectivity — needs `e(A ⊗_{eAe} N) ≅ N`

## Overall project progress

- Stage 3.2 proofs in progress
- BasicAlgebraExistence.lean: proof infrastructure improved, 3 sorrys remain
- The file builds clean with warnings only (no errors)

## Next step

1. **Highest leverage**: Prove `exists_full_idempotent_basic_corner` — the key sub-steps are:
   - Extract diagonal idempotents from the Wedderburn-Artin decomposition (concrete construction in ∏ Mₙᵢ(k))
   - Transport them to A/rad(A) via the algebra equivalence
   - Lift to A using `CompleteOrthogonalIdempotents.lift_of_isNilpotent_ker`
   - Show the sum is full (AeA = A) and the corner ring is basic
2. **Alternative**: Prove the evaluation isomorphism `Ae ⊗_{eAe} eM ≅ M` as a unified lemma, from which both fullness and essential surjectivity follow

## Blockers

- All 3 sorrys are deep algebraic results requiring substantial formalization work
- `cornerFunctor_full` and `cornerFunctor_essSurj` both reduce to the evaluation isomorphism, which requires working with balanced tensor products (not directly available in Mathlib for non-commutative rings)
- The Morita equivalence via full idempotents is listed as a TODO in Mathlib's Morita.Basic file
