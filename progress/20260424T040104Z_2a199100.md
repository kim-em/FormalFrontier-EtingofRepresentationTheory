## Accomplished

Claimed feature issue #2458 (Schur-Weyl #3: identify `Theorem5_18_4`
`L_i` summands with `SchurModule`) and, after reading the scoping
doc (`progress/schur-weyl-scoping.md`) plus the current state of
`Theorem5_18_4_bimodule_decomposition`,
`schurPoly_linearIndependent`, and
`iso_of_formalCharacter_eq_schurPoly`, decomposed it into three
session-sized sub-issues. No code changes — decomposition only.

The parent's own scope warning
("this may be large (1+ sessions). If you hit the 3-attempt threshold
on any sub-step, decompose further … Likely sub-splits: (i) character
match of one L_i with one SchurModule; (ii) assembly of the iso")
confirmed the decomposition path. The decomposition:

- **#2491** (`part A: endow bimodule L_i with FDRep GL_N structure`) —
  pure plumbing: lift `Module (diagonalActionImage) (L i)` from
  `Theorem5_18_4_bimodule_decomposition` to a full
  `FDRep k (GL_N k)` via the `GL_N → End V → diagonalActionImage`
  factor. Prerequisite for any character computation on `L i`.
- **#2492** (`part B: formalCharacter additivity for bimodule
  decomposition`) — the algebraic identity
  `character(V^⊗n) = Σ (dim S_i) · character(L_i)` as an
  FDRep-level statement, combining `formalCharacter` additivity
  with tensor-by-trivial multiplicativity. Depends on #2491.
- **#2493** (`part C: schurWeyl_gl_decomposition — final assembly`) —
  uses A + B + `schurPoly_linearIndependent` +
  `formalCharacter_schurModule_eq_schurPoly` +
  Schur's lemma to close #2458's target. Depends on #2491 and #2492.
  Comment notes the circular-dependency tension with Schur-Weyl #6
  (`iso_of_formalCharacter_eq_schurPoly`): the specialized subrep
  case should be closable via `finrank_hom_simple_simple` without
  invoking the full #6.

Parent #2458 skipped with the `Decomposed into #2491, #2492, #2493`
breadcrumb; planner-triage will close it once the three subs land.

## Current frontier

- #2491, #2492, #2493 unclaimed.
- #2458 in `replan` state awaiting planner.
- No Lean code changed on branch; branch is at `main` commit
  `e2129eef`.

## Overall project progress

- Stage 3 (formalization) ongoing.
- Schur-Weyl sub-plan from `progress/schur-weyl-scoping.md`:
  - #4 (`schurPoly_linearIndependent`): **done** — in
    `FormalCharacterIso.lean:94`.
  - #3 (this): decomposed into #2491, #2492, #2493.
  - #1, #2, #5, #6: still pending per scoping doc.
- Sorry count on branch: **unchanged** (no code landed).

## Next step

A planner can either pick up #2491 directly (it's unblocked and fully
self-contained plumbing) or continue unblocking other Schur-Weyl
sub-issues. The shortest path to closing `iso_of_formalCharacter_eq_schurPoly`
(and therefore `Proposition5_22_2`) is #2491 → #2492 → #2493,
then tie into the existing plan for #6.

## Blockers

None new. #2491 is unblocked. #2492 and #2493 are `blocked` on their
respective predecessors and will auto-unblock via `check-blocked` as
the chain lands.
