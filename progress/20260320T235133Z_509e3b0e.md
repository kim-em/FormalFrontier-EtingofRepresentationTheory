## Accomplished

Proved 2 of 3 sorry'd parts in Corollary9_7_3.lean (unique basic algebra):

1. **Corollary_9_7_3_ii** (dimension bound): `dim B ≤ dim A` proved via
   MoritaStructural (sorry'd) giving `B ≅ eAe`, then `CornerRing.finrank_le`.

2. **Corollary_9_7_3_i_unique** (uniqueness): If B₁, B₂ are basic and Morita
   equivalent to A, then B₁ ≅ₐ B₂. Proof: MoritaStructural gives B₂ ≅ e(B₁)e
   and B₁ ≅ f(B₂)f. Dimension argument forces cornerSubmodule = ⊤, hence e = 1,
   so B₂ ≅ B₁.

Helper infrastructure added:
- `idempotent_eq_one_of_cornerSubmodule_eq_top`: full corner ⟹ e = 1
- `cornerRingAlgEquivOfUnit`: CornerRing(A, 1) ≃ₐ[k] A

## Current frontier

- Corollary_9_7_3_i (existence of basic algebra) remains sorry'd
- This needs constructing B = eAe from primitive idempotent extraction
- Wedderburn-Artin (Theorem 3.5.4) is proved but requires `IsAlgClosed k`;
  the corollary is stated for arbitrary fields

## Overall project progress

- ~207/583 items sorry_free in items.json (~35.5%)
- Corollary9.7.3: 2/3 parts proved (was 0/3), status moved to "partial"
- Issue #1468: 2/3 deliverables complete

## Next step

- Existence proof (Corollary_9_7_3_i) requires either:
  - Extending Wedderburn-Artin to general fields, or
  - Adding `IsAlgClosed k` hypothesis and connecting to existing Theorem 3.5.4
- Alternative: prove MoritaStructural (currently sorry'd in MoritaStructural.lean)
  which would unlock full proofs

## Blockers

- Existence proof blocked on primitive idempotent extraction infrastructure
  (need to go from Wedderburn-Artin decomposition to concrete idempotents)
- Wedderburn-Artin theorem requires `IsAlgClosed k` but corollary is for general fields
