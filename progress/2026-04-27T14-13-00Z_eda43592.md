## Accomplished

Review session on issue #2554 — audited un-reviewed Schur-Weyl plumbing
additions in `Chapter5/PolynomialRepEmbedding.lean`:

- **PR #2528** (Schur-Weyl #2b injection part): `polyTensorRow`,
  `polyTensorBundle`, `polynomialRep_embeds_in_tensorPower_inj`.
- **PR #2551** (`hP_mul` derivation + primed wrapper):
  `MvPolynomial.eq_of_eval_eq_on_gl`, `eval_polyRightTransl`,
  `hP_mul_of_hP`, `polynomialRep_embeds_in_tensorPower'`.

### Findings — all PASS

- Q1 (injection): `polyTensorBundle` correctly bundles one polynomial
  per basis row, evaluated at every pure-tensor `(V*)^⊗n`-basis index.
  Injectivity bottoms out at `homogeneousPolyToTensor_injective`.
  `[CharZero k]` is tight against the bridge's `(n!)⁻¹` factor.
- Q2 (`eq_of_eval_eq_on_gl`): proof uses
  `MvPolynomial.funext` on `(p-q) * det(X)` cleanly. Hypothesis
  `[Field k] [Infinite k]` is tight. No Mathlib duplicate.
  Mathlib-PR-ready.
- Q3 (`hP_mul_of_hP` chain & primed wrapper): `[CharZero k]` on
  `eval_polyRightTransl` and `hP_mul_of_hP` is propagated from
  `polyRightTransl_X` upstream via section auto-binding (verified
  empirically). Primed wrapper hides only `hP_mul`; conclusion is
  forwarded verbatim from the unprimed form.

### Follow-ups filed

- **#2564** (mathlib-upstream tracker): contribute
  `MvPolynomial.eq_of_eval_eq_on_gl` upstream to
  `Mathlib/LinearAlgebra/Matrix/MvPolynomial.lean`.
- **#2565** (refactor): add `omit [CharZero k] in` on
  `polyRightTransl_X` (`PolynomialTensorBridge.lean:403`) to relax
  `eval_polyRightTransl` and `hP_mul_of_hP` to `[Field k] [Infinite k]`.

### Minor opportunistic cleanups (non-blocking, not filed)

- Duplicated "set `g = 1`" coda between
  `polynomialRep_embeds_in_tensorPower_inj` and
  `polynomialRep_embeds_in_tensorPower`.
- Verbose `show ... ↔ ... from ⟨..., ...⟩` block in
  `polyTensorRow_eq_zero_iff` (lines 142-154).

### Documents produced

- `progress/reviews/2026-04-27-schur-weyl-injection-and-hPmul.md` —
  full audit doc (Q1/Q2/Q3 + Findings + summary).
- Consolidated review comment posted on issue #2554.

### Build verification

`lake build EtingofRepresentationTheory.Chapter5.PolynomialRepEmbedding`
passes (8033/8033, no errors; only pre-existing
`linter.style.show` warnings in the bridge file).

## Current frontier

Audit complete. Issue #2554 ready to close via PR.

## Overall project progress

- Schur-Weyl chain: injection (#2528) + equivariance (#2538/#2539) +
  `hP_mul` derivation (#2551) all now audited and clean. The chain's
  "polynomial rep embeds equivariantly into tensor power" deliverable
  (`polynomialRep_embeds_in_tensorPower'`) stands up to scrutiny.
- Open Schur-Weyl chain items (per progress/sorry-landscape.md):
  - **#2540** (Schur-Weyl #3 Part B, claimed): line-774 sorry in
    `glTensorRep_equivariant_schurWeyl_decomposition`.
  - **#2493** (Schur-Weyl L_i part C, blocked on #2540).
  - **#2482** (Schur-Weyl #5, blocked on #2493): consumes
    `polynomialRep_embeds_in_tensorPower'` directly. This audit
    confirms the consumption surface is sound.
  - **#2483** (Schur-Weyl #6, blocked on #2482).
- File size: `PolynomialRepEmbedding.lean` now 761 lines — over the
  500-line soft threshold. No split recommended yet; revisit if next
  wave adds another large chunk.

## Next step

1. PR this audit (review doc + progress entry) closing #2554. Auto-merge.
2. Sibling audit #2555 (FormalCharacterIso character-additivity, PRs
   #2516 + #2534) is still unclaimed — separate session can pick it up
   in parallel.
3. Schur-Weyl chain remains gated on #2540's bimodule decomposition.

## Blockers

None.
