## Accomplished

- **Diagnosed missing hypothesis**: `exists_orthogonal_idempotents_for_simples` requires `IsAlgClosed k` — the rank-1 condition `finrank k (e_i · M_j) = δ_{ij}` needs End_A(M_i) = k (Schur + algebraic closure). Over non-algebraically-closed fields, End_A(M_i) can be a nontrivial division algebra D_i, and the minimal idempotent action on M_i has k-dimension [D_i : k] > 1. Counterexample: A = H (quaternions) over k = R.
- **Added `IsAlgClosed k`** to `exists_orthogonal_idempotents_for_simples`, `Theorem_9_2_1_i`, `Theorem_9_2_1_ii`, `Theorem_9_2_1_iii`
- **Added `IsAlgClosed.Basic` import**
- **Verified `leftIdeal_indecomposable_of_hom_delta`** is already fully proved (deliverable 2 from the issue was completed by a prior PR)
- **Proved intermediate steps**: semiprimary ring structure, Jacobson radical annihilation of simple modules
- **Verified no downstream breakage**: `Proposition9_2_3` doesn't use these theorems yet

## Current frontier

`exists_orthogonal_idempotents_for_simples` still has `sorry` for the core Wedderburn-Artin construction (steps 3-6 in the docstring). The required construction is:
1. Decompose A/Rad(A) ≅ ∏ End_k(M_i) via Wedderburn-Artin (Mathlib has `IsSemisimpleRing.exists_ringEquiv_pi_matrix_divisionRing`)
2. Show D_i = k using IsAlgClosed (needs `IsAlgClosed.algebraMap_bijective` or similar for finite-dimensional division algebras)
3. Construct E₁₁ rank-1 idempotents in each matrix block
4. Transport through the ring equivalence
5. Lift from A/Rad(A) to A using `OrthogonalIdempotents.lift_of_isNilpotent_ker`
6. Show rank property transfers (Rad annihilates simples, so actions agree)

Key Mathlib APIs available but not yet connected:
- `CompleteOrthogonalIdempotents.single`: idempotents in product rings
- `OrthogonalIdempotents.lift_of_isNilpotent_ker`: lifting through nilpotent kernels
- `IsSemisimpleModule.jacobson_le_annihilator`: Jacobson radical kills simples

## Overall project progress

- Sorry count in Theorem9_2_1.lean: 3 (unchanged — 1 in helper, 2 in parts ii/iii which are out of scope)
- `leftIdeal_indecomposable_of_hom_delta`: fully proved (already done by prior PR)
- `leftIdeal_projective`, `leftIdeal_finite`, `finrank_hom_leftIdeal_eq`: all fully proved
- `Theorem_9_2_1_i` assembly: fully proved (calls sorry'd helper)

## Next step

Prove `exists_orthogonal_idempotents_for_simples` by:
1. Connecting simple A-modules to Wedderburn-Artin blocks of A/Rad(A) (needs A/Rad(A)-module structure on simple A-modules)
2. Showing IsAlgClosed → D_i = k for finite-dimensional division algebras (may need custom lemma)
3. Constructing rank-1 matrix idempotents and transporting through equivalences

This is genuinely difficult formalization requiring ~200+ lines of Lean connecting several Mathlib APIs. A planner should consider breaking it into sub-issues.

## Blockers

- The core Wedderburn-Artin construction connecting simple modules to matrix blocks requires significant infrastructure not yet assembled in either Mathlib or this project
- Mathlib lacks a direct "idempotent from Wedderburn block acts as rank-1 on corresponding simple module" API
