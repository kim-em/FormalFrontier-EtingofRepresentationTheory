## Accomplished

Partial work on #2379 (etilde6v2Rep_isIndecomposable, Ẽ₆ indecomposability):

- Set up the structural framework for `etilde6v2Rep_isIndecomposable`, following the etilde7 pattern (PR #2367)
- Added arrow helpers for all 6 arrows in the quiver: hom21, hom10, hom03, hom43, hom65, hom50
- Added `compl_eq_of_le` helper (complement equality from mutual containment)
- Proved injectivity of the key representation maps:
  - `inj_starEmbed1`: starEmbed1 is injective
  - `inj_embed2to3_AB`: (a,b) ↦ (a,b,0) is injective
  - `inj_embed2to3_CA`: (a,b) ↦ (b,0,a) is injective (both block-A and block-C reads)
  - `inj_starEmbedNilp`: (x, Nx) recovery via first block
- Set up `prop_inj` machinery for propagating W = ⊥ through injective arrows
- Wired up the propagation chain: W(0) = ⊥ propagates to W(1), W(5), W(6), plus W(2), W(4) via h24 hypothesis
- Replaced the single sorry at line 2354 with 5 clearly-documented sub-sorries:
  1. `leaf24_containment` (W(2) ⊆ W(4) as subspaces)
  2. `hN₁`, `hN₂` (N-invariance of W₁(2), W₂(2))
  3. `hbot0` (W(0) = ⊥ from W(2) = ⊥)
  4. `hbot3` (W(3) = ⊥ from W(0) = ⊥, W(4) = ⊥ via Γ surjectivity)
- File builds clean (8038 jobs successful).

## Current frontier

The structural framework for `etilde6v2Rep_isIndecomposable` is in place. Only 5 mathematical sub-lemmas remain to be proven, in direct analogy to how etilde7 was handled (PR #2367 set up framework; #2394 tracks the remaining sub-sorries).

The hardest sub-sorry is `leaf24_containment` — showing W(2) ⊆ W(4). The usual "core" argument at vertex 3 doesn't directly apply because vertex 3 has only one direct leaf (4) contributing via starEmbed1; the second block of V(3) is fed only by Γ from the center, not by a direct leaf edge. A novel argument using the m ≥ 1 nilpotent coupling from leaf 6 is needed.

## Overall project progress

- Stage 3.1 (formalization) is the active frontier
- 3 remaining indecomposability proofs (Ẽ₆, Ẽ₇, T(1,2,5), D̃ₙ) each in framework-built state
- Sorries added: 5 sub-sorries in etilde6v2Rep_isIndecomposable (replacing 1 sorry for the whole theorem body)
- The framework adds value by letting future agents focus on mathematical sub-lemmas one at a time rather than the full proof

## Next step

Follow-up planner should create sub-issues for each of the 5 sub-sorries:
1. Prove `leaf24_containment` (W(2) ⊆ W(4)) — the hard leaf containment lemma
2. Prove `hN₁` and `hN₂` (N-invariance of W(2)/W'(2))
3. Prove `hbot0` (W(0) = ⊥ starting point for propagation)
4. Prove `hbot3` (W(3) = ⊥ via Γ surjectivity argument)

Each of these is a distinct mathematical piece. The `leaf24_containment` is the hardest and highest priority — the rest of the proof structure assumes it.

## Blockers

None. The framework builds cleanly. Further progress requires mathematical work on the sub-sorries.
