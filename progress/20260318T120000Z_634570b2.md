## Accomplished

Partial progress on issue #1111 (Stage 3.2: Ch6 prove Example 6.4.9 A_n root count):

- **An_cartan_entry**: Clean formula for A_n Cartan matrix entries (if k=j then 2, if adjacent then -1, else 0)
- **An_qform_peel**: Key decomposition lemma q_{m+1}(x) = q_m(x|_{0..m-1}) + 2x_m² - 2x_{m-1}·x_m
  - Uses Fin.sum_univ_castSucc to split finite sums, Finset.sum_ite_eq' for adjacency sums
  - This was the hardest lemma — general-n sum manipulation in Lean 4
- **An_qform_ge_endpoints**: Endpoint bound q(x) ≥ x₀² + x_{n-1}² for A_n
  - Proof by induction using An_qform_peel + nlinarith with sq_nonneg
- **An_result**: Wires An_bound + An_count into the final theorem via positiveRoots_card_eq

## Current frontier

Two sorrys remain:
1. **An_bound** (all positive root coords < 2): Needs case analysis + induction using the peel lemma. The mathematical argument is clear (SOS decomposition → each term is 0 or 1 → max value ≤ 1) but formalizing the integer sequence reasoning is complex.
2. **An_count** (rootCountFinset cardinality = n(n+1)/2): Needs to count {0,1}-vectors satisfying q=2, which are exactly contiguous interval indicators. Combinatorial argument for general n.

## Overall project progress

- ~193/583 items sorry_free (33.1%)
- This PR adds 4 proved lemmas (An_cartan_entry, An_qform_peel, An_qform_ge_endpoints, An_result structure)
- 2 new sorrys (An_bound, An_count) that unblock future completion

## Next step

- Fill An_bound: use SOS identity (follows from An_qform_peel by induction) + integer constraint argument
- Fill An_count: either direct combinatorial proof or bijection with {(a,b) : a ≤ b < n}
- Alternative: prove reversal symmetry of A_n to simplify An_bound case analysis

## Blockers

None — the remaining sorrys are well-scoped and unblocked by the peel decomposition.
