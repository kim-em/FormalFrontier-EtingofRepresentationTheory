## Accomplished

- Proved `Nat.Partition.toYoungDiagram_removeOuterCorner` in `FRTHelpers.lean` (PR #1309)
  - Shows that removing an outer corner from a partition via row lengths then converting
    to a Young diagram equals directly removing the corner from the Young diagram
  - Key technique: `convert ... using 2` + `Multiset.coe_sort` + `List.mergeSort_eq_self`
    to handle sorted-list round-trip with dependent types
- Reduced FRTHelpers sorries from 3 to 2
- Created sub-issues for remaining work: #1307 (branching rule), #1308 (hook quotient identity)

## Current frontier

`FRTHelpers.lean` has 2 remaining sorries:
1. `syt_branching_rule` (line ~235): Requires constructing a bijection between SYTs
2. `hook_corner_sum` (line ~252): Requires hook length analysis under corner removal

Both are substantial combinatorial proofs requiring 100+ lines of new infrastructure each.

## Overall project progress

Stage 3.2 proof filling continues. The FRT theorem induction skeleton is complete
(`card_standardYoungTableau_mul` compiles via sorry'd sub-lemmas). The base case and
`toYoungDiagram_removeOuterCorner` are sorry-free. Two deep combinatorial sub-lemmas remain.

## Next step

- Prove `syt_branching_rule` (#1307): construct the branching bijection SYT(n+1, λ) ≃ Σ c, SYT(n, λ\c)
- Prove `hook_corner_sum` (#1308): the hook quotient identity (independent of #1307)

## Blockers

None - both sub-issues are ready for work.
