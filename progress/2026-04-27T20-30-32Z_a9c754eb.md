## Accomplished

Issue #2601 (Ch5 heartbeat tightening): bisected the heartbeat bumps on
`glTensorRep_equivariant_schurWeyl_decomposition`
(`Chapter5/FormalCharacterIso.lean:760-761`):

- `maxHeartbeats`: 6400000 → **1200000** (5.3× reduction). Minimum compiling
  value during bisect was 800000 (700000 fails); chose 1.5× safety margin.
- `synthInstance.maxHeartbeats`: 1600000 → **60000** (26.7× reduction).
  Minimum compiling value was 40000 (30000 fails); chose 1.5× safety margin.
- Reformatted comments to satisfy `linter.style.maxHeartbeats` (each
  `set_option ... in` followed by a `-- reason` line).

Verified with two clean rebuilds (~30s and ~27s) of
`EtingofRepresentationTheory.Chapter5.FormalCharacterIso` after deleting
the olean.

## Current frontier

Audit finding F1 from review #2592 closed.

## Overall project progress

Hygiene work; no item-level deltas. Schur-Weyl L_i pipeline retains the
same proof structure with significantly reduced compile-time budget on
`FormalCharacterIso`. Open queue still has #2602 (refactor) and
heartbeat-related follow-ups elsewhere (`Theorem5_18_1.lean` lines
345/424/470/487 still emit the same linter style warning).

## Next step

Pick up #2602 (extract `glHom` + per-component ρ helpers) — its blocker
#2590 is closed, and #2630 has now landed as PR #2637.

## Blockers

None.
