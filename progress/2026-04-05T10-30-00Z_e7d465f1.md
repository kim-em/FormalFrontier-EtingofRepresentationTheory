## Accomplished

- Analyzed issue #2104 (difficulty 9): the old `garnir_columnInvCount_decrease` required ALL Garnir terms to have strictly fewer column inversions — provably false for partition (2,2) where some terms have equal count.
- Restructured the straightening algorithm's well-founded induction:
  - Replaced `columnInvCount'`-based `Nat.strongRecOn` with abstract `straighteningLT` well-order using `WellFounded.induction`
  - Replaced 3 intermediate theorems (`garnir_columnInvCount_decrease`, `garnir_identity_expansion`, `garnir_reduction'`) with a single `garnir_reduction` using the new ordering
  - Removed 6 helper lemmas that were only used by the old approach
  - Net: -115 lines (62 added, 177 deleted)
- Sorry count: 3 → 5, but the critical change is that the old sorry was **unprovable** (false statement), while all new sorries are **provable** once tabloid dominance order is defined.
- Created follow-up sub-issues:
  - #2117: Define tabloid dominance order for `straighteningLT` (difficulty 5)
  - #2118: Prove Garnir expansion respects tabloid dominance order (difficulty 8, blocked on #2117)
- PR #2119 created and builds successfully.

## Current frontier

The `straighteningLT` well-order is sorry'd — needs to be instantiated with the tabloid dominance order (#2117). Once that's done, the hard combinatorial proof that Garnir respects dominance (#2118) can be tackled.

## Overall project progress

- Stage 3 formalization continues across Chapter 5 and Chapter 6
- The polytabloid basis proof architecture is now structurally correct (all sorries provable)
- Key remaining sorries in PolytabloidBasis.lean: `polytabloid_linearIndependent` (blocked on #2088), `column_standard_in_span'`, `straighteningLT`/`straighteningLT_wf`/`garnir_reduction`

## Next step

Claim and work on #2117 (define tabloid dominance order) — this unblocks #2118 (Garnir dominance proof).

## Blockers

None for this PR. The follow-up work (#2118) is blocked on #2117.
