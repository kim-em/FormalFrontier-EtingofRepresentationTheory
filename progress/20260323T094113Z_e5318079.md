## Accomplished

- Created `PowerSumCauchyIdentity.lean` — new file with coefficient extraction infrastructure for the Cauchy determinant `det(1/(1-xᵢyⱼ))`.
- Defined `diagExponent N α` — the finsupp encoding the diagonal monomial `x^α y^α` in `CauchyVars N` (with simp lemmas `diagExponent_inl`, `diagExponent_inr`).
- Proved `cauchyRHS_coeff_diag` (modulo `cauchyProd_coeff_perm`): For `α : Fin N → ℕ` with distinct entries, `coeff (diagExponent N α) (cauchyRHS N k) = 1`.
  - The proof shows: only σ = id contributes to the sum (by injectivity of α), giving `sign(id) · 1 = 1`.
- Stated `cauchyProd_coeff_perm` (sorry'd): the coefficient of `x^α y^β` in `∏_j 1/(1-x_j y_{σ(j)})` is 1 iff `α_j = β_{σ(j)}` for all j.
- Added import to `Chapter5.lean` module file.

### Proof structure of cauchyRHS_coeff_diag

1. Expand `cauchyRHS` as sum over permutations
2. For each σ, factor out `C(sign σ)` using `MvPowerSeries.coeff_C_mul`
3. Apply `cauchyProd_coeff_perm` to get coefficient of product
4. Show only σ = id satisfies `α j = α (σ j)` (by `Function.Injective`)
5. Sum collapses to `sign(id) · 1 = 1`

## Current frontier

1 sorry remains in `PowerSumCauchyIdentity.lean`: `cauchyProd_coeff_perm` (line 79).

This is a technical lemma about coefficients of products of geometric series in disjoint variables. The proof approach: each factor `1/(1-x_j y_{σ(j)})` is determined by `(1 - x_j y_{σ(j)}) · inv = 1`. Taking coefficients gives a recurrence showing nonzero coefficients occur only at `d(inl j) = d(inr(σ j))`. The product factorizes because the variable pairs are disjoint (σ is a permutation).

## Overall project progress

- ~207/583 items sorry-free (before this PR)
- `cauchyRHS_coeff_diag`: proved (modulo 1 technical sorry)
- `alternatingKostka_norm_sq_eq_one`: still sorry (requires power sum Cauchy identity + Cauchy-Vandermonde in addition to coefficient extraction)

### Remaining chain to prove `alternatingKostka_norm_sq_eq_one`

1. **cauchyProd_coeff_perm** [sorry'd here] — coefficient factorization for product of geometric series in disjoint variables
2. **Power sum Cauchy identity** [not started] — `(1/n!) ∑_σ P_σ(x)P_σ(y) = ∏_{i,j} 1/(1-x_iy_j)` at degree n
3. **Cauchy-Vandermonde for power series** [not started] — `det(1/(1-x_iy_j)) = Δ(x)·Δ(y)·∏ 1/(1-x_iy_j)`
4. Combine: `(1/n!) ∑_σ [x^{λ+ρ}](Δ·P_σ)² = [x^{λ+ρ} y^{λ+ρ}](cauchyRHS) = 1`
5. Connect to `∑_ν L²_{νλ}` via Parseval

Items 2-5 are substantial (~500+ lines estimated).

## Next step

- Prove `cauchyProd_coeff_perm`: establish that `invOfUnit (1-XaXb) 1` has coefficient 1 at `n·(e_a + e_b)` and 0 elsewhere (via recurrence from `mul_invOfUnit`), then show product coefficients factor for disjoint variable pairs.
- Then tackle the power sum Cauchy identity (item 2 above) — either via the exponential formula or direct combinatorial bijection.

## Blockers

None for this task. The remaining work is well-scoped but substantial.
