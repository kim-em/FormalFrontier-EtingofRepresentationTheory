## Accomplished

### Proved charValue_stability (issue #2022, partial)

Refactored the proof of `charValue_stability` in Theorem5_22_1.lean:

1. **Proved `charValue_stability`** — the key lemma that `charValue N bp μ` is independent of N (depends only on the underlying partition). Proof factors through:
   - `charValue_reduce_to_n`: reduce any `charValue N bp` to `charValue n (canonicalBP N n bp)` (sorry'd)
   - `canonicalBP_eq_of_weightToPartition_eq`: equal partitions give equal canonical BPs (proved)

2. **Added `BoundedPartition.extend`** — append a trailing zero part (N → N+1). Proved `extend_last` and `extend_truncate`.

3. **Previously (same session)**: Added `BoundedPartition.truncate`, `truncate_weightToPartition`, `charValue_drop_zero` (sorry'd), helper definitions `altMonomialExp`, `diffExp`.

### PR #2028 (partial)
Created partial PR closing #2022. Issue marked for replan.

## Current frontier

3 sorries remain in Theorem5_22_1.lean:
1. `charValue_drop_zero` (line ~1521) — Key polynomial identity: removing a trailing zero variable preserves charValue. Mathematical proof approach fully worked out (via aeval setting x_last=0, relating alternant dets).
2. `charValue_reduce_to_n` (line ~1530) — Iterate charValue_drop_zero (and its reverse via extend) to go from N to n variables. Needs two sub-lemmas:
   - Down direction (N > n): antitone + pigeonhole gives trailing zeros, iterate drop_zero
   - Up direction (N < n): reverse drop_zero via extend
   - Base case (N = n): canonicalBP n n bp = bp for antitone bp
3. `finrank_weight_eq_card_sum` (line 787) — Separate issue #2023.

## Overall project progress

- `charValue_stability` is now proved (modulo 1 sorry: `charValue_reduce_to_n`)
- This was previously a direct sorry; now it has real proof structure
- Net sorry count unchanged (3), but proof architecture is significantly clearer
- `charValue_drop_zero` and `charValue_reduce_to_n` are cleanly scoped sub-problems

## Next step

1. **Prove `charValue_drop_zero`** — The core mathematical content. Approach: use `MvPolynomial.aeval` to set x_last=0, show alternantDet(N+1)|_{x_N=0} = (∏ x_i) * alternantDet(N), use `coeff_monomial_mul` for the coefficient shift.
2. **Prove `charValue_reduce_to_n`** — Mechanical iteration of charValue_drop_zero. Needs helper: if N > n and bp antitone summing to n, then last part is 0. Also needs canonicalBP_self (canonicalBP n n bp = bp for antitone bp).
3. A new issue should be created from the replan of #2022 covering these remaining sorries.

## Blockers

- `charValue_drop_zero` requires relating polynomial coefficients across different variable rings (MvPolynomial (Fin (N+1)) ℚ vs MvPolynomial (Fin N) ℚ), which is technically involved in Lean/Mathlib.
