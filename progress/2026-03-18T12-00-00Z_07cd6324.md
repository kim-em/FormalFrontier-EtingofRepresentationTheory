## Accomplished

Fixed issue #1081: removed duplicate `Etingof.IsIndecomposable` definition from `Theorem3_8_1.lean`.

### Changes made

1. **Definition2_3_8.lean**: Added `Etingof.IsIndecomposable.not_exists_nontrivial_compl` helper theorem — converts from the canonical (Nontrivial + IsCompl) form to the negation form (¬∃ M N, M≠⊥ ∧ N≠⊥ ∧ sup=⊤ ∧ inf=⊥) used in contradiction-style proofs.

2. **Lemma3_8_2.lean**: Changed `endo_indecomposable_iso_or_nilpotent` and `sum_nilpotent_endo_indecomposable` hypotheses from the raw `¬∃...` form to `Etingof.IsIndecomposable A W`. Simplified proof of the Fitting decomposition step (now just `hW.2 _ _ hFit`).

3. **Theorem3_8_1.lean**: Removed the duplicate definition (lines 23-27). Added import for Definition2_3_8. Adapted the decomposable case in Krull-Schmidt existence to handle the Nontrivial requirement (added `by_cases hS_triv : S = ⊥`). Reordered `hWC_ne`, `hW'D_ne`, `hW'C_ne` before the corresponding `_indec` proofs so Nontrivial can be derived. Adapted indecomposability transfer proofs to construct the (Nontrivial, ∀...) pair using `by_contra` + `not_exists_nontrivial_compl`.

### Verification

- `lake build EtingofRepresentationTheory.Chapter2.Definition2_3_8` ✓
- `lake build EtingofRepresentationTheory.Chapter3.Lemma3_8_2` ✓
- `lake build EtingofRepresentationTheory.Chapter3.Theorem3_8_1` ✓
- `lake build EtingofRepresentationTheory.Chapter3` ✓

## Current frontier

The duplicate `IsIndecomposable` between Ch2 and Ch3 is resolved. The third definition (`IsIndecomposableModule` class in Ch9/Definition9_2_2) remains separate as noted in the issue — it uses a different name and is a class rather than a def, so no namespace conflict exists.

## Overall project progress

- Stage 3.2 proof filling active
- ~191/583 items sorry_free (~32.8%)
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- Bug fix: unified `Etingof.IsIndecomposable` across Ch2 and Ch3

## Next step

- Feature work on remaining unclaimed issues (#1025, #1063, #1077, #1078)
- Address #1082 (missing exhaustiveness hypothesis in Theorem 9.2.1)
- Consider unifying `IsIndecomposable` and `IsIndecomposableModule` (noted in #1081 as optional)

## Blockers

None.
