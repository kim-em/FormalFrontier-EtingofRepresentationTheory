## Accomplished

- Proved Proposition6_6_7_source (F⁻ᵢ preserves indecomposability) with 2 localized sorrys
- Defined `arrowOutToReversed`: constructs reversed arrow from ArrowsOutOf using `cast`
- Stated `reversedArrow_arrowOut_eq`: roundtrip lemma (sorry'd — Decidable.casesOn blocks proof)
- Complete proof structure for source case:
  - Zero case: simple at i implies F⁻(V) = 0 (fully proved)
  - Indecomposable case: nontrivial + subrep construction + complementarity + transport
  - Disjointness at i: proved via sourceMap injectivity
  - Codisjointness at i: proof outline complete, blocked by type class synthesis (sorry'd)
  - Transport: W = ⊥ everywhere from U = ⊥ (fully proved)
  - IsCompl at v ≠ i: fully proved

## Current frontier

2 sorrys remain in Proposition6_6_7.lean:

1. `reversedArrow_arrowOut_eq` (line 684): `reversedArrow_ne_eq ha (arrowOutToReversed hi a) = a.2`
   - Both functions are identity-like type casts through `Decidable.casesOn`
   - Composition should be `id` but `cases` tactic can't generalize the decidable instances
   - `unfold` exposes the match but creates `id` wrappers and `Sigma.casesOn` that prevent `rfl`
   - Root cause: nested `Decidable.casesOn` in `ReversedAtVertexHom` definition

2. Codisjointness at source vertex (line 947): `⨅ comap W₁_at + ⨅ comap W₂_at = ⊤`
   - Proof approach: decompose via IsCompl, form z₁/z₂ in direct sum, show mkQ(z₁)=0
   - Blocked by: `AddCommGroup` instance for `(reflectionFunctorMinus Q v hi ρ).obj v`
   - The F⁻ space at source vertex goes through `Decidable.casesOn` preventing synthesis
   - Also affected: `Submodule.ker_mkQ` rewrite (needs `id` cleanup after unfold)

Both blockers share the same root cause: `Decidable.casesOn` in `ReversedAtVertexHom` and `reflectionFunctorMinus` creates dependent types that prevent:
- Type class synthesis (`AddCommGroup`, `Neg`)
- Definitional equality (`rfl`)
- Tactic generalization (`cases`)

## Overall project progress

- Proposition6_6_7 sink case: fully proved (read-only, ~530 lines)
- Proposition6_6_7 source case: proved modulo 2 technical sorrys
- The source case is ~500 lines of new code

## Next step

Fix the 2 sorrys. Possible approaches:
1. **Refactor `ReversedAtVertexHom`** to avoid `Decidable.casesOn` (use `if-then-else` or `Sum` types)
2. **Add API lemmas to Definition6_6_4.lean**: `reversedArrow_ne_eq_eq_cast` showing `reversedArrow_ne_eq ha = cast (ReversedAtVertexHom_ne_eq ha rfl)`, enabling `cast_cast` + `cast_eq`
3. **Provide `AddCommGroup` instance** for `reflectionFunctorMinus.obj i` explicitly in Definition6_6_4.lean

Option 2 is most targeted. Option 1 would be cleanest but highest risk.

## Blockers

The `Decidable.casesOn` design pattern in Definition6_6_2.lean and Definition6_6_4.lean is the root cause. A refactor or additional API lemmas in those files would unblock both sorrys.
