## Accomplished

- Fixed all build errors in Problem6_9_1.lean (4 type mismatch errors + 2 unknown constant errors):
  - Added `.symm` to `congr_arg Prod.fst/snd hlin` calls where equality direction was reversed
  - Replaced removed `LinearMap.mul_apply` with `Module.End.mul_apply`
  - Changed `pow_succ` to `pow_succ'` for correct rewrite order in odd-power swapOp lemmas
- Fixed all build errors in Theorem5_22_1.lean (unsolved goals + simp no progress):
  - Updated `YoungSymmetrizerK` and `YoungSymmetrizerZ` definitions to use `Column * Row` order matching the b·a convention from #2178
  - Rewrote `YoungSymmetrizerZ_apply_one` proof to iterate over Column (outer) then Row (inner), using `smul_mul_assoc` instead of `mul_smul_comm`

## Current frontier

- PR #2192 submitted fixing both files
- Main branch CI should be green after merge

## Overall project progress

- 14 sorry lines across project (unchanged — this was a CI fix, not sorry reduction)
- Main CI was broken since ~April 4; this fix restores it

## Next step

- Merge PR #2192 to unblock all downstream work
- Resume sorry reduction: Corollary6_8_4 (1 sorry), polytabloid proofs, extended Dynkin E-tilde

## Blockers

- None
