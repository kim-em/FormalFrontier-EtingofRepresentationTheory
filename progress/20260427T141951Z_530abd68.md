## Accomplished

Audited PR #2509 (`Theorem5_18_1_bimodule_decomposition_explicit`,
`Theorem5_18_4_bimodule_decomposition_explicit`, and the
`schurEvaluationEquiv_apply_tmul` helper) under issue #2557.

- Verified `step1 := rfl` in `schurEvaluationEquiv_apply_tmul`
  by tracing the 5-step `let`-chain through `TP.congr` /
  `piScalarRight` / `e.symm.restrictScalars` — each step reduces
  on pure tensors definitionally.
- Verified `perComp_symm_tmul` rfl: closing rfl is honest because
  `homIsotypicBridge.symm = codRestrict`, and
  `(l.codRestrict c h v).val = l v` is a `Subtype` constructor rfl.
- Verified `[CharZero k]` in (3) is genuine — required by
  `symGroupImage_isSemisimpleRing` (Maschke). Same hypothesis as
  the non-explicit `Theorem5_18_4_bimodule_decomposition`. **Not**
  in scope of #2559 (which targets `formalCharacter_coeff` and
  downstream).
- Verified `[CharZero k]`-style typeclass alignment, universe
  alignment for `V = Fin N → k`, and absence of restrictive
  hypotheses (`Module.Free k`, etc.).
- Verified all cross-references in doc-strings (#2458, #2489,
  #2540, #2482, #2483) are accurate.
- Built both files (`/tmp/build-2557.log`) — clean, only style
  warnings.

Audit report at
`progress/reviews/2026-04-27-bimodule-decomposition-explicit.md`.

Result: **PASS** — no correctness defects, no new follow-up issues
filed. Q4 raises a noisy linter pattern (maxHeartbeats comment
placement) that affects 9+ call sites pre-existing; opportunistic
cleanup, not worth a dedicated #2509 follow-up.

## Current frontier

PR #2509 fully audited and PASSes. Remaining open audits in the
wave:
- #2560 — Schur-Weyl L_i FDRep GL_N structure (PR #2504)
- #2561 — Schur-Weyl #2a foundations (PR #2511)

The Schur-Weyl chain critical-path infrastructure is now audited
for both the equivariance layer (#2547, PASS) and the explicit
bimodule decomposition (#2557, PASS).

## Overall project progress

- Stage 3.2 (Chapter 5) deep in Schur-Weyl wave; PR #2540 (Schur-Weyl
  #3 Part B) blocked on the equivariance proof that consumes
  `Theorem5_18_4_bimodule_decomposition_explicit`.
- All audits filed in this wave so far have PASSed (or
  PASS-with-followups).

## Next step

Pick up #2560 or #2561 (other open Schur-Weyl audits in the wave),
or proceed to Schur-Weyl #3 Part B (#2540) once that issue is
unclaimed and ready for work.

## Blockers

None.
