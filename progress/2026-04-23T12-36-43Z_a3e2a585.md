## Accomplished

Scoped issue #2440 (Schur-Weyl / FormalCharacterIso gap) per the issue
body's research+decomposition deliverable. Produced
`progress/schur-weyl-scoping.md` covering:

- book-level strategy (Etingof Theorems 5.18.4, 5.22.1, 5.23.2 — the
  relevant pieces are already formalized or within reach);
- Mathlib survey (GREEN: Schur's lemma, `IsSemisimpleModule`, isotypic
  components, Maschke, `TensorPower`; YELLOW: `Module.End.IsSemisimple`,
  Wedderburn-Artin; RED: no named GL_N Schur-Weyl decomposition, no
  formal character comparison theorem);
- project-internal survey (Theorem5_18_4 already has the abstract
  decomposition V^⊗n ≅ ⊕ S_i ⊗ L_i sorry-free, and all of FormalCharacterIso
  except the one line-221 sorry is proved — the gap is naming-the-summands
  + lifting to arbitrary polynomial reps);
- a proof decomposition of 6 sub-issues with dependency graph and
  per-issue difficulty estimates;
- flagged a latent issue: `Theorem5_23_2_i` is vacuously proved (by
  `IsSemisimpleModule k Y` over a field, which is trivially true), so
  the book-level complete-reducibility content is not actually formalized —
  sub-issue 5 of the decomposition is responsible for fixing this.

Total estimated effort to close #2440 once sub-issues are filed: **5–8
worker-sessions**, bounded by the risk concentrated on sub-issue #2 (embedding
a homogeneous polynomial GL_N-rep into `(V^⊗n)^m`).

No code changes to `FormalCharacterIso.lean` in this PR — the issue
scope is explicitly research+decomposition only.

## Current frontier

Wave 54 / 55 has landed. Issue #2440 is the hardest of the remaining
"structural" items; after this scoping doc the hardest un-scheduled
items are the three framework walls tracked by #2436 (waiting on Kim's
decision), #2438 (Garnir rewrite), #2439 (DTildeVertex refactor).

## Overall project progress

- 11 sorries still blocked on Wall 1 (Ẽ_n / T(p,q,r) indecomposability,
  issue #2436 — awaiting Kim's A/B/C decision).
- 1 sorry on Wall 2 (D̃_n refactor, issue #2439, structurally sound).
- 1 sorry on Wall 3 (Garnir straightening, issue #2438, framework rewrite).
- 1 sorry on FormalCharacterIso (this issue, #2440 — now scoped; ready
  for planner to post 6 sub-issues).
- Non-wall items: Proposition 5.22.2 currently depends on the #2440
  sorry (hence on sub-issue #6 of the decomposition); closing #2440
  unblocks it.

## Next step

A planner session should:
1. Post sub-issues 1–6 per §6 of `progress/schur-weyl-scoping.md`.
2. Add explicit dependency edges: #2 depends on #1; #5 depends on #1,
   #2, #3; #6 depends on #4 and #5.
3. Close #2440 with a forward link to sub-issue #6 (since #6 is the
   one that actually closes the sorry and wraps the others).
4. Merge this PR first so the scoping doc is on `main` before the
   sub-issues reference it.

A worker session can then claim sub-issues 1, 3, or 4 in parallel (they
have no cross-dependencies). Sub-issue 2 is the longest and most at risk
of walling; recommend starting it early so any re-scoping can happen
before #5 is claimed.

## Blockers

None for the scoping deliverable. The three wave-54 framework walls
(#2436, #2438, #2439) remain orthogonal to this issue and unaffected.
