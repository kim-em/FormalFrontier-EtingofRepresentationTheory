## Accomplished

Worker session `21a34079` decomposed parent issue #2648
(`garnir_twisted_in_lower_span_aux`) into two sub-issues, then
claimed and completed the easier sub-issue #2651.

**Decomposition of #2648** (difficulty 8, ~200-300 line estimate):

- **#2651** (this PR — easy, standalone): bridge lemma
  `polytabloidTab_in_lower_span_of_dominates`. Transfers an SYT-level
  span-membership into the column-standard / strict-dominates-or-
  smaller-rowInv span used by the aux conclusion. Used inside the
  Algorithm A IH-case dispatch.
- **#2652** (medium → hard, depends on #2651): main `garnir_twisted_in
  _lower_span_aux` Algorithm A loop + col-std existence sub-lemma for
  the `[α] = [σ]` case.

Parent #2648 was skipped via `coordination skip` with the
`Decomposed into #2651, #2652` breadcrumb; the planner's replan
triage will close it once both sub-issues land.

**This PR** (#2651) added 81 lines to
`EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean`,
inserted between `tabloidSupport_straightening` (line 1260) and
`twistedPolytabloid_pigeonhole_pair` (line 1363):

1. **`sytPerm_isColumnStandard'`** — `sytPerm T` is column-standard.
   Proof: rewrite `(sytPerm T).symm p` as `T.val (canonicalFilling p)`,
   reduce `colOfPos`/`rowOfPos` of `p` to the cell-coordinates `.val.2`
   / `.val.1`, then apply `T.prop.2.2` (the col-strict-increasing
   clause of the SYT definition).
2. **`sytPerm_isRowStandard'`** — `sytPerm T` is row-standard. Mirrors
   the inline argument in `column_row_standard_is_syt`'s row-standard
   derivation; uses `T.prop.2.1` (the row-strict-increasing clause).
3. **`polytabloidTab_in_lower_span_of_dominates`** — the bridge
   lemma. For col-std σ with positive row inversion count and SYT T
   with `tabloidDominates la σ (sytPerm T)`, `polytabloidTab T` lies
   in the target span used by `garnir_twisted_in_lower_span_aux`.
   Proof: rewrite `polytabloidTab T = generalizedPolytabloidTab
   (sytPerm T)` via `generalizedPolytabloidTab_eq_polytabloidTab`, then
   case split on `toTabloid (sytPerm T) = toTabloid σ`:
   - **Equal** (`[T] = [σ]`): use `Or.inr` with `rowInvCount'(sytPerm T)
     = 0 < rowInvCount' σ` (from `sytPerm_isRowStandard'` and
     `rowInvCount'_eq_zero_iff`).
   - **Unequal** (`[T] ≺ [σ]`): use `Or.inl` strict-dominance
     (`tabloidStrictDominates` from `hdom` plus the inequality).

All three new theorems are sorry-free.

## Current frontier

- **#2651 closed by PR (forthcoming)**.
- **#2652** unblocks once this PR lands (it depends on #2651). The
  remaining work is the Algorithm A main loop on tabloid support of
  `twistedPolytabloid w σ`, plus the col-std existence sub-lemma for
  the `[α] = [σ]` case. Estimated ~150-250 lines.
- **#2649** (sub-B of #2605, packed mutual induction restructure)
  depends on #2648's residual scope (#2652).
- **#2605** (Wall 3 critical-path) remains the bottleneck for
  closing the last Wall 3 sorry on `garnir_twisted_in_lower_span`.

## Overall project progress

Stage 3 formalization. Sorry count unchanged: **7 sorry leaves / 4
files** (Chapter6/InfiniteTypeConstructions: 3,
Chapter5/SpechtModuleBasis: 2, Chapter5/FormalCharacterIso: 1,
Chapter2/Theorem2_1_2: 1).

This PR adds 81 sorry-free lines. The bridge lemma is now available
for the next worker on #2652.

Critical paths:
- **Wall 1** (Ch6): blocked on #2436 human-oversight (replan).
- **Wall 3** (Ch5 SpechtModuleBasis): #2605 critical path, with
  #2648 decomposed into #2651 (this PR) + #2652. After #2652 lands,
  #2649 (sub-B packed-induction) becomes actionable.
- **Schur-Weyl L_i chain**: #2582, #2610 paths still in progress
  (separate from Wall 3).
- **Theorem 2.1.2** (Ch2): blocked on Wall 1.

## Next step

**Workers** (priority order):
1. **#2643** (Schur-Weyl C-4a-i — c_λ acts as scalar α on S_λ block).
   Highest unclaimed item on the Schur-Weyl L_i chain.
2. **#2633** (Theorem5_18_4_GL_rep_decomposition_simple — small,
   bridges B-side simplicity to GL_N-rep simplicity).
3. **#2629** (CharZero hygiene). Quick hygiene win.
4. **#2602** (extract `glHom` helpers). Lowest priority refactor.
5. **#2652** (Wall 3 critical-path) becomes unblocked once this PR
   lands. The next planner will move it from blocked to actionable.

## Blockers

None for this session. PR pending CI.
