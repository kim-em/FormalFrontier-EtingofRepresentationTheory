## Accomplished

Fixed a sign convention error in the Frobenius character formula chain (Theorem 5.15.1, issue #1364).

The `vandermondePoly` is defined as `∏_{i<j}(xⱼ-xᵢ)`, but the book's Frobenius formula uses the alternant `a_ρ = ∏_{i<j}(xᵢ-xⱼ)`. These differ by `sign(Fin.revPerm) = (-1)^{n(n-1)/2}`. The missing sign factor made `alternating_kostka_eq_delta` provably false (verified by counterexample: n=2, λ=ν=[1,1] gives sum=-1 but δ=1).

Corrected three theorems:
- `alternating_kostka_eq_delta`: RHS changed from `δ_{λ,ν}` to `sign(rev) • δ_{λ,ν}`
- `spechtCharacter_eq_alternating_sum_permCharacter`: RHS wrapped in `sign(rev) • (...)`; proof sorry'd pending alternating_kostka_eq_delta
- `Theorem5_15_1`: RHS wrapped in `sign(rev) • coeff(...)` with proof relying on the corrected intermediate lemma

## Current frontier

The 3 sorry'd declarations in Theorem5_15_1.lean:
- `isotypicComponent_linearEquiv_fun` (line 768, issue #1345)
- `alternating_kostka_eq_delta` (line 1220, issue #1364) — hardest; requires Kostka matrix upper-triangularity
- `spechtCharacter_eq_alternating_sum_permCharacter` (line 1294) — re-provable once alternating_kostka_eq_delta is done; proof strategy documented in comments

## Overall project progress

- ~194/583 items sorry-free (33.3%)
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- Theorem 5.15.1 statement now correct (sign convention fixed)

## Next step

- Prove `alternating_kostka_eq_delta` (the hardest remaining sorry in the Frobenius formula)
- Once that's done, re-prove `spechtCharacter_eq_alternating_sum_permCharacter` using the sign(rev)² = 1 cancellation

## Blockers

- None
