## Accomplished

Proved `hook_corner_sum` (FRT inductive step) modulo a sorry'd sub-lemma
`hook_quotient_identity`. The algebraic reduction casts to ℚ, replaces
|SYT(n,λ\c)| with n!/hookProd(λ\c) using the IH, factors out n!, applies
the hook quotient identity, and simplifies n!*(n+1) = (n+1)!.

The hook quotient identity `∑_c hookProd(λ)/hookProd(λ\c) = n+1` is stated
over ℚ since individual terms are non-integer (e.g., 3/2 for partition (2,1)).

Changes merged to main via PR #1314. Filed #1319 for the remaining sorry.

## Current frontier

- `hook_quotient_identity` (FRTHelpers.lean ~line 249): sorry'd, tracked in #1319
- `syt_branching_rule` (FRTHelpers.lean ~line 235): pre-existing sorry
- These are the 2 remaining sorries blocking a complete FRT proof

## Overall project progress

- ~186/583 items sorry_free (~31.9%)
- FRT theorem structure: base case proved, inductive step proved modulo
  hook_quotient_identity and syt_branching_rule
- hook_corner_sum direct sorry eliminated (replaced by cleaner sub-lemma sorry)

## Next step

- Prove `hook_quotient_identity` (#1319): requires building rowLen/colLen change
  lemmas for removeCorner, hookLength change lemmas, and the combinatorial sum identity
- Or prove `syt_branching_rule`: construct the branching bijection
  SYT(n+1,λ) ≃ Σ_c SYT(n,λ\c) via removing the largest entry

## Blockers

- hook_quotient_identity requires substantial infrastructure (rowLen/colLen of
  removeCorner, hookLength change formulas) that doesn't exist yet
- The sum identity ∑_c hookProd(λ)/hookProd(λ\c) = n+1 is a deep result
  (equivalent to the Greene-Nijenhuis-Wilf hook walk identity)
