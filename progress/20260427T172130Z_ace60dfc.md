## Accomplished

Review session `ace60dfc` — completed audit issue #2592 covering the
two un-audited links of the Schur-Weyl L_i critical path:

- **PR #2578** (issue #2540): closed sorry on
  `glTensorRep_equivariant_schurWeyl_decomposition`
  (`Chapter5/FormalCharacterIso.lean`).
- **PR #2579** (issue #2572): introduced
  `Theorem5_18_4_GL_rep_decomposition_explicit`
  (`Chapter5/Theorem5_18_4.lean`).

Audit report:
`progress/reviews/2026-04-28-schur-weyl-Li-equivariant-and-explicit.md`.

**Verdict: PASS-with-followups** on both PRs. All 10 audit questions
(Q1-Q5 for PR #2578, Q6-Q10 for PR #2579) check out. Three
non-blocking follow-ups filed:

- **F1** → issue #2601 (heartbeat tightening on
  `glTensorRep_equivariant_schurWeyl_decomposition` — its 6400000 bump
  was justified pre-#2599 but is likely overkill after #2599's hoist).
- **F2** → issue #2602 (extract `glHom` + per-component-`ρ` helpers —
  blocked on #2590).
- **F3** → comment on #2590 noting that its "line 654 mutual recursion"
  heads-up is stale on the current code (`_explicit`'s body never
  references the non-explicit form).

Audit summaries posted on issues #2540 and #2572.

## Audit highlights

- `glHom : GL_N →* centralizer(symGroupImage)` is the canonical
  `g ↦ g^{⊗n}` construction, matches `glTensorRep`'s `toFun`
  byte-for-byte; membership in centralizer follows from
  `Theorem5_18_4_centralizers.2.symm` + `Algebra.subset_adjoin`.
- `ρ i` direct construction (post-composition with
  `centralizerToEndA (glHom g)`) correctly bypasses an `AddCommGroup`
  diamond between `Submodule.addCommGroup` and the
  `centralizerModuleHom`-derived structure.
- `Module.Finite k` derivations are sound: `Module.Free.of_divisionRing`
  + `Module.Finite.linearMap` + `LinearMap.restrictScalarsₗ` injection
  is the standard chain.
- The action formula in `_explicit` (added by PR #2599) closes by
  `rfl` because `L_carrier i = LinearEquiv.refl k _` and
  `(L i).ρ g l = (centralizerToEndA (glHom g)).comp l`.

## Current frontier

Schur-Weyl L_i critical path is clean through PR #2579. Audit chain
status:

- ✅ #2549 (equivariance PRs #2538/#2539)
- ✅ #2554 (plumbing PRs #2528/#2551)
- ✅ #2555 (character additivity PRs #2516/#2534)
- ✅ #2557 (explicit bimodule PR #2509)
- ✅ #2560 (L_i FDRep GL_N PR #2504)
- ✅ #2561 (foundation tier PR #2511)
- ✅ #2592 (this audit) — PRs #2578 + #2579

## Overall project progress

Stage 3 formalization. Sorry count unchanged (audit only). Schur-Weyl
character chain is fully audited end-to-end. Downstream work
(#2493 / #2482 / #2483 — irreducibility, basis identifications,
final assembly) is unblocked.

## Next step

- Workers can claim either of the open `feature` issues:
  - #2590 (delegate non-explicit `_GL_rep_decomposition` to `_explicit`)
  - #2594 (propagate `Module.Finite` into `_explicit` bimodule
    existentials)
- Tighten heartbeats: #2601 (newly filed F1 follow-up).
- Helper extraction: #2602 (newly filed F2 follow-up, blocked on
  #2590).

No other open audit gaps on the Schur-Weyl chain.

## Blockers

None.
