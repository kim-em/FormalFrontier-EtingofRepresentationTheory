## Accomplished

Closed sorry on `glTensorRep_equivariant_schurWeyl_decomposition` in
`EtingofRepresentationTheory/Chapter5/FormalCharacterIso.lean` — the
final piece of the Schur-Weyl #3 character chain (issue #2540).

The proof:
1. Sets `V := Fin N → k`, gets the explicit bimodule decomposition
   `e : V^{⊗n} ≃[k] ⨁ i, ↥(V' i) ⊗[k] (↥(V' i) →ₗ[A] V^{⊗n})` with
   evaluation formula `e.symm (of i (v ⊗ₜ l)) = l v` from
   `Theorem5_18_1_bimodule_decomposition_explicit`.
2. Builds `glHom : GL_N →* centralizer(symGroupImage)` via the diagonal
   action `g ↦ g^{⊗n}` (using the centralizer identity from
   `Theorem5_18_4_centralizers`).
3. Constructs `ρ i : GL_N →* End_k (Hom_A(V'_i, V^{⊗n}))` directly as
   post-composition with `centralizerToEndA (glHom g)`. Built directly
   (not via `Module.toModuleEnd`) to avoid an SMul-instance diamond
   between `centralizerModuleHom` and the global `Module` instance
   resolution.
4. Establishes `Module.Finite k` for `↥(V' i)` and `↥(V' i) →ₗ[A] E`
   via Free + LinearMap finiteness + restrictScalars injection.
5. Proves equivariance by reducing `(glTensorRep g) ∘ e.symm =
   e.symm ∘ directSum_action g` via `DirectSum.linearMap_ext` +
   `TensorProduct.ext'` to the basis case `of i (s ⊗ₜ l)`. Both sides
   evaluate to `PiTensorProduct.map (mulVecLin g) (l s)`.

Heartbeats bumped to `maxHeartbeats 6400000` (kernel whnf timeout
during type-checking the existential output).

## Current frontier

Issue #2540 fully closed — no new sorries. The `glTensorRep_...`
theorem now type-checks downstream uses such as
`formalCharacter_tensorPower_eq_sum_character_L`.

## Overall project progress

Stage 3 formalization. With this PR, the Schur-Weyl character chain
loses its key remaining definition-level sorry (line 774 of
`FormalCharacterIso.lean`). The only remaining sorry in this file is
`iso_of_formalCharacter_eq_schurPoly` at line 387 (Wall 1 territory,
unrelated).

## Next step

Next planner cycle: re-triage the queue. With `glTensorRep_...` proved,
downstream Schur-Weyl issues (#2493, #2482, #2483) move forward. Wall 1
(#2436) is the highest-priority remaining wall.

## Blockers

None.
