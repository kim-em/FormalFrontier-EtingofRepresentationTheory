## Accomplished

Worker session on #2471 (Theorem5_18_1 bimodule main assembly). Landed
both deliverables with zero sorries in two commits on `agent/e734e799`:

### Commit 1 (166532f): `homIsotypicBridge`

```lean
noncomputable def homIsotypicBridge
    (A : Subalgebra k (Module.End k E))
    (V : Submodule A E) [IsSimpleModule A V]
    (c : Submodule A E)
    (hc_eq : c = isotypicComponent A E V) :
    (V →ₗ[A] c) ≃ₗ[k] (V →ₗ[A] E)
```

Forward: post-compose with `c.subtype`. Inverse: `g.codRestrict c` with
`range_le_isotypicComponent_of_simple` transported along `c = isotypic-
Component A E V`. Needs 1.6M `maxHeartbeats` + 800k `synthInstance.max-
Heartbeats`. The explicit `(k := k) (E := E) (V := V)` named arguments
on the helper call prevent section-variable auto-binding from mis-
inferring `k := ↥V`.

### Commit 2 (9416ca5): `Theorem5_18_1_bimodule_decomposition`

Full bimodule form of Etingof 5.18.1(iii) — `E ≃[k] ⨁ᵢ Vᵢ ⊗[k] Lᵢ`
where `Lᵢ := Vᵢ →ₗ[A] E` is a genuine `B`-module (not just a `k`-vector
space), and the `Vᵢ` are pairwise non-isomorphic simple `A`-modules.

Assembly inside the proof body (avoided defining `V'` and `L` at top
level to prevent dependent-typeclass timeouts):

1. **Finite index**: `isotypicComponents A E` is Noetherian-finite (from
   `IsSemisimpleRing A → IsNoetherianRing A → IsNoetherian A E` + `Finite`
   instance on `isotypicComponents`), and `φ : isotypicComponents ≃ Fin m`.
2. **Choice**: `V' c := (eq_bot_or_exists_simple_le c.1).choose`, with
   `c.1 = isotypicComponent A E (V' c)` via `eq_isotypicComponent_of_le`.
3. **Distinctness**: `V i ≃[A] V j ⇒ isotypicComponent A E (V i) =
   isotypicComponent A E (V j) ⇒ c i = c j ⇒ i = j`.
4. **Main iso chain**: `isotypicDirectSumEquiv.symm` then
   `restrictScalars k` then per-component Schur evaluation +
   `homIsotypicBridge` then `DirectSum.lequivCongrLeft φ`.

Key helpers / transport:
* `LinearEquiv.ofEq c.1 (isotypicComponent A E (V' c)) (V'_spec c)`
  bridges the A-module equality.
* `LinearEquiv.isIsotypicOfType_iff.mpr` transfers
  `IsIsotypicOfType A ↥(isotypicComponent A E (V' c)) ↥(V' c)` (from
  `.isotypicComponent`) to `IsIsotypicOfType A (↥c.1) ↥(V' c)`.
* `Module.Finite k ↥p` for `p : Submodule A E` obtained via
  `Module.Finite.of_injective (p.subtype.restrictScalars k) Subtype.val_injective`.

Both the bridge and main theorem use 1.6M/800k heartbeats.

`lake build EtingofRepresentationTheory.Chapter5.Theorem5_18_1`
succeeds with zero sorries.

## Current frontier

* **Commits landed on `agent/e734e799`**: 166532f, 9416ca5.
* **PR to open next**: closes #2471.
* **Unblocks**: #2472 (`Theorem5_18_4_bimodule_decomposition`) and
  thereby #2458 (Schur-Weyl #3).

## Overall project progress

* **Wall 2 (D̃_n)**: Stage A/B/C main math merged; residuals #2459,
  #2474 unclaimed.
* **Wall 3 (#2450 Garnir)**: open, unclaimed, needs design.
* **Schur-Weyl chain**:
  * #1 ✅, #4 ✅
  * Bimodule foundations:
    * `isotypicDirectSumEquiv`, `schurEvaluationEquiv` (PR #2467) ✅
    * centralizer/bridge helpers (PR #2473) ✅
    * `homIsotypicBridge` + `Theorem5_18_1_bimodule_decomposition` —
      this session, pending PR
  * #2472 (Theorem5_18_4 upgrade) — unblocked by this session's PR
  * #2 (#2468) — unclaimed, parallel-safe
  * #3 (#2458) — blocked transitively on #2472.
* **Sorry count on this branch**: unchanged from main (this session
  adds no sorries, only new theorems with complete proofs).

## Next step

After this PR merges, the next planner can close #2471 via the
auto-merge and move to the follow-up:

1. **#2472** — Theorem5_18_4 bimodule upgrade (highest priority on
   Schur-Weyl critical path; unblocks #2458).
2. **#2468** — Schur-Weyl #2 embedding. Parallel-safe.
3. **#2459** / **#2474** — Wall 2 Fin-transport tails. Low-risk mechanical.

## Blockers

* **#2458** blocked on **#2472** (now actionable once this PR merges).
* **#2401** blocked on **#2436** (Ẽ/T framework, human oversight).
* **#2436** awaits Kim's input.
