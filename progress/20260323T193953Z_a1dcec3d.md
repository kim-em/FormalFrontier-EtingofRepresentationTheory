## Accomplished

### Proved principalSeries_simple_of_ne (issue #1646)

Proved that V(χ₁,χ₂) is irreducible when χ₁ ≠ χ₂ (Theorem 5.25.2 part 1) by filling in the two sorry'd helper lemmas:

1. **principalSeries_nontrivial**: Shows the subrepresentation lattice is nontrivial by constructing a nonzero covariant function via `mkCovariantFun` and showing ⊥ ≠ ⊤.

2. **principalSeries_delta_spans_top**: Shows any G-stable subspace S containing a "delta at rep(none)" (nonzero at rep(none), zero at all rep(some t)) must be all of V. Key strategy:
   - Apply the Weyl element w = rep(some 0) to produce a delta at rep(some 0) (uses: rep(some 0)² = -I ∈ B, and rep(some t)·w has nonzero (1,0) entry for t≠0)
   - Apply translations τ_{-t} to shift the delta to rep(some t)
   - Decompose any f as f(rep(none))·g' + Σ_t f(rep(some t))·(α⁻¹·ρ(τ_{-t})(wg')), all in S

Three new helper lemmas:
- `action_weyl_some_zero`: ρ(w)(f) at rep(some 0) = f(rep(some 0)·w)
- `cosetRep_some_mul_weyl_not_borel`: rep(some t)·w not in Borel for t≠0
- `action_weyl_some_ne_zero`: ρ(w)(f) vanishes at rep(some t) for delta functions when t≠0

Also fixed a pre-existing duplicate declaration of `simple_of_full_faithful_preservesMono'`.

## Current frontier

- `Theorem5_25_2_part1` (principalSeries_simple_of_ne) is fully proved, sorry-free
- Remaining sorries in Theorem5_25_2.lean are for parts 2 and 3 (complementW infrastructure)

## Overall project progress

- principalSeries_simple_of_ne chain: 0 sorries (was 2)
- File-level: 6 sorry declarations remain (complementW_eval_surjective, principalSeriesSubmodule_finrank, principalSeries_decomp, complementW_simple, complementW_iso_implies_eq, Theorem5_25_2_part3b)

## Next step

- Work on Theorem 5.25.2 parts 2 and 3 (complementW infrastructure)
- Or address other high-priority items from the sorry landscape

## Blockers

None.
