## Accomplished

Partial progress on Theorem5_27_1 character formula (issue #1782):

- **Proved LHS simplification** of the character formula: transformed the trace sum
  `∑ q, if g⁻¹•q = q then trace(c•ρ(s)) else 0` into the canonical form
  `∑ q, if q.out⁻¹*g*q.out ∈ H then χ(φ(q.out⁻¹)(a)) * char(q.out⁻¹*g*q.out) else 0`
  using `coset_fixed_iff` + trace linearity + `FDRep.character` unfolding.

- **Restructured character formula proof** with `trans` through intermediate form,
  separating the LHS simplification (now proved) from the RHS fiber sum decomposition.

- **Eliminated `set` variables** (`σ'`, `L'`) that were causing Decidable instance
  mismatches with `classical`, working directly with raw expressions.

- **Documented detailed proof outline** for the remaining RHS fiber sum decomposition:
  (A) left-H-invariance of f via `stab_char_inv` + `char_mul_comm`,
  (B) change of variables h↦h⁻¹ via `Equiv.sum_comp`,
  (C) right-H-invariance of f∘inv,
  (D) fiber sum via `groupEquivQuotientProdSubgroup`,
  (E) matching F(q) = f(q.out⁻¹).

## Current frontier

- Character formula: LHS branch proved, RHS branch (fiber sum decomposition) still sorry'd
- Sorrys (i)-(iii) (irreducibility, orbit injectivity, completeness) untouched — these
  require deep Clifford theory infrastructure not in Mathlib

## Overall project progress

33 sorrys across 21 files. 568 of 583 items (97.4%) sorry-free.
This PR makes structural progress on the character formula but doesn't reduce the sorry count.

## Next step

1. Complete the RHS fiber sum proof (steps A-E documented in comments). The key technical
   pieces needed: `stab_char_inv`, `FDRep.char_mul_comm`, `Equiv.sum_comp`,
   `groupEquivQuotientProdSubgroup`. Watch out for Decidable instance mismatches.
2. Consider extracting a reusable helper lemma for "fiber sum over quotient for H-invariant
   functions": `∑ h, f(h) = |H| * ∑ q, f(q.out)` when f is constant on left cosets.
3. Sorrys (i)-(iii) require substantial Clifford theory infrastructure (~200-300 lines each).

## Blockers

- **Decidable instance mismatches**: `classical` and concrete `QuotientGroup.instDecidableEq`
  instances create terms that can't be rewritten with `rw`/`simp`. Workaround: use `trans` +
  separate goals, or `convert`, or avoid `set` for local definitions.
- **Mackey criterion / Clifford theory** not in Mathlib: proofs (i)-(iii) require building
  this infrastructure from scratch.
