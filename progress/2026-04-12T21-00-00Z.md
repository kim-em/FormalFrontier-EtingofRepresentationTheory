## Accomplished

- Proved `columnInvCount'_swap_lt` — swapping a column inversion pair strictly decreases column inversion count
  - MapsTo: injection g maps after-inversions into before-inversions \ {(p₁,p₂)}, 3 branches with subcases
  - InjOn: g is injective on after-inversions, 9 cases from 3×3 branch combinations
  - Key technical challenge: Lean 4's `subst`/`rfl` eliminates function parameters (RHS of equality), fixed by reversing equalities with `rw [eq_comm]` before `subst`
- Proved `exists_column_standard_mul` — for any σ, construct q₀ ∈ Q_λ with q₀σ column-standard via strong induction on `columnInvCount'`
- This eliminates one of two remaining sorries blocking `finrank_spechtModule_eq_card_syt'`
- Created PR https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/2269 with auto-merge enabled
- Closes issue #2265

## Current frontier

Only one sorry remains in `SpechtModuleBasis.lean`:
- `polytabloidTab_column_standard_in_span` (line 306) — core Garnir straightening for column-standard case (issue #2266, difficulty 8)

## Overall project progress

- `finrank_spechtModule_eq_card_syt'` (dim V_λ = |SYT(λ)|) is proved modulo 1 sorry (down from 2)
- The ≥ direction (`finrank_spechtModule_ge_card_syt`) is fully proved
- The ≤ direction chain is fully connected:
  - `exists_column_standard_mul` ✅ (just proved)
  - `polytabloidTab_column_standard_in_span` — 1 sorry remaining
  - `generalizedPolytabloidTab_mem_span_polytabloidTab` → `tabloidProjection_spechtModule_le_span` → `finrank_spechtModule_le_card_syt` — all proved modulo above

## Next step

Prove `polytabloidTab_column_standard_in_span` (issue #2266, difficulty 8). This requires:
- Well-founded relation on tabloids (Tabloid is Fintype, so tabloidStrictDominates is well-founded)
- Garnir set construction at row descents
- Dominance decrease argument for Garnir expansion
- Handling twisted polytabloids (key difficulty)

## Blockers

None for the completed work. The remaining `polytabloidTab_column_standard_in_span` is genuinely difficult (difficulty 8) and is tracked as issue #2266.
