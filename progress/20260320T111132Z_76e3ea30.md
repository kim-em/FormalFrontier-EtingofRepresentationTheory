## Accomplished

Significant structural progress on issue #1373 (alternating_kostka_eq_delta, Theorem 5.15.1).

### Proved lemmas (sorry-free)
- **`multiset_entries_eq_of_partition_eq`**: If `finsuppToPartition v = la`, then the full multisets of entries (including zeros) of `v` and `la.toFinsupp` are equal. Uses cardinality + nonzero filter argument.
- **`shifted_partition_strict_mono`**: The shifted partition `la.toFinsupp + ρ` is strictly anti-monotone (strictly decreasing). Uses sorted list pairwise property + ρ strict decrease.
- **`rev_of_permExponent_eq_rhoShift`**: If `permExponent n π = rhoShift n` then `π = Fin.revPerm`. Uses π⁻¹ = revPerm via Fin.val extensionality + revPerm self-inverse.

### Structured the inequality proof via rearrangement inequality
The inequality sorry in `sorted_shifted_strict_dominates` (originally: show `finsuppToPartition(la+ρ-e_π) ≠ la` when `π ≠ rev`) is now decomposed into:

1. **`inner_product_eq_of_partition_eq`** (sorry'd): From partition equality, derive `∑ a(i)·e_π(i) = ∑ a(i)·ρ(i)` via sum-of-squares expansion
2. **Monovary setup** (proved): Both `a = la.toFinsupp + ρ` and `ρ` are strictly decreasing → they Monovary
3. **Permutation σ = rev ∘ π⁻¹** (proved): Maps `g(σ(i)) = e_π(i)`, connecting rearrangement to inner product
4. **Rearrangement equality condition** (proved): `Monovary.sum_mul_comp_perm_eq_sum_mul_iff` gives `Monovary f (g ∘ σ)`
5. **Antitone → identity** (proved): From Monovary + StrictAnti → Antitone → StrictMono via rev → identity via `Fin.coe_orderIso_apply`
6. **`rev_of_permExponent_eq_rhoShift`** (proved): Final step closing the proof

## Current frontier

3 sorry warnings remain (same count as before, but better structured):

1. **`inner_product_eq_of_partition_eq`** (line ~1353): Algebraic manipulation — multiset equality → sum of squares equality → inner product equality via expansion and permutation sum invariance. This is the sole remaining blocker for the inequality sorry.

2. **`sorted_shifted_strict_dominates` dominance** (line ~1422): Majorization (partial sum inequalities). Requires showing partial sums of the sorted shifted partition dominate la. Hard — needs rearrangement inequality for partial sums.

3. **`alternating_kostka_eq_delta` la ≠ nu case** (line ~1552): Genuine cancellation in the alternating sum. Very hard — requires Kostka matrix inverse / unitriangularity.

## Overall project progress

- ~194/583 items sorry-free (33.3%)
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- Theorem 5.15.1: la = nu case fully proved; la ≠ nu case and strict dominance remain

## Next step

- **Highest priority**: Prove `inner_product_eq_of_partition_eq`. This is pure algebra:
  1. Use `multiset_entries_eq_of_partition_eq` to get multiset equality
  2. Derive `∑ v(i)² = ∑ la.toFinsupp(i)²` from multiset equality
  3. Expand both as `∑(a-σ)²` and use `∑ e_π² = ∑ ρ²` (permutation reindexing)
  4. Cancel to get `∑ a·e_π = ∑ a·ρ`
- After that, the inequality sorry is fully resolved (all other steps are proved).
- Dominance sorry and la≠nu case remain hard open problems.

## Blockers

None — all remaining sorries are well-understood mathematical problems, not infrastructure blockers.
