## Accomplished

Proved Corollary 5.15.4 (Cauchy identity) — the determinant of the matrix
(1/(1 - xᵢyⱼ)) equals Σ_σ sign(σ)/∏(1 - xⱼy_{σ(j)}).

The proof unfolds the Leibniz formula via `Matrix.det_apply'`, reindexes the
outer sum by σ ↦ σ⁻¹ (using `Fintype.sum_equiv` with `Equiv.inv`), matches
the sign via `sign_inv` and `map_intCast`, and reindexes the inner product
by i ↦ σ(i) (using `Fintype.prod_equiv`).

Updated `progress/items.json`: Chapter5/Corollary5.15.4 → `sorry_free`.

## Current frontier

Corollary 5.15.4 is complete. No remaining sorries in the file.

## Overall project progress

- ~185/583 items sorry_free (~31.7%)
- Chapter 5: 35/157 sorry_free (~22.3%)
- Remaining unclaimed feature issues: #984 (hook length), #985 (symmetric/exterior power)

## Next step

- Issue #990 PR submitted. Pick up #990 (Cauchy identity) or #985 (symmetric/exterior power) next.
- Issue #991 (review audit) is also available for a review session.

## Blockers

None.
