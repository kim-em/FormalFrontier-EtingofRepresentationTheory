## Accomplished

- Added `DecidableEq` and `Fintype` instances for `BoundedPartition N n`
- Proved `rename_alternant_det`: alternant determinant is antisymmetric under variable permutation (`rename σ (det M) = sgn(σ) • det M`)
- Proved Vandermonde determinant is nonzero in `MvPolynomial (Fin N) ℚ`
- Structured proof of Proposition5_21_1: reduces to antisymmetric basis decomposition via integral domain cancellation and `schurPoly_mul_vandermonde`
- Provided `Finset.univ` as witness for the existential

## Current frontier

One sorry remains in Proposition5_21_1: the antisymmetric basis decomposition lemma.
The goal is: `Δ * p_μ = Σ_λ coeff_{λ+ρ}(Δ * p_μ) • D_λ`

This requires:
1. `coeff_rename`: coefficient-level interaction between `rename σ` and `coeff m`
2. Alternant coefficient identity: `coeff_{μ+ρ}(D_λ) = δ_{λ,μ}`
3. Proof that antisymmetric polynomials are determined by their leading (strictly decreasing) coefficients

A detailed proof sketch is documented in the file comments.

## Overall project progress

Proposition5_21_1 is partially proven — the proof structure is complete with one sorry for the antisymmetric basis decomposition. All infrastructure (Fintype, DecidableEq, antisymmetry lemma, Vandermonde nonzero) is in place.

## Next step

Prove the antisymmetric basis decomposition: for an antisymmetric polynomial F, show `F = Σ_λ coeff_{λ+ρ}(F) • D_λ`. The proof sketch in the file gives a complete argument using `MvPolynomial.ext` and the rename-coeff relationship.

## Blockers

None — the remaining work is self-contained.
