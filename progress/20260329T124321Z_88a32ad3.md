## Accomplished

### Infrastructure for CoxeterInfrastructure type-changing reflection functors (issue #1936)

Added `[∀ (a b : Fin n), Subsingleton (@Quiver.Hom (Fin n) Q a b)]` hypothesis
to the lemma chain: `one_round_or_simpleRoot`, `indecomposable_reduces_to_simpleRoot`,
`indecomposable_bilinearForm_eq_two`, and downstream in `Corollary6_8_3.lean`.

Wrote infrastructure lemma **statements** (proofs sorry'd):
- `subsingleton_hom_reversedAtVertex`: Subsingleton preserved through reversedAtVertex
- `subsingleton_hom_iteratedReversed`: Subsingleton preserved through iteration
- `fintypeHomOfSubsingleton`: Fintype derivation from Subsingleton + Classical
- `fintypeArrowsIntoOfSubsingleton`: Fintype(ArrowsInto) from Subsingleton (needed by Prop 6.6.8)
- `reflFunctorPlus_free_ne/eq`: Module.Free preserved by F⁺
- `reflFunctorPlus_finite_ne/eq`: Module.Finite preserved by F⁺
- `simpleReflectionDimVector_eq_simpleReflection`: bridge from ArrowsInto-indexed sum to Cartan matrix
- `iteratedReversed_hom_from_nonmem`: symmetric edge companion lemma

## Current frontier

The main `one_round_or_simpleRoot` proof remains sorry'd. The infrastructure sub-lemma
proofs are also sorry'd (8 total sorrys, up from 1 on main).

## Overall project progress

- CoxeterInfrastructure.lean: 8 sorrys (was 1, but decomposed into structured sub-problems)
- Corollary6_8_3.lean: compiles with Subsingleton hypothesis
- Both files build successfully

## Next step

1. **Fill infrastructure sorrys**: The sub-lemma proofs are straightforward but hit
   Lean type-theory issues (dependent type rewriting for `reflFunctorPlus_obj_ne/eq`,
   `ReversedAtVertexHom` unfolding for Subsingleton). Each needs interactive debugging.
2. **Universe blocker**: The main induction in `one_round_or_simpleRoot` cannot proceed
   until `QuiverRepresentation`'s 4 universe params are handled. Options:
   - Specialize to a fixed universe (e.g., Type 0 obj types)
   - Use a Prop wrapper like `ExistsIndecompOnReversed` but resolve the elaboration issues
   - Restructure the induction to avoid quantifying over representations across steps

## Blockers

**Lean 4 universe constraint**: `QuiverRepresentation.{u_1, u_2, u_3, u_4}` has 4 universe
params. When the induction state contains `∃ ρ : QuiverRepresentation k (Fin n)` on
`iteratedReversedAtVertices Q (σ.take m)`, Lean cannot unify `u_3` (obj universe) across
steps because the quiver instance changes type. A `Prop` wrapper was attempted but hit
the same issue when destructuring. This is the fundamental blocker for the full proof.
