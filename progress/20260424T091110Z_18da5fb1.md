## Accomplished

Review session on issue #2547 — audit of the Schur-Weyl equivariance layer
landed in PRs #2538 (bridge equivariance) and #2539 (embedding equivariance).

Produced `progress/reviews/2026-04-24-schur-weyl-equivariance.md` covering
all six focus areas R1-R6 from the issue. **All six passed.** No correctness
defects found, no follow-up issues filed.

Audit highlights:
- **R1 (statement correctness)**: traced `polyToTensor (polyRightTransl g
  (∏_l X(f l)))` and `tgtGLAction g (polyToTensor (...))` at the monomial
  level; both expand to `∑_c (∏_l g_{c l, (f l).2}) • symTensor(...)` with
  the same index structure. `polyRightTransl` and `tgtGLAction` both
  compose in the same direction (`(gh) = g ∘ h`). No contravariant flip.
- **R2 (`hP_mul` semantic load)**: verified that `eval_h ∘ polyRightTransl g
  = eval_{h·g}` on generators, so the evaluation-level identity follows
  from `ρ.map_mul + hP`. Polynomial-level lift requires Zariski density of
  `{det ≠ 0}` in `k^{N²}`, which `[CharZero k]` gives. Scope of #2545
  (the follow-up deriving `hP_mul`) correctly matches.
- **R3 (simp/ext proofs)**: `splitDualBasis_tgtGLAction`'s `suffices`
  *strengthens* the claim to a universal LinearMap form, then recovers
  the original via `congrArg (·z)`. No silent weakening. Simp set is
  tight.
- **R4 (reindex choice)**: `funCongrLeft` vs `piCongrLeft` is an API
  choice for unfolding ease — both are reindexings that commute
  pointwise with `PiTensorProduct.map g.toLin'`.
- **R5 (unused hyps)**: two minor observations — `_halg` is unused
  (documentary) and `set ... with hegd`/`hevalg` bindings are unused.
  Non-blocking, too small to file issues for.
- **R6 (downstream usability)**: signature matches #2482 step 1 exactly;
  expected caveat is discharging `hP_mul` at the call site (via #2545).

Posted findings as a consolidated comment on #2547
(https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/issues/2547#issuecomment-4311983292)
and wrote the full review doc to `progress/reviews/2026-04-24-schur-weyl-equivariance.md`.

Both files under audit (`PolynomialTensorBridge.lean` at 677 lines and
`PolynomialRepEmbedding.lean` at 571 lines) carry zero sorries and
remain well under the 1000-line hard cap. No Lean code changes in
this review.

## Current frontier

`polynomialRep_embeds_in_tensorPower` (embedding + equivariance) is
audited and cleared for downstream consumption. #2482 (Schur-Weyl #5)
can now cite it, pending discharge of the `hP_mul` hypothesis — which
is the explicit target of follow-up issue #2545.

## Overall project progress

Stage 3 formalization — Schur-Weyl chain. Schur-Weyl #2a + #2b are now
fully landed and independently audited. Next milestones are:
- #2545 — derive `hP_mul` from `ρ.map_mul + [CharZero k]`, shared lemma.
- #2482 — Schur-Weyl #5: polynomial GL_N-rep decomposes as direct sum
  of Schur modules.
- #2483 — Schur-Weyl #6: close the final sorry in
  `iso_of_formalCharacter_eq_schurPoly`.

Queue state after this session: one remaining unclaimed issue (#2546,
wave-56 sorry landscape summary), no active `feature` issues.

## Next step

Dispatch `/summarize` on #2546 to generate `progress/sorry-landscape.md`
(wave-56). After that completes, the planner's next cycle should file
the next round of Wall 3 sub-issues (C.1.c if the pigeonhole #2543 lands)
and any post-equivariance Schur-Weyl successors.

## Blockers

None.
