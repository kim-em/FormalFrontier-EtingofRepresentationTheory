## Accomplished

- **Proved connectivity preservation** (InfiniteTypeConstructions.lean, formerly line 3041): removing a degree-1 vertex (leaf) from a connected acyclic graph preserves connectivity. Used `SimpleGraph.Connected.induce_compl_singleton_of_degree_eq_one` from Mathlib, following the same pattern as DynkinForward.lean.

- **Proved 2-regular connected graph has a cycle** (InfiniteTypeConstructions.lean, formerly line 3197): a connected finite 2-regular graph satisfying the acyclicity condition leads to contradiction. Built a SimpleGraph, showed it would be a tree (Connected + IsAcyclic from our list-based acyclicity), then used edge counting: tree has n-1 edges but handshaking gives n edges. Key insight: used `IsCycle.getVert_injOn'` and `List.ofFn` for clean cycle list construction to convert between Walk-based and List-based cycle formats.

Both sorries were sub-goals of `acyclic_path_posdef` / `acyclic_deg_le_2_posdef`, which are used in the critical path to Gabriel's theorem.

## Current frontier

Both deliverables from issue #2301 are complete. The file builds successfully (only pre-existing errors remain).

## Overall project progress

- **Sorries removed this session**: 2 (from InfiniteTypeConstructions.lean)
- **Remaining sorries in file**: 6 (issues #2300, #2302, #2303 and others)
- **CI**: Build passes for this file (pre-existing error at line 3742 is issue #2300)

## Next step

1. Claim and work on #2300 (graph_with_list_cycle_infinite_type) - difficulty 6, critical path
2. Or #2302 (not_posdef_single_branch_infinite_type) - difficulty 7, critical path
3. Both block `not_posdef_infinite_type` which blocks Gabriel's theorem

## Blockers

None.
