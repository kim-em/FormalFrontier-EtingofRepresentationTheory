## Accomplished

Proved `Proposition5_21_2_dimension` — the Schur polynomial dimension formula:
S_λ(1,...,1) = ∏_{i<j}(λᵢ - λⱼ + j - i) / (j - i).

The proof strategy:
1. Lift the fundamental identity `schurPoly * vandermondeDet = shiftedDet` to `Polynomial ℚ` via the geometric embedding `X_i ↦ X^i`.
2. Factor `(1-z)` from each Vandermonde difference using `geom_sum_mul_neg`.
3. Cancel `(1-z)^M` using `Polynomial.eq_zero_of_infinite_isRoot` (polynomial extensionality on the integral domain `Polynomial ℚ`).
4. Evaluate at `z = 1` and compute the products.

Added helper lemmas:
- `pow_sub_eq_mul_geom_sum`: factorization z^a - z^b = (1-z)(z^a · geom_sum)
- `nested_prod_factor_const`: factor constant out of nested products
- `geomEmbed` / `geomEmbed_eval`: the embedding MvPolynomial → Polynomial and its evaluation property

Key challenges solved:
- Fin N vs ℕ elaboration issues with `Finset.mem_Ioi.mp` (type annotations force ℕ elaboration, removed them)
- Polynomial eval of products/sums (using `Polynomial.eval_prod`, `eval_mul`, `eval_pow`, `eval_finset_sum`)
- Cancellation of `(1-z)^M` via `mul_left_cancel₀` with `linear_combination` to rearrange
- ℕ cast subtraction (`Nat.cast_sub`) with explicit proofs of inequality

## Current frontier

Proposition5_21_2 complete. Both geometric and dimension formulas proved.

## Overall project progress

Chapter 5 Schur polynomial propositions formalized (modulo sorry'd Schur polynomial definition and fundamental identity in Proposition5_21_1).

## Next step

Pick up next unclaimed issue from the queue.

## Blockers

None.
