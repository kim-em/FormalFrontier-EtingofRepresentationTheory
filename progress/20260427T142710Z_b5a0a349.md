## Accomplished

Review session `b5a0a349` — completed audit of Schur-Weyl #2a
foundations (PR #2511) per issue #2561.

- Claimed #2561 (review label) — the most important of four unclaimed
  issues, on the critical path. Other three were lower-priority
  follow-ups (#2559 cleanup, #2564 explicitly low-priority Mathlib
  upstream tracker, #2565 explicitly non-blocking hygiene).
- Audited all 8 question categories Q1-Q8 against the +206 lines of
  un-audited foundation infrastructure on
  `Chapter5/PolynomialTensorBridge.lean:290-435`:
  - Q1 (`polyRightTransl` def): correct — generators map to
    `(X · g)_{ij} = ∑_l X_{i,l} · g_{l,j}`. Doc-string story matches.
  - Q2 (`tgtGLAction` def): correct — `g^⊗n ⊗ id` action with
    `g.toLin'` column-vector convention.
  - Q3 (`toLin'_stdBasis`): correct — pure linear algebra, no
    characteristic dependence.
  - Q4 (`tgtGLAction_seqTensor`): correct — proof chain
    `TensorProduct.map_tmul → PiTensorProduct.map_tprod → simp_rw
    [toLin'_stdBasis] → MultilinearMap.map_sum → map_smul_univ →
    sum_tmul → smul_tmul'` produces exactly the displayed expansion.
  - Q5 (`tgtGLAction_symTensor`): correct — bijection
    `Equiv.arrowCongr τ refl : b ↦ b ∘ τ.symm` correctly relates the
    per-τ b-sum on LHS to the c-sum on RHS, with `(n!)⁻¹` preserved
    verbatim on both sides via `smul_comm + ← Finset.smul_sum`.
  - Q6 (polynomial helpers): `prod_X_eq_monomial_fn` (Finset
    induction), `polyRightTransl_X` (one-line `aeval_X`),
    `polyRightTransl_prod` (`Finset.prod_univ_sum +
    Fintype.piFinset_univ + ring`) all check.
  - Q7 (downstream usability): no spurious instance, no
    transpose/dual fix-up. Audited consumers (#2549 / #2554) compose
    cleanly.
  - Q8 (doc-strings): all eight artifacts' doc-strings honestly
    describe what they compute.
- Filed F1 (hygiene): the entire `## GL_N-equivariance` block at
  lines 290-435 unnecessarily inherits `[CharZero k]` via section
  auto-binding from line 167, even though none of the proofs actually
  use the instance. The pattern `omit [CharZero k] in` already
  appears at lines 187 and 594 in the same file, so the author was
  aware. Filed as **#2568** (extends the existing #2565 cleanup to
  the rest of the block).
- Wrote audit report
  `progress/reviews/2026-04-28-schur-weyl-2a-foundations.md` and
  posted consolidated review comment on #2561.

## Current frontier

- Schur-Weyl chain: with this audit closed, the Schur-Weyl #2a
  foundation tier (PR #2511) is **PASS-with-one-hygiene-followup**.
  Combined with previously audited:
  - #2549 — equivariance statements (PRs #2538 / #2539): PASS
  - #2554 — plumbing layer (PRs #2528 / #2551): PASS
  - #2555 — character-additivity (PRs #2516 / #2534): PASS
  - #2557, #2560 — still in-flight (un-audited L_i FDRep GL_N
    structure + post-foundation plumbing #2509)
  the entire **post-foundation Schur-Weyl plumbing** + #2a
  **foundations** are end-to-end audited as of this session.
- Remaining un-audited: #2504 (L_i FDRep GL_N structure, in #2560)
  and #2509 (covered by #2557).
- Hygiene cleanups in flight: #2559 (formalCharacter_coeff CharZero),
  #2565 (polyRightTransl_X CharZero downstream), #2568 (rest of
  GL_N-equivariance block CharZero).

## Overall project progress

Stage 3 formalization. 8 sorries across 4 files (per most recent
landscape, `progress/sorry-landscape.md`). 582/583 items sorry-free
(99.8%). Schur-Weyl chain audit coverage: foundations + plumbing +
character-additivity all audited PASS as of this session. Two more
Schur-Weyl reviews (#2557 + #2560) remain in flight.

## Next step

- Workers: claim #2557, #2560 (other in-flight reviews) or any of
  the three hygiene cleanups (#2559, #2565, #2568) — all are
  independent and non-blocking.
- Workers continue with #2540 (Schur-Weyl #5 implementation, blocked
  cascade head).
- Repair flow handles any failing PRs.
- Next planner: re-triage if #2540 advances or another foundation PR
  needs auditing. PR #2542 (character identity skeleton) and PRs
  #2503 / #2505 (Wall 3 foundations) remain candidates for future
  audit cycles.

## Blockers

None. The hygiene finding F1 is non-blocking; current consumers all
carry `[CharZero k]` legitimately.
