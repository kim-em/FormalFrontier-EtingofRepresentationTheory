## Accomplished

### Specht module FDRep infrastructure (issue #1580)

Proved three key lemmas bridging Specht module algebra-level properties to the categorical FDRep level, needed for the Frobenius character formula:

1. **`spechtModuleFDRep_simple`** (fully proved): Bridges `IsSimpleModule (SymGroupAlgebra n) (SpechtModule n la)` to `CategoryTheory.Simple (spechtModuleFDRep n la)`. Uses `Simple.of_full_faithful_preservesMono'` via `forget₂ FDRep Rep`.

2. **`spechtModuleFDRep_noniso`** (fully proved): Lifts `Theorem5_12_2_distinct` (algebra-level non-isomorphism) to categorical non-isomorphism. Key technique: `FDRep.isoToLinearEquiv` gives a ℂ-linear equiv whose underlying types are definitionally `SpechtModule`. G-equivariance (via `FDRep.Iso.conj_ρ`) lifts to `SymGroupAlgebra`-equivariance by `MonoidAlgebra.induction_on`, contradicting distinctness.

3. **`spechtCharacter_orthonormal`** (fully proved): Character inner product `⅟|S_n| · ∑_σ χ_ν(σ) · χ_μ(σ⁻¹) = δ_{νμ}`. Combines `FDRep.char_orthonormal` with the above two lemmas.

### Key technical insight

The coercion bridge between FDRep categorical types (`↑(FDRep.of ρ).V`) and algebraic types (`SpechtModule n la`) is resolved by:
- Using `set e := FDRep.isoToLinearEquiv iso` to create a local definition
- Using `change` to retype `e` as `SpechtModule ≃ₗ[ℂ] SpechtModule` (definitionally equal)
- Using `show` to control goal forms in MonoidAlgebra induction cases

## Current frontier

- `alternatingKostka_norm_sq_eq_one` (line ~2338): The LAST remaining sorry in the Frobenius character formula chain. Requires Vandermonde coefficient orthogonality — a deep polynomial identity not yet in the codebase.

## Overall project progress

- **Theorem5_15_1.lean**: 1 sorry remaining (`alternatingKostka_norm_sq_eq_one`)
- All Specht module FDRep infrastructure is complete (3 lemmas proved this turn)
- The sorry reduction: went from 2 sorry'd declarations to 1
- `spechtCharacter_orthonormal` is fully available for downstream use

## Next step

1. Prove `alternatingKostka_norm_sq_eq_one` — this requires either:
   - Vandermonde coefficient orthogonality (polynomial identity ∑_ν K_{νλ}² = 1 via orthonormal expansion)
   - Or an independent (non-circular) proof of the Frobenius formula
2. Both paths require substantial new polynomial infrastructure

## Blockers

- `alternatingKostka_norm_sq_eq_one` requires Vandermonde coefficient orthogonality, which is a deep polynomial identity. The character orthonormality infrastructure is now complete, but the final connection (expressing ∑ L² as a character inner product and evaluating it) needs the Vandermonde expansion.
