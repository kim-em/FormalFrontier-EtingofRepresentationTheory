## Accomplished

Gut the refuted `_isIndecomposable` sub-sorry scaffolding in
`EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`
per issue #2437 (wave-54 Wall 1 cleanup).

- **`etilde6v2Rep_isIndecomposable`** (4 sub-sorries → 1 outer sorry,
  ~260 lines removed).
- **`etilde7Rep_isIndecomposable`** (3 sub-sorries → 1 outer sorry,
  ~295 lines removed).
- **`t125Rep_isIndecomposable`** (3 sub-sorries → 1 outer sorry,
  ~272 lines removed).

Each replacement keeps the theorem signature (including the unused
`hm : 1 ≤ m` hypothesis and the `attribute [-instance]` pragma)
unchanged. A short in-body comment references the counter-example
analysis in `progress/indecomposability-framework-investigation.md`
and the wave-54 Wall 1 decision issue.

`dTildeRep_isIndecomposable` (line 2173) is **not** touched — that
sorry is a Lean-level blocker (Wall 2), not framework-refuted.

## Current frontier

- Chapter 6 `InfiniteTypeConstructions.lean` now has **4 sorries**
  (down from 11 — all four at the outer theorem level):
  `dTildeRep_isIndecomposable`, `etilde6v2Rep_isIndecomposable`,
  `etilde7Rep_isIndecomposable`, `t125Rep_isIndecomposable`.
- Project-wide sorry count drops from 14 → 7 (−7, as predicted by
  the issue).
- Downstream `_not_finite_type` theorems compile unchanged — the
  outer signatures are preserved, so the `_isIndecomposable`
  theorems remain usable (they just appeal to `sorry` until the
  framework is replaced).

## Overall project progress

- Stage 3 (formalization) ongoing. 281/285 Lean files sorry-free.
- This PR reduces the sorry noise in the Ẽ/T cluster from 10
  scaffold-level sub-sorries to 3 outer theorem-level sorries,
  accurately reflecting the framework-refuted state documented in
  wave-54.

## Next step

- `dTildeRep` refactor (issue #2439, Wall 2, Option a).
- Schur-Weyl / `FormalCharacterIso` scoping (issue #2440).
- Garnir straightening rewrite (issue #2438, Wall 3, Option 1).

All three are parallel-safe and independent of the `human-oversight`
framework decision on issue #2436 (Wall 1) — though the refuted
outer sorries in this PR will remain open until that decision is
taken.

## Blockers

Issue #2436 (Wall 1, `human-oversight`) still requires human input
on the Ẽ/T framework (Option A — Tits form, or Option B — stronger
explicit construction). This PR does not resolve that; it only
cleans up the refuted scaffolding so the sorry landscape matches
reality.
