## Accomplished
- Partial proof of Lemma 4.10.3 (generic determinant irreducibility)
- Full proof structure: induction on n, base case (1x1), inductive step via cofactor expansion + `irreducible_mul_X_add`
- Proved 6 helper lemmas sorry-free:
  - `submatrix_eq_rename`: submatrix det = rename of smaller det
  - `submatrix_vars_fst_ne_zero`: variables in submatrix dets have first component >= 1
  - `minor00_eq_rename`: specialization for column 0
  - `minor00_ne_zero`: the (0,0) minor is nonzero
  - `minor00_vars`: variable (0,0) not in (0,0) minor
  - `rest_vars`: variable (0,0) not in remaining cofactor terms
- Main theorem fully structured — depends on 2 sorry'd helpers

## Current frontier
Two sorry's remain in `EtingofRepresentationTheory/Chapter4/Lemma4_10_3.lean`:
1. `rename_irreducible`: MvPolynomial.rename with injective function preserves Irreducible. This is a true general fact missing from Mathlib. Proof strategy documented: use left inverse + degreeOf_mul_eq to show factors have vars in range f.
2. `minor00_isRelPrime` (partially proven — has `hf_irr` and `isRelPrime_iff_not_dvd` rewrite): needs showing M00 doesn't divide the rest. Strategy: evaluate X(0,1)→1, X(0,j)→0 for j>1 to get M00 | M01, then use variable (1,1) ∈ vars(M00) but (1,1) ∉ vars(M01).

## Overall project progress
Stage 3.2 ongoing. This PR adds a partial proof of Ch4 Lemma 4.10.3 with the full induction structure and most helper lemmas proved.

## Next step
Fill in the two remaining sorry's. The `rename_irreducible` lemma could also be contributed to Mathlib as it's a useful general fact. The `IsRelPrime` proof needs a specialization/evaluation argument.

## Blockers
- `rename_irreducible` is not in Mathlib — needs custom proof involving `degreeOf_mul_eq` and vars-in-range argument
- The `IsRelPrime` non-divisibility argument requires careful variable tracking
