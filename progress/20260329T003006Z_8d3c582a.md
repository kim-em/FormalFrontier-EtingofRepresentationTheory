## Accomplished

**Issue #1923**: Partial progress on trace formula infrastructure for Theorem5_22_1 Sorry 1.

### Infrastructure Added
- **`youngSymEndomorphism_normalized_isProj`**: Proved that `α⁻¹ • E` is an `IsProj` onto `SchurModuleSubmodule`, where E is the Young symmetrizer endomorphism and α is its idempotent scalar
- **`youngSymEndomorphism_normalized_isIdempotent`**: Derived idempotent property from the projection proof
- **`trace_normalized_youngSym_eq_finrank`**: Used `IsProj.trace` to connect trace of normalized Young symmetrizer to `finrank` of SchurModuleSubmodule

### Proof Structure
- Reduced `formalCharacter_schurModule_eq_sum_permTracePoly` to a clean ext + coefficient argument via `weight_trace_coefficient_identity`
- The main theorem now has a real proof (not sorry'd) that delegates to the helper lemma

### Remaining Sorry
- **`weight_trace_coefficient_identity`**: The weight-by-weight trace computation. Requires:
  1. Tensor power basis eigenvector computation (diagUnit action on basis elements)
  2. Weight space decomposition of tensor power
  3. Commutativity of E with diagonal torus (E preserves weight spaces)
  4. IsProj.trace on weight-μ component gives finrank of (L_λ)_μ
  5. Trace of σ on weight-μ basis = coeff_μ(permTracePoly)

## Current frontier

`weight_trace_coefficient_identity` in `Theorem5_22_1.lean` — needs tensor basis infrastructure connecting abstract `glWeightSpace` to concrete tensor weight decomposition.

## Overall project progress

- Sorry count in `Theorem5_22_1.lean`: 2 (unchanged count, but sorry 1 is now cleanly reduced to a single well-documented helper)
- 3 new proved helper lemmas added (projection, idempotent, trace)
- Main theorem `formalCharacter_schurModule_eq_sum_permTracePoly` has real proof structure

## Next step

Prove `weight_trace_coefficient_identity`. This is the core tensor-basis computation. Key approach:
1. Build or find the weight space decomposition of tensor power (`(Fin N)^⊗n = ⊕_μ W_μ` where `W_μ = span{e_f : weight(f)=μ}`)
2. Show `youngSymEndomorphism` preserves each `W_μ` (from commutativity with diagonal action)
3. Apply `IsProj.trace` on each weight component
4. Connect `permTracePoly` coefficients to trace of permutation representation on tensor basis

Alternative: use `MvPolynomial.funext` to reduce to evaluation at all points, then use existing `sum_youngSym_permTracePoly_eq_alpha_schurPoly` (but this is defined after sorry 1 in the file, so may need reordering).

## Blockers

None hard. The remaining work requires significant tensor basis infrastructure that may warrant a separate issue/session.
