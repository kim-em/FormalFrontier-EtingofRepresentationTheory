## Accomplished

Worked on issue #1120: Stage 3.2: Ch6 prove Theorem_Dynkin_classification (graph ↔ A/D/E type).

### Backward direction: standard types → IsDynkinDiagram

Proved 4/5 properties for A_n and D_n, and all 5 properties for E6/E7/E8:

**E6, E7, E8** — fully proved using `native_decide` for symmetry/diagonal/0-1 entries,
explicit tree paths for connectivity, and Cholesky SOS decompositions for positive definiteness.

**A_n** — proved:
- Symmetry (via `or_comm`)
- Zero diagonal
- 0-1 entries
- Connectivity (ascending/descending `List.ofFn` paths)
- Positive definiteness: infrastructure built (recursive `pathQF`, lower bound, zero criterion) but linkage lemma `pathQF_eq_dotProduct` sorry'd

**D_n** — proved:
- Symmetry
- Zero diagonal
- 0-1 entries
- Full connectivity including branch vertex routing through (n-3)
- Positive definiteness: sorry'd

### Infrastructure built

- `pathQF`: recursive quadratic form on `ℕ → ℤ` that avoids Fin casting
- `pathQF_lower`: lower bound `q(x) ≥ x₀² + x_{n-1}²` by induction
- `pathQF_le_zero_imp`: `q(x) ≤ 0 → x = 0` by induction
- `isDynkinDiagram_of_graph_iso`: graph isomorphism preserves IsDynkinDiagram

### Sorry inventory (3 total)

1. `pathQF_eq_dotProduct` — links recursive form to matrix dotProduct. Requires Finset sum
   decomposition over Fin n (Fin.sum_univ_succ + matrix entry properties). The mathematical
   identity is clear; the Lean formalization is blocked on Finset sum manipulation.
2. D_n positive definiteness — needs similar SOS/Cholesky argument as E-types but for general n.
   Could use `pathQF` infrastructure once linkage is proved, plus a leaf-addition lemma.
3. Forward direction (out of scope per issue #1120).

## Current frontier

File compiles with 0 errors, 3 sorry's. The key unproved piece is `pathQF_eq_dotProduct`
which would unlock A_n pos def (the pathQF_lower + pathQF_le_zero_imp proofs are complete).
D_n pos def could then follow from A_{n-2} + leaf-addition.

## Overall project progress

- 188/583 items sorry_free (32.2%)
- This PR adds substantial proofs for the Dynkin classification theorem backward direction
- E6/E7/E8 completely proved; A_n and D_n have 4/5 properties proved

## Next step

- Prove `pathQF_eq_dotProduct` using `Fin.sum_univ_succ` to decompose the dotProduct.
  This requires showing `C (succ i) (succ j) = C' i j` for the Cartan submatrix.
- Once A_n pos def is complete, D_n pos def follows from the path subgraph + leaf argument.

## Blockers

`pathQF_eq_dotProduct` requires Finset sum decomposition over Fin n which is technically
challenging in Lean 4. The mathematical content is straightforward (peeling off the first
row/column of a quadratic form). An agent with expertise in Finset/Fin manipulation could
resolve this.
