## Accomplished

Foundations for #2496 (Schur-Weyl #2a equivariance) landed in branch
`agent/8189c438`. New code in
`EtingofRepresentationTheory/Chapter5/PolynomialTensorBridge.lean`:

- `polyRightTransl g : MvPolynomial (Fin N × Fin N) k →ₐ[k] MvPolynomial _ k`
  — alg hom sending `X ij ↦ Σ_l X (ij.1, l) * C (g l ij.2)` (the
  right-translation action `(g · P)(X) = P(X · g)` on generators).
- `tgtGLAction g : PolyTensorTgt k N n →ₗ[k] PolyTensorTgt k N n` —
  `g^⊗n ⊗ id` action on `V^⊗n ⊗ (V^*)^⊗n` via
  `TensorProduct.map (PiTensorProduct.map (fun _ => g.toLin')) LinearMap.id`.
- `toLin'_stdBasis`: standard expansion `g.toLin' (e_j) = Σ_b g b j • e_b`.
- `tgtGLAction_seqTensor`: `tgtGLAction g (seqTensor f) = ∑ b, (∏_l g (b l) (f l).2) • seqTensor (((f l).1, b l))`
  via `PiTensorProduct.map_tprod`, `MultilinearMap.map_sum`, `map_smul_univ`,
  and `TensorProduct.sum_tmul`.
- `tgtGLAction_symTensor`: the **key bijection step**. For
  `tgtGLAction g (symTensor f) = ∑ c, (∏_l g (c l) (f l).2) • symTensor (((f l).1, c l))`,
  swap `c, τ` sums and apply `Equiv.arrowCongr τ (Equiv.refl _)` to match
  the inner sums (the `b ↔ c ∘ τ` substitution).
- `polyRightTransl_X`, `polyRightTransl_prod`: the polynomial-side
  expansion via `Finset.prod_univ_sum`.
- `prod_X_eq_monomial_fn`: `∏_l X (f l) = monomial (Σ_l Finsupp.single (f l) 1) 1`.

Branch builds clean (only pre-existing `[CharZero k]`-unused-section
warnings).

Decomposed via #2510: the remaining `polyToTensor_rightTransl` assembly
needs a multiset-invariance helper for `symTensor`, which in turn needs
an explicit `matchingPerm : (f g : Fin n → α) → multiset f = multiset g →
{σ // g = f ∘ σ}` construction. No Mathlib lemma exists for
`List.Perm (List.ofFn f) (List.ofFn g) → ∃ σ : Equiv.Perm (Fin n), g = f ∘ σ`;
the construction is straightforward (induction on n via `Fin.succAbove`)
but ~50–100 lines.

## Current frontier

Partial PR landing the foundations on a fresh branch off `main`. Sub-issue
#2510 has the full assembly plan with proof sketches.

## Overall project progress

Stage 4 (formalization) continuing on Wall 3 (Ch5). PR queue empty, two
unclaimed feature issues: #2498 (Wall 3 #2450 part B pigeonhole) and
the new #2510 from this decomposition. Three other feature issues
claimed and in flight: #2497, #2491, #2489.

## Next step

Pod will dispatch #2510 (or a planner cycle picks up the breadcrumb on
#2496 and either closes it as fully-decomposed or narrows to the
residual scope). The `matchingPerm` construction is the only
mathematically novel piece in #2510; the rest is mechanical given the
foundations landing in this PR.

## Blockers

None. The decomposition is clean; #2510 is fully self-contained with
proof sketches.
