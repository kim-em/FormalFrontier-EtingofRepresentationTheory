## Accomplished

- **Claimed issue #2271** (garnir_straightening_step — Garnir row inversion decrease)
- **Decomposed the sorry** in `garnir_straightening_step` into two focused sub-problems:
  1. `garnir_polytabloid_identity` (difficulty 5): algebraic identity from Garnir relation
  2. `garnir_twisted_in_lower_span` (difficulty 8): combinatorial row inversion decrease via dominance
- **New proven results**:
  - `exists_row_inversion_pair`: extract row inversion when rowInvCount' > 0
  - `twistedPolytabloid`: definition of column antisymmetrization with Garnir permutation w
  - `twistedPolytabloid_one`: f_1(σ) = ψ_σ (twisted polytabloid at identity = standard polytabloid)
- **Detailed proof strategy documentation** added in the section comment
- **PR #2304** created and auto-merge enabled

## Current frontier

Three sorries in SpechtModuleBasis.lean:
1. `garnir_polytabloid_identity` (line ~509): Purely algebraic — apply garnirAnnihilate_tabloid to each tabloid in ψ_σ, multiply by sign(q), sum over Q_λ, exchange sum order, extract w=1 term. Requires careful Finset.sum_comm and subtype splitting.
2. `garnir_twisted_in_lower_span` (line ~534): Hard combinatorial — show each non-identity Garnir permutation produces tabloids in strictly better dominance, giving fewer row inversions after column restandardization. Needs dominance theory (James Ch. 7-8).
3. `garnir_straightening_step` (line ~559): Combines (1) and (2) once a Garnir set is constructed for the row inversion. Needs `garnirSet` and `garnirSet_has_row_pair` from PolytabloidBasis.lean.

## Overall project progress

- **Items**: 581/583 sorry-free (99.7%)
- **Sorries**: 16 across files (was 14 before; +2 from decomposition of 1 → 3)
- **CI**: Healthy
- Key clusters: Ch2 Gabriel's theorem (2), Ch5 Garnir+Schur (5+), Ch6 graph classification (8)

## Next step

1. **Prove `garnir_polytabloid_identity`** — this is purely algebraic (sum manipulation with garnirAnnihilate_tabloid). Estimated 40-60 lines. The key steps are: apply Garnir to each q, sum with sign(q), exchange sums via Finset.sum_comm, split w=1 from w≠1.
2. **Prove `garnir_straightening_step`** from the sub-lemmas — construct the Garnir set using `garnirSet` for positions p₁ (row r, col j₁) and p₃ (row r+1, col j₁), then apply the two sub-lemmas.
3. **`garnir_twisted_in_lower_span`** is the hardest remaining piece and may need further decomposition into dominance sub-lemmas.

## Blockers

None blocking forward progress. The sorry decomposition provides clear sub-problems for future agents.
