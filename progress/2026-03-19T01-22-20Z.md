## Accomplished

- Claimed and completed issue #1127: Stage 3.2 Ch5 define schurPoly and prove alternant identity
- Defined `schurPoly` as the quotient of shifted alternant by Vandermonde determinant (no sorry)
- Proved `schurPoly_mul_vandermonde`: Schur poly * Vandermonde det = shifted alternant det (no sorry)
- Proved `vandermonde_dvd_alternant` via a chain of helper lemmas:
  - `X_sub_X_prime`: `X_i - X_j` is prime in `MvPolynomial (Fin N) Q` (via automorphism + `MulEquiv.prime_iff`)
  - `X_sub_X_not_associated`: distinct linear factors are non-associated (via evaluation at indicator functions)
  - `X_sub_X_isRelPrime`: distinct linear factors are coprime (from primality + non-association)
  - `prod_dvd_alternant`: product of all `(X_j - X_i)` for `i < j` divides any alternant det (via `Fintype.prod_dvd_of_isRelPrime`)
  - `alternant_det_associated_prod`: Vandermonde alternant is associated to the product (via `det_permute'` + `det_vandermonde` + column reversal by `Fin.revPerm`)
- Fixed pre-existing proof breakages in `X_zero_sub_dvd` and `X_sub_X_dvd` caused by Mathlib `rename` API change (now returns `AlgHom` instead of `AlgEquiv`)
- Sorry count: 6 -> 2 (remaining: `charValue` placeholder, `Proposition5_21_1` main theorem)
- Created PR #1148

## Current frontier

- `Proposition5_21_1.lean` now has complete Schur polynomial infrastructure
- The `charValue` definition still needs the Murnaghan-Nakayama rule or Young tableaux
- The main `Proposition5_21_1` theorem requires character theory not yet in Mathlib

## Overall project progress

- Stage 3.2 formalization continues across Chapter 5
- Multiple Schur polynomial helpers now proved from scratch (no axioms)

## Next step

- Work on next unclaimed issue from the queue

## Blockers

- None
