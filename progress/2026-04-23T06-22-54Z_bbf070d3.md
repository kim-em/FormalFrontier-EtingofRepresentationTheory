## Accomplished

- Closed the `k = 0` corner case of `garnir_straightening_step` in
  `EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean` (issue #2416).
- Added a companion lemma `rowInvCount'_swap_lt`, the row-analogue of
  `columnInvCount'_swap_lt`, reusing the injection-based counting argument.
- Derived inline from `k = 0` plus partition sortedness that
  `row(p₁) = 0` and both `col(p₁)`, `col(p₂)` are single cells, then
  used this to show every `q ∈ Q_λ` fixes `p₁` and `p₂`, hence commutes
  with `t = swap(p₁, p₂)`. Term-by-term tabloid equality then gives
  `ψ_σ = ψ_{t·σ}`, where `t·σ` is column-standard with strictly fewer
  row inversions.

## Current frontier

- `EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean` now has a
  single remaining `sorry`: `garnir_twisted_in_lower_span` at line 880
  (issue #2380), the combinatorial heart of Chapter 5's straightening
  theorem. The rest of the straightening machinery — including the `k = 0`
  case handled here — is fully proved.
- `lake build EtingofRepresentationTheory.Chapter5.SpechtModuleBasis`
  succeeds (12s).

## Overall project progress

- Chapter 5 Specht module basis: straightening theorem framework complete
  modulo the deep combinatorial sorry at `garnir_twisted_in_lower_span`
  (#2380).
- Unrelated work continues on Chapter 6 indecomposability in parallel
  (#2394, #2400, #2417-2420).

## Next step

- A worker claiming #2380 should focus on `garnir_twisted_in_lower_span`:
  column-restandardise `w·σ` for cross-column `w`, express `f_w(σ)` via
  the Q-equivariance identity `twistedPolytabloid_col_eq`, and use
  dominance theory to bound row inversions. PR for this issue will
  complete the full Chapter 5 straightening theorem.

## Blockers

None.
