## Accomplished

- Decomposed issue #1644 (Theorem5_25_2 parts 1-2, product 16) into 3 sub-issues:
  - #1645: `complementW_finrank` (dim W_μ = p^n) — claimed and worked on
  - #1646: `principalSeries_simple_of_ne` (V(χ₁,χ₂) irreducible when χ₁ ≠ χ₂)
  - #1647: `principalSeries_decomp` + `complementW_simple` (V(μ,μ) decomposition)

- Proved `mem_coset_of_cosetIndex`: every g ∈ GL₂(𝔽_q) uniquely decomposes as b * rep(i)
  for b ∈ Borel and i ∈ Option 𝔽_q (coset index). Key technique: explicit matrix construction
  b = [[det(g)/g₁₀, g₀₀], [0, g₁₀]] with entry-by-entry verification via `fin_cases; simp; field_simp; ring`.

- Proved `principalSeries_eval_injective`: evaluation at coset representatives determines
  covariant functions. Uses covariance f(bg) = χ(b)f(g) and the coset decomposition.

- Added 8 helper lemma declarations (with sorry) structuring the proof of `complementW_finrank`:
  - `complementW_eval_injective/surjective`: direct bijection between complementWSubmodule and 𝔽_q → ℂ
  - `principalSeriesSubmodule_finrank`: dim V(χ₁,χ₂) = p^n + 1
  - `detFun_mem_principalSeries`, `augmentation_detFun_ne_zero`
  - `ker_augOnPrincipalSeries_eq`, `augOnPrincipalSeries_surjective`

## Key patterns discovered

- `Matrix.GeneralLinearGroup.mkOfDetNeZero` has no auto-generated `_val` simp lemma;
  use `simp [mkOfDetNeZero, mk', unitOfDetInvertible]` to unfold coercion
- `IsUnit.ne_zero ((Units.isUnit g).map Matrix.detMonoidHom)` for nonzero determinant
- Matrix entry verification for GL₂: `fin_cases i <;> fin_cases j <;> simp [...] <;> (try ring) <;> (field_simp; ring)`

## Current frontier

- Issue #1645 partially done: coset decomposition and eval injectivity proved, 8 helper sorries remain
- The remaining helpers are:
  1. `complementW_eval_injective`: needs augmentation decomposition into cosets + χ(b)=μ(det b) cancellation
  2. `complementW_eval_surjective`: needs covariant extension with augmentation = 0 constraint
  3. `detFun_mem_principalSeries`: μ(det(bg)) = μ(b₀₀)μ(b₁₁)μ(det(g)), straightforward
  4. `augmentation_detFun_ne_zero`: aug(μ∘det) = |G| ≠ 0
  5. Others follow from these

## Overall project progress

- ~195/583 items sorry-free (~33%)
- Theorem5_25_2.lean: 6 original sorries, still 6 (but significant infrastructure added)
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free

## Next step

- Continue filling in `complementW_eval_injective` — the key step is showing
  aug(f) = f(rep(none)) · |B| when f(rep(some t)) = 0 for all t
  (because χ(b)·μ(det b)⁻¹ = 1 when χ₁=χ₂=μ)
- Then `complementW_eval_surjective` via constructing the covariant extension
- Finally connect to `complementW_finrank` via `LinearEquiv.ofBijective`

## Blockers

None. The remaining work is straightforward matrix arithmetic and coset sums.
