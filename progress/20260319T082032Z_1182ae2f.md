## Accomplished

Investigated issue #1078 (Proposition 6.6.7 sink indecomposability, final sorry). Attempted multiple approaches to prove `reflFunctorPlus_mapLinear_ne_ne` — the key helper lemma needed by the subrep conditions, complementarity, and transport lemma.

**Added infrastructure:**
- `decidable_eq_isFalse`: normalizes `Decidable p` to `.isFalse h` when `¬p` is known
- `decidable_eq_isTrue`: normalizes `Decidable p` to `.isTrue h` when `p` is known
- Documentation of the Decidable.casesOn opacity blocker

**Approaches attempted for `reflFunctorPlus_mapLinear_ne_ne`:**
1. `unfold` + term-mode `match` on DecidableEq instances — match resolves but does NOT substitute `inst a i` in the goal (motive inference produces non-dependent motive)
2. `generalize` + `cases` — fails with "result is not type correct" (dependent types)
3. `rw [h_da]` / `simp only [h_da]` — fails with "motive is not type correct" / no progress (dependent type rewriting)
4. `exact rfl` / `set_option maxRecDepth 1000 in exact rfl` — fails, types are not definitionally equal
5. `conv`-based selective rewriting — same dependent type constraints

**Fixed lint issues:**
- Line length violations in W₁_at/W₂_at definitions
- Added reason comment for `set_option maxHeartbeats`

## Current frontier

All remaining sorry's in the sink indecomposability proof (`reflFunctorPlus_mapLinear_ne_ne`, subrep conditions, transport at i) are blocked by the same fundamental issue: `reflectionFunctorPlus` is defined using `Decidable.casesOn`, and Lean's rewrite/simp machinery cannot handle dependent type rewriting through these opaque terms.

## Overall project progress

- Stage 3.2 proof filling active
- ~191/583 items sorry_free (~32.8%)
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- Ch5: bottleneck (65 sorries)
- Gabriel's theorem chain: Prop 6.6.7 blocked by architectural issue

## Next step

The proper fix requires **refactoring `reflectionFunctorPlus`** in `Definition6_6_3.lean` to avoid `Decidable.casesOn`. Options:
1. Use a structure with explicit vertex-indexed data (obj, maps) instead of case-splitting on DecidableEq
2. Define the representation using `dite` at the value level with proper `dif_neg`/`dif_pos` reduction lemmas
3. Use `Pi.single`-style construction that doesn't require type-level case analysis

A planner should create a new issue for this refactor, as it's a prerequisite for completing Prop 6.6.7 and likely other reflection functor proofs (Prop 6.6.6, source case).

## Blockers

**Architectural blocker**: `reflectionFunctorPlus` (Definition 6.6.3) uses `Decidable.casesOn` for type-level case splitting. This creates terms where `inst a i : Decidable (a = i)` cannot be rewritten by `rw`, `simp`, or `generalize` due to dependent type constraints. ALL remaining sorry's in Prop 6.6.7 sink case are blocked by this.
