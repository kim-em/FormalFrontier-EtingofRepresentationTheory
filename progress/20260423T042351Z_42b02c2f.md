## Accomplished

- Claimed issue #2394 (Ẽ₇ nilpotent propagation: prove hN₁, hN₂, hbot0)
- Performed deep mathematical analysis of the three sorries at lines 2770, 2773, 2807 in
  `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`
- Derived nontrivial intermediate constraints: for `x ∈ W₁(4)`, the decomposition of `(0, x)`
  at vertex 1 forces both components to have zero first block (via hom50 injectivity + IsCompl
  uniqueness), yielding `q_α, q_β ∈ ℂ^{m+1}` with `q_α + q_β = x`, `(0, q_α) ∈ W₁(1)`,
  `(0, q_β) ∈ W₂(1)`, `(q_α, q_α, Nq_α) ∈ W₁(5)`, `(q_β, q_β, Nq_β) ∈ W₂(5)`.
- Further derived `(ν_1, ν_2, Nq_α) ∈ W₁(5)` via vertex 6 decomposition of `(q_α, q_α)`.
- Identified that these constraints are **insufficient to close hN₁**: no evident path from
  "`(?, ?, Nq_α) ∈ W₁(5)`" to "`Nq_α ∈ W₁(7)`" without additional structural insight.
- Dimension analysis for hbot0: `block_A ⊕ arm1(V(1))` is dim `4(m+1) - 1`, short by the
  `ker(N)` direction. `W'(0) ⊇ block_A ⊕ T(W'(1))` also insufficient (≤ 3(m+1)-dim).
- Posted detailed analysis as a comment on #2394 and skipped the issue with `coordination skip`.

## Current frontier

- #2394 marked `replan`. The comment documents what was attempted, the constraints derived,
  and explicitly notes that `dTildeRep_isIndecomposable` (line 2173), `etilde6v2Rep_isIndecomposable`
  (1 sorry, PR #2411), and `t125Rep_isIndecomposable` (#2400) all share the **same** technical
  gap: propagating nilpotent invariance from an arm-coupled center to a non-adjacent leaf in
  extended Dynkin representations.

## Overall project progress

- Stage 3 formalization: ~582/583 items sorry-free; ~12 sorry sites remain across 4 files
  (InfiniteTypeConstructions.lean, plus Ch2/Ch5/Ch6 straightening/bridge sorries).
- **Structural blocker**: three extended Dynkin indecomposability theorems (D̃ₙ, Ẽ₆, Ẽ₇, and
  by extension T(1,2,5) for Ẽ₈) all depend on an unformalized mathematical technique
  ("arm-coupled nilpotent propagation").

## Next step

- **For a planner**: Consider whether the nilpotent-coupled indecomposability for extended
  Dynkin quivers needs a dedicated research-scale issue (not a standard feature issue). The
  three affected proofs (`dTildeRep_isIndecomposable`, `etilde6v2Rep_isIndecomposable`,
  `etilde7Rep_isIndecomposable`, and `t125Rep_isIndecomposable`) likely benefit from a
  unifying lemma proven once.
- **Alternative**: Continue working sorries elsewhere that don't depend on these (e.g.,
  Chapter 5 straightening, Chapter 2 forward bridge for non_posdef_not_HasFiniteRepresentationType).
- #2400 is currently unclaimed and has the identical mathematical structure — will almost
  certainly hit the same wall.

## Blockers

- **Mathematical**: Propagating `N`-invariance from arm 1's coupling through the center block
  structure to leaves 4/7 of Ẽ₇ (and analogously for Ẽ₆, Ẽ₈, D̃ₙ). The existing framework
  (`leaf_containment`, `compl_eq_of_le`, `nilpotent_invariant_compl_trivial`) captures most
  of the structure but the final step requires novel insight not in the current codebase.
