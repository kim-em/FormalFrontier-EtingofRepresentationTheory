## Accomplished

Closed issue #2497 — Wall 3 #2450 part A (column-restandardizer API with
sign tracking).

Added 4 new declarations to
`EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean` (lines
915–974, immediately before `garnir_twisted_in_lower_span`):

* `garnirColReindex σ w q` — `Classical.choose`-defined function from
  `exists_column_standard_mul (w * q⁻¹ * σ)`.
* `garnirColReindex_mem_columnSubgroup` — membership in `ColumnSubgroup n la`.
* `garnirColReindex_colStandard` — `isColumnStandard' n la (γ * (w * q⁻¹ * σ))`.
* `garnirColReindex_polytabloid_eq` — sign-tracking corollary
  `gPT(w q⁻¹ σ) = sign(γ) • gPT(γ · w q⁻¹ σ)`.

Sign-tracking proof reuses the existing pattern from
`tabloidProjection_spechtModule_le_span` (line 1648 of the original file):
rewrite `generalizedPolytabloidTab_col_mul` then collapse via
`smul_smul`/`hsign_sq`/`one_smul` using `sign(γ)² = 1`.

`lake build EtingofRepresentationTheory.Chapter5.SpechtModuleBasis`
succeeds with **zero new sorries**. The pre-existing sorry in this file
remains exactly one: `garnir_twisted_in_lower_span` (now at line 1013,
shifted from line 950 by the insertion).

## Current frontier

Wall 3 #2450 decomposition is now 1/4 complete:

* **Part A (#2497)** ✅ — column-restandardizer API (this PR).
* **Part B (#2498)** open — pigeonhole collapse lemma (math heart).
* **Part C (#2499)** unfiled — residual whole-sum grouping (depends on A + B).
* **Part D (#2500)** unfiled — assembly into `garnir_twisted_in_lower_span`
  (depends on A + B + C).

(Per #2497, parts C and D are mentioned but may be filed by the next
planner once parts A and B are closer to landing.)

## Overall project progress

Specht-module ≤-direction (`finrank_spechtModule_le_card_syt`): blocked
behind `garnir_twisted_in_lower_span` only. The straightening machinery
is otherwise complete; this single sorry is the last barrier between
the project and the full Specht dimension theorem along the dominance-
plus-row-inversion induction.

## Next step

Claim #2498 (pigeonhole collapse) — the math heart of the Wall 3
strategy. The whole-sum strategy was validated on the λ=(2,2),
σ=swap(0,1) counter-example in `progress/20260424T034536Z_2a4d9cc5.md`;
that progress file is the canonical anchor for Form 1 (direct: tabloid
constancy under Q_λ-conjugation) of the pigeonhole hypothesis.

Once parts A and B land, a planner cycle should file parts C and D.

## Blockers

None.
