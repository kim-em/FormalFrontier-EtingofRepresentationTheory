## Accomplished

- **Proved Example 3.5.6(1)**: `Etingof.radical_truncated_polynomial` — the Jacobson radical of k[x]/(x^n) equals Ideal.span {root}. Proof uses:
  - `nilradical_le_jacobson` for the ≥ direction (root is nilpotent)
  - `ker_isMaximal_of_surjective` + `ker_evalRingHom` + `ker_quotient_lift` for the ≤ direction (evaluation at 0 is surjective to k with kernel = Ideal.span {root})
- **Fixed Example 3.5.6(2) statement**: The original statement used `Ideal.comap` from a left ideal in the full matrix ring, which is incorrect for non-commutative rings (a left ideal generated by strictly upper triangular matrices in M_n(k) is NOT just the strictly upper triangular matrices). Replaced with a directly defined `strictlyUpperTriangularIdeal` within the subalgebra.
- **Proved `IsTwoSided` instance** for `strictlyUpperTriangularIdeal`
- Added `Mathlib.RingTheory.Polynomial.Ideal` and `Mathlib.RingTheory.KrullDimension.Basic` imports

## Current frontier

- Example 3.5.6(2) (`radical_upper_triangular`) has correct statement, correct definition, two-sided instance, but proof is sorry'd
- The proof requires non-commutative Jacobson radical machinery: diagonal extraction maps for ≤ direction, nilpotency + `mem_jacobson_iff` for ≥ direction. Detailed sketch in comments.

## Overall project progress

- Stage 3.2 (proof filling) in progress
- Chapter 3: Example 3.5.6(1) is now sorry_free. Example 3.5.6(2) remains sorry'd.
- Items status updated: Chapter3/Example3.5.6 → proof_formalized (part 1 done, part 2 statement fixed)

## Next step

- Prove Example 3.5.6(2): needs (a) nilpotency of strictly upper triangular matrices (M^n = 0 by row-shift argument), (b) diagonal extraction ring homomorphisms, (c) connecting via `Ideal.mem_jacobson_iff`. The non-commutative case means `nilradical_le_jacobson` cannot be used directly.

## Blockers

- None for forward progress. Part 2 proof is challenging but not blocked — it requires careful formalization of matrix nilpotency and the `mem_jacobson_iff` characterization for non-commutative rings.
