## Accomplished

Issue #1446: Stage 3.2 Ch5 prove Theorem5_15_1 rearrangement sorries (partial).

Proved sorry #1 (dominance/majorization) in `sorted_shifted_strict_dominates` and all 3 supporting helper lemmas:

1. **`sortedParts_take_sum_eq`** — Connects `p.sortedParts.take k . sum` to `∑_{i<k} p.toFinsupp i` via `list_sum_eq_fin_sum_getD` and inline `getD_take` reasoning.

2. **`finsuppToPartition_sort_perm`** (~60 lines) — Constructs a sorting permutation σ such that `v i = (finsuppToPartition v).toFinsupp (σ i)`. Uses `Equiv.ofFiberEquiv` via fiber cardinality matching (same multiset-counting argument as `coeff_symmetric_eq_coeff_partition`).

3. **`finsuppToPartition_toFinsupp_antitone`** — Shows `toFinsupp` of `finsuppToPartition` is antitone (decreasing), using `List.Pairwise.rel_get_of_le` on sorted parts.

The dominance proof chain uses the **rearrangement inequality** (`Monovary.sum_smul_comp_perm_le_sum_smul`): since `la + ρ` is strictly decreasing and `ρ` is the identity permutation of `{n-1,...,1,0}`, any other permutation π ≠ rev produces a vector with strictly smaller indicator-weighted partial sums.

## Current frontier

- **1 sorry remains** at line 2109: `alternating_kostka_eq_delta` for `la ≠ nu` case.
  - This requires `∑_π sign(π) · K_{sort(λ+ρ-e_π), ν} = 0` when `λ ≠ ν`
  - Analysis: This is equivalent to the full Frobenius character formula. It requires **character orthogonality** (`<θ_λ, θ_λ> = 1` forces all off-diagonal coefficients to vanish) or an equivalent deep result (Jacobi-Trudi identity, Cauchy identity)
  - Cannot be proved term-by-term (genuine alternating-sum cancellation needed)
  - Requires significant new infrastructure not present in the file

## Overall project progress

- Sorry count in `Theorem5_15_1.lean`: 2 → 1 (eliminated `sorted_shifted_strict_dominates` sorry and its helper `finsuppToPartition_sort_perm`)
- The dominance/majorization argument is now complete
- The main theorem `Theorem5_15_1` has a complete proof structure modulo `alternating_kostka_eq_delta` for `la ≠ nu`

## Next step

- Create a focused issue for the remaining sorry (`alternating_kostka_eq_delta` la ≠ ν), documenting the required infrastructure (character inner products / orthogonality)
- Consider alternative proof strategies: inner product argument `<θ_λ, θ_λ> = 1`, Jacobi-Trudi identity, or direct Vandermonde-based computation

## Blockers

- The remaining sorry requires character orthogonality infrastructure (inner products of class functions on S_n) which is not yet formalized in this project.
