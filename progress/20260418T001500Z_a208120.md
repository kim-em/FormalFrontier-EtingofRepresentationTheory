## Accomplished
- Filled 3 sorry sites in `leaf_case` of `non_adjacent_branches_infinite_type` (issue #2363):
  1. Walk length ≥ 2: proved from distinct endpoints (v₀ ≠ w) via case analysis on list length
  2. Nodup path length ≥ 3: if length = 2, v₀ and w would be adjacent, contradicting non-adjacency
  3. Full D̃ subgraph embedding: constructed φ : Fin (k+6) ↪ Fin n with:
     - Fixed k = chain.length - 2 (was incorrectly chain.length - 3 in scaffolding)
     - Injectivity via acyclicity-based distinctness proofs for all vertex pairs
     - Edge preservation for all D̃ edge types (leaves, spine, and symmetric)
     - Applied tree_embed_adj_eq + subgraph_infinite_type_transfer + dTilde_not_finite_type
- Created PR #2396 with auto-merge enabled

## Current frontier
- All leaf_case sorries filled; `non_adjacent_branches_infinite_type` now depends only on
  `walk_to_nodup_path` (line 7989) and `dTilde_nodup_path_between` (line 7967), both being
  worked on by issue #2384

## Overall project progress
- Stage 3 formalization: 582/583 items sorry-free (99.8%)
- ~12 sorry sites remaining across 4 files (down from ~14 before this session)
- The leaf_case elimination removes the last blocking dependency in the D̃ embedding path

## Next step
- Issues #2384 (walk_to_nodup_path + dTilde_nodup_path_between) will complete the path infrastructure
- After #2384 merges, the only remaining Ch6 sorries are: indecomposability (4), posdef (5),
  single_branch_leaf_case (2)
- Consider tackling the tree_two_leaf_posdef or single_branch_leaf_case sorries next

## Blockers
- None for this work item
- walk_to_nodup_path and dTilde_nodup_path_between remain sorry'd (issue #2384 in progress)
