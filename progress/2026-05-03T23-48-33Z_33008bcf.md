## Accomplished

Worked on issue #2644 (Schur-Weyl L_i C-4a-ii: abstract image of primitive
idempotent is simple over centralizer). Built infrastructure file
`EtingofRepresentationTheory/Chapter5/PrimitiveIdempotentSimplicity.lean`
containing all the helper lemmas needed for the main theorem, plus the
main theorem signature. The body of the main theorem is currently `sorry`;
a full WIP draft is preserved as a `/- ... -/` comment block inside the
file for the next worker.

Helpers (zero sorries, fully proved):
* `rank_one_idempotent_basis_vec` — extract `v₀` with `π v₀ = v₀` from
  rank-1 idempotent.
* `tensor_through_rank_one` — `(π ⊗ id) ξ = v₀ ⊗ l₀` for some `l₀`.
* `block_factorization_extended` — extends sub-α's per-pure-tensor block
  factorization k-linearly to whole `S_i ⊗ L_i`.
* `cval_commute_centralizer` — c ∈ A commutes with B = centralizer(A).
* `cBLinear` — c.val packaged as a B-linear endomorphism.
* `imageSubmoduleB` — image of c as a `Submodule ↥B E`.

Sorry count: project +1 (one new sorry in the main theorem body).

## Current frontier

`image_of_primitive_idempotent_isSimple_centralizer` body is `sorry`,
deferred to a follow-up issue. The proof outline is documented in detail
inline (8 numbered steps), and the WIP draft is preserved at the end of
the file for direct reuse.

The body is blocked on a typeclass-instance diamond: a local `haveI`
binding `centralizerModuleHom (k := k) (E := E) (A := A) (V := ↥(S iLam))`
to provide `Module ↥B (↥(S iLam) →ₗ[A] E)` is not def-equal to the form
returned by `isSimpleModule_homA_centralizer`, causing
`IsSimpleModule.congr` to fail with type mismatch. Removing the haveI
causes auto-synthesis of the same instance to time out at >4M heartbeats.

## Overall project progress

Stage 3.1 (formalization). Wall 3 + Schur-Weyl L_i decomposition
in active progress. This PR adds infrastructure for one more sub-piece of
C-4a (#2644). Remaining open feature issues at session end: 2644 (this,
partial), 2657 (sub-γ), 2667 (Algorithm A core), 2683 (β.2), 2684 (β.3),
2543, 2520, plus several Wall 3 / final assembly issues.

## Next step

A follow-up issue should resolve the body of
`image_of_primitive_idempotent_isSimple_centralizer`. Two refactor paths:

1. **Refactor `centralizerModuleHom` to a non-class `def`** returning a
   `Module ...` value, then `letI` it once at the top of the theorem.
   This breaks the instance-diamond by making the instance non-canonical.
2. **Refactor `isSimpleModule_homA_centralizer` to take the Module
   instance as an explicit hypothesis** (rather than inferring it),
   so the resulting `IsSimpleModule` instance matches the local `haveI`.

The WIP body in the file should compile under either refactor with
mostly-mechanical changes.

## Blockers

None at the project level. The follow-up issue is well-scoped (preserved
proof outline + WIP code block + concrete refactor suggestions).
