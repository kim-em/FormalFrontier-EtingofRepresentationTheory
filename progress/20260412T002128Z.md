## Accomplished

- **Threaded `[F.functor.Linear k]`** through `head_isomorphism`, `exists_surjection_with_trivial_kernel_head`, and `basic_morita_regular_module_iso` — necessary infrastructure for the k-linear adjunction argument
- **Added `Mathlib.CategoryTheory.Preadditive.Schur` import** for categorical Schur lemma
- **Partially formalized `h_finrank` proof** in `head_isomorphism`:
  - Semisimple decomposition via `IsSemisimpleModule.exists_linearEquiv_fin_dfinsupp`
  - Finrank transport through `LinearEquiv.restrictScalars k`
  - `Module.Finite k` instances for simple summands via `Module.Finite.of_injective`
- **Documented detailed proof strategy** for both remaining sorries with specific Mathlib API references

## Current frontier

`MoritaStructural.lean`: 2 sorries in `head_isomorphism` (lines 550, 564) — same count as before, but now with partial formalization and detailed documentation.

The two sorries share the same core blocker: proving each simple B₂-module appears with multiplicity exactly 1 in Pt/JP. This requires:
1. k-linear adjunction `homEquiv` (from `F.functor.Linear k` + `Equivalence.inverseLinear` + `comp_smul`)
2. Hom evaluation iso: `Hom_{B₁}(B₁, M) ≅_k M`
3. finrank of DFinsupp = sum of component finranks
4. Assembly of per-simple surjections (Krull-Schmidt or Wedderburn-Artin)

## Overall project progress

- **Wave 48**: 8 sorries / 6 files (unchanged; sorries in head_isomorphism were already counted)
- **Items**: 581/583 sorry-free (99.7%)
- **Files**: 275/281 sorry-free (97.9%)
- PR #2251 created with auto-merge enabled

## Next step

1. **Prove the k-linear adjunction**: Construct `LinearEquiv k` from `AddEquiv` + `F.functor.Linear k`. This is the key infrastructure enabling the multiplicity = 1 argument.
2. **Prove evaluation iso**: `finrank_k Hom_{B₁}(B₁, M) = finrank_k M` — standard via `LinearMap.toSpanSingleton` or `LinearMap.applyₗ`.
3. **Connect DFinsupp finrank to DirectSum**: Use `DFinsupp.linearEquivFunOnFintype` + `Module.finrank_pi_fintype`.
4. Once (1)-(3) are done, both sorries in head_isomorphism can be closed.

## Blockers

- The k-linear adjunction construction (item 1 above) requires manually building a `LinearEquiv` from the `AddEquiv` provided by `Adjunction.homAddEquiv`, using `F.inverse.Linear k` (from `Equivalence.inverseLinear`) and `comp_smul`. This is ~20-30 lines of new infrastructure not in Mathlib.
- The surjection assembly (item 4) requires either Krull-Schmidt for semisimple modules or the Wedderburn-Artin decomposition B₂/J₂ ≅ kⁿ for basic algebras. Neither is a single Mathlib lemma.
