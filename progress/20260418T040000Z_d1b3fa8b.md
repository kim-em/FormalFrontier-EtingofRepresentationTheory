## Accomplished
- Identified and fixed a soundness issue in `garnir_twisted_in_lower_span` (issue #2380):
  - The original statement lacked `w ∉ ColumnSubgroup n la` hypothesis
  - For w ∈ Q_λ, `twistedPolytabloid_col_eq` gives f_w(σ) = sign(w)·ψ_σ, making the original
    sorry unprovable (circular: needed ψ_σ ∈ lower span to prove ψ_σ ∈ lower span)
  - Added the missing hypothesis to make the statement correct
- Restructured `garnir_straightening_step` to handle Q_λ terms algebraically:
  - Split Garnir sum into column-preserving (Q_λ) and cross-column parts
  - For Q_λ terms: each sign(w)·f_w(σ) = ψ_σ (k copies total)
  - Rearranged: (1+k)·ψ_σ = -(non-Q_λ sum) ∈ lower span
  - Divided by (1+k) ≠ 0 (CharZero) to get ψ_σ ∈ lower span
  - Applied `garnir_twisted_in_lower_span` only to w ∉ Q_λ terms
- Also analyzed issue #2400 (T(1,2,5) indecomposability) — found remaining 3 sorries are
  the hardest sub-goals (nilpotent propagation), equivalent in difficulty to Ẽ₇. Skipped.
- Created PR #2412 with auto-merge enabled

## Current frontier
- `garnir_twisted_in_lower_span` still has 1 sorry, but the statement is now correct
- The remaining proof needs dominance theory for cross-column Garnir permutations
  (James Ch. 7-8 / Fulton Ch. 7), rated difficulty 8

## Overall project progress
- Stage 3 formalization: 582/583 items sorry-free (99.8%)
- ~13 sorry sites remaining across 4 files (same count, but one sorry now has correct statement)
- The Ch5 straightening sorry is unblocked by PR #2390 (Garnir set fix) and this PR (statement fix)

## Next step
- Prove `garnir_twisted_in_lower_span` for w ∉ Q_λ: needs column restandardization of wσ
  and dominance argument showing rowInvCount' strictly decreases for cross-column permutations
- Key infrastructure needed: `exists_column_standard_mul` (already proved at line 269),
  `columnInvCount'_swap_lt` (already proved at line 69), dominance ordering on tabloids

## Blockers
- None for the statement fix (PR #2412)
- The remaining proof of `garnir_twisted_in_lower_span` requires significant combinatorial
  infrastructure (dominance theory) that may not yet exist in the codebase
