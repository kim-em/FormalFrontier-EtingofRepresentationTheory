## Accomplished

Feature session `4cdb1d95` — claimed issue #2583 (Schur-Weyl L_i part
C-4: irreducibility of `SchurModule k N λ` as `GL_N`-rep) and
**decomposed it into three self-contained sub-issues** before
releasing the claim. No code changes.

Decomposition rationale: the proof outlined in the issue body requires
three independent pieces of new infrastructure, each non-trivial and
unsuitable for sharing a single session:

1. **#2610 — sub-issue C-4a (algebraic core)**: image of the Young
   symmetrizer `c_λ` on `V^⊗n` is a simple
   `B = centralizer(symGroupImage)`-module. Requires bridging the
   bimodule decomposition (`Theorem5_18_4_bimodule_decomposition_explicit`)
   to the idempotent-image picture. Hardest of the three; may itself
   need further decomposition (e.g. (i) `c_λ` acts as `α` on `S_λ`
   and 0 on other Specht modules, (ii) abstract idempotent-image
   simplicity).

2. **#2611 — sub-issue C-4b (GL_N transfer)**: simplicity transfers
   from `B = diagonalActionImage`-module to the corresponding GL_N
   rep via Zariski density / "every matrix is a k-linear combination
   of invertibles". Self-contained, standalone, also unblocks #2582
   (sibling issue C-3).

3. **#2612 — sub-issue C-4c (final assembly)**: combine #2610 + #2611
   to derive `schurModule_isSimple`. Pure plumbing; blocked on the
   other two.

Breadcrumb left on parent #2583 with `Decomposed into #2610, #2611,
#2612` exactly as `pr-repair-flow`/`agent-worker-flow` prescribes;
parent is now in `replan` state for planner triage.

## Current frontier

- Sub-issues #2610 and #2611 are unclaimed and unblocked. Workers
  can claim either independently.
- Sub-issue #2612 is `blocked` on both #2610 and #2611.
- Parent #2583 is `replan` awaiting planner closure.
- Sibling #2582 (still `claimed`) shares the GL_N-transfer step with
  #2611 — coordinate to avoid duplicate work.

## Overall project progress

Stage 3 formalization. No sorry-count change this session. Schur-Weyl
L_i critical-path status (issue #2493 final assembly):

- ✅ #2580 (formalCharacter of `glTensorRep`)
- ✅ #2581 (polynomial identity `(∑Xᵢ)ⁿ = ∑ χ_λ(1)·s_λ`)
- 🔄 #2582 (irreducibility of L_i — claimed, in flight)
- 🆕 #2583 → decomposed into:
  - #2610 (algebra core), #2611 (GL_N transfer), #2612 (assembly)
- ⏳ #2493 (final assembly — blocked on the above)

Total open feature issues: 3 unclaimed, 1 claimed, 6 blocked
(unchanged net count — replaced #2583 with #2610 + #2611 + #2612).

## Next step

A worker should claim one of:
- **#2611 (GL_N transfer)**: tractable, self-contained, also unblocks
  #2582 if extracted as a shared lemma. Recommended first.
- **#2610 (algebraic core)**: harder; the issue body itself
  flags possible further decomposition. Worker should not hesitate to
  decompose again if needed.

#2612 is blocked until both #2610 and #2611 land.

## Blockers

None.

## Notes on infrastructure

The `MvPolynomial.eq_of_eval_eq_on_gl` lemma at
`Chapter5/PolynomialRepEmbedding.lean:602` provides the polynomial-
density tool that #2611 likely needs (a polynomial vanishing on GL_N
vanishes everywhere). This was already noted in #2564 (upstream
tracker) — a worker on #2611 should reuse it rather than re-deriving.
