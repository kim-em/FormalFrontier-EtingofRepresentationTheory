## Accomplished

Structural progress on Theorem 9.2.1(ii) — the decomposition A ≅ ⊕ᵢ (dim Mᵢ) Pᵢ.

1. **Helper lemma `exists_complete_orthogonal_idempotents_for_simples`**: New lemma (lines 891–1194) that encapsulates the Wedderburn-Artin construction of complete orthogonal idempotents indexed by `Σ i, Fin (finrank k (M i))` with the Hom delta property. Contains full infrastructure:
   - WA decomposition setup (Phase 1)
   - Block-module correspondence: `σ : ι → Fin n` injective, with `hc_identity` (block acts as id on corresponding simple) and `hc_zero` (other blocks act as 0) (Phase 2)
   - σ injectivity via `IsSimpleRing.nonempty_linearEquiv_of_isSimpleModule` with full matAct Module construction (Phase 2)
   - Idempotent lifting via `Etingof.lift_complete_orthogonal_idempotents` (Phase 6)

2. **Theorem_9_2_1_ii sorry eliminated**: The main theorem's sorry replaced by `exact Theorem921.exists_complete_orthogonal_idempotents_for_simples M hM hM_exhaustive`. The assembly proof (COI → decomposition A ≅ ⊕ dim(Mᵢ) Pᵢ via direct sum of left ideals, component-wise isomorphism to Pᵢ, sigma curry, and pi equivalence) was already complete.

3. **Build error fixed**: Fixed `rw [Ring.jacobson, Submodule.mem_sInf]` → cleaned up to sorry.

## Current frontier

4 sorry stubs remain in `exists_complete_orthogonal_idempotents_for_simples`:
- `hσ_surj` (σ surjectivity): requires showing ⋂ ann(Mⱼ) ≤ J, i.e., for each maximal ideal I, ∃ j with ann(Mⱼ) = I
- `hdim` (dimension match): d(σ i) = finrank k (M i), requires showing M_i ≅ k^{d(σ i)} as k-vector spaces via the Mat-module structure
- `hebar_coi` (CompleteOrthogonalIdempotents for ebar): requires σ surjectivity + dimension match
- `hrank` (rank property after lifting): requires all of the above

## Overall project progress

- Theorem9_2_1.lean: 4 sorries (all in helper lemma), down from 1 sorry in the main theorem but with better structure
- The assembly proof in Theorem_9_2_1_ii is fully complete
- items.json updated with detailed notes

## Next step

- Close `hσ_surj` and `hdim` (independent of each other), which unblocks `hebar_coi` and `hrank`
- For surjectivity: cleanest argument is cardinality (|ι| = n) or constructing a simple A-module from each WA block
- For dimension match: use Mat-module structure to show finrank k (M i) = d(σ i) via simple module uniqueness

## Blockers

The 4 sorries require substantial Lean formalization of deep algebra facts (unique simple module of Mat_n(k), Jacobson radical = ⋂ annihilators). Each is individually doable but time-intensive.
