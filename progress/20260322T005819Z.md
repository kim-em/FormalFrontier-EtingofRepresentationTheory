## Accomplished

Decomposed the single opaque `sorry` in `artin_forward` (Theorem 5.26.1 forward direction) into structured, well-typed helper lemmas:

### Helper lemmas created

1. **`frobenius_char_reciprocity`** — Frobenius reciprocity for character inner products:
   `∑_g f(g) · Ind_H^G(χ)(g⁻¹) = (|G|/|H|) · ∑_h f(↑h) · χ(h⁻¹)`
   - Main algebra proved (sum factoring, swapping, reindexing via bijection φ(k) = xk⁻¹x⁻¹)
   - 1 sorry remains: converting dite sum over G to subtype sum over ↥H + h→h⁻¹ reindex (Lean Fintype instance mismatch, mathematically trivial)

2. **`class_fun_vanishes_on_subgroup_of_orthogonal`** — If f|_H is orthogonal to all irreducible characters of H, then f vanishes on H. Needs Theorem 4.2.1 applied to H (character completeness).

3. **`covering_implies_vanishing`** — FULLY PROVED. If f vanishes on all H ∈ X and X covers G, then f = 0.

4. **`artin_Q_span_of_induced_chars`** — Integer rank preservation (Remark 5.26.2). When the orthogonal complement of induced characters is trivial, the ℚ-span contains all irreducible characters.

### `artin_forward` itself is sorry-free given the helpers

The proof follows the book: combines Frobenius reciprocity → character completeness → covering to show the orthogonal complement is trivial, then applies the ℚ-span argument.

PR #1569 (partial, issue #1556 marked for replan).

## Current frontier

- `Theorem5_26_1.lean` has 3 sorry's (down from 1, but decomposed into well-defined sub-problems)
- The sorry in `frobenius_char_reciprocity` is a Lean technicality (Fintype instance mismatch between `{x // x ∈ H}` and `↥H`)
- The sorry in `class_fun_vanishes_on_subgroup_of_orthogonal` requires reproducing the `classFunction_eq_zero_of_orthogonal_simples` argument (private in Theorem4_2_1.lean) applied to subgroup H
- The sorry in `artin_Q_span_of_induced_chars` is the deepest: needs integer rank preservation for ℕ-entry matrices

## Overall project progress

- ~195/583 items sorry-free (~33%)
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- Ch5: ~44 remaining sorries
- Ch6: 31 remaining sorries
- Ch9: 9 remaining sorries
- Ch2: 3 remaining sorries

## Next step

- The `frobenius_char_reciprocity` sorry is almost provable — just needs the dite-to-subtype conversion + inversion reindex. A future agent could finish it by finding the right Mathlib API for converting `∑ k : G, dite (k ∈ H) stuff 0` to `∑ h : ↥H, stuff h`.
- `class_fun_vanishes_on_subgroup_of_orthogonal` could be proved by making `classFunction_eq_zero_of_orthogonal_simples` public in Theorem4_2_1.lean, or reproducing the Wedderburn argument locally.
- `artin_Q_span_of_induced_chars` is the hardest and may need new linear algebra infrastructure.

## Blockers

None for this task.
