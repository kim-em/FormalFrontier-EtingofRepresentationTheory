## Accomplished

Claimed #2428 (Ẽ₇ `hbot0`), did a deeper gap analysis beyond the previous
session's (e902034b), and skipped with `replan` after confirming the same
structural obstruction.

Key new content in the analysis (posted as issue comment):

- **Identified that the closed `d5tildeRep_isIndecomposable` proof
  (line 1569) DOES use a center-propagation lemma** (`center_decomp` at
  line 1851) that fills `W'(center) = ⊤` by writing every center element
  as a sum of arm embeddings. This is EXACTLY the missing technique — but
  it only works when the arms span the center.

- **Precise dimension gap for Ẽ₇ V(0)**: `arm1(V(1)) + block_A` has
  dim `3(m+1)`, but `V(0)` has dim `4(m+1)`. The uncovered `(m+1)`
  dimensions live in `block_D`, specifically the direction orthogonal to
  `im(N)` within block D (which is `e_m = (0,…,0,1)` plus related
  structure). `arm1` only reaches block D via `Nq`, whose last coordinate
  is always 0.

- **Structural conclusion**: the d5tilde propagation technique does NOT
  transfer to Ẽ₇ / Ẽ₆ / T(1,2,5), because the arms don't span V(0).
  A different mechanism is needed to force `block_D ⊂ W'(0)` (or to
  propagate some W-constraint through a non-arm path).

## Current frontier

All 8 remaining indecomposability sub-sorries across Ẽ₆/Ẽ₇/T(1,2,5) are
blocked on variations of the same obstruction. The queue is effectively
empty for feature work:

- #2400 (T(1,2,5)) — same dimension gap at V(0), arm1 has block F = Nr
  with identical structural issue
- #2427 (Ẽ₇ hN₁/hN₂) — already in replan
- #2417–#2419 (Ẽ₆ analogs) — all in replan
- #2407 (D̃ₙ) — in replan
- #2380 (Garnir heart) — in replan, unrelated algebraic issue

## Overall project progress

- Stage 3 (formalization) ongoing. Ch6 indecomposability cluster (Ẽ₆,
  Ẽ₇, T(1,2,5), D̃ₙ) is structurally stuck on the arm-span gap at the
  central vertex.
- Closed indecomposability proofs that DO work: `cycleRep` (cycle, all
  arrows id+nilpotent) and `d5tildeRep` (D̃₅, two arms fully span center).
  Neither generalizes to the T-graphs with 3+ arms where a nontrivial
  nilpotent coupling is required.
- Ch5 Garnir straightening: PR #2426 in flight for the refactor.

## Next step

**Strongly recommend a meditate/planner session** to decide between:

1. **Introduce helper infrastructure** to bridge block_D: e.g., prove
   a lemma "`block_D ⊂ W'(0)` whenever `W(1)` is invariant under a lift
   of `N` to V(0)" — but this circular-depends on W(1) analysis.

2. **Follow the null-root / Gabriel argument** from the book more
   literally: show that the dimension vector `δ` is a real root (Tits
   form positive semidefinite with unique null vector), which forces
   indecomposability by general representation-theoretic means. This
   requires building Tits-form machinery not currently in the project.

3. **Restructure the quiver orientation**: the current formalization
   directs all arrows toward center. An orientation where some arrows
   point away from center might make the arm-span condition naturally
   satisfied (the `d5tilde` success uses a 2-arm configuration where
   both arms are essentially leaf→center).

The planner should close #2394 and similar parent issues and open either
a meditate work item, or create concrete sub-issues for one of the three
avenues above.

## Blockers

- **Technical**: Arms into Ẽ₇/Ẽ₆/T(1,2,5) center don't span V(center);
  `(m+1)` dimensions in block_D (orthogonal to `im(N)`) are unreached by
  any arm embedding.
- **Pipeline**: Queue is empty for feature work; all remaining sorries
  in the indecomposability cluster share the same obstruction.

## Session action

Skipped #2428 with `coordination skip` after detailed analysis. Did not
claim #2400 since it requires the same technique and the planner needs
to decide on a unified approach.
