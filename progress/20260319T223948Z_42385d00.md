## Accomplished

Worked on issue #1286 (Stage 3.2: Ch5 prove FRT base case + inductive step).

**Proved `card_standardYoungTableau_mul_zero`** (base case, line 166):
- Showed partition of 0 has empty cells, empty hook product = 1
- Constructed unique SYT element for empty partition (Cell type is empty)
- Used `Nat.Partition.partition_zero_parts` from Mathlib

**Decomposed `card_standardYoungTableau_mul_succ`** (inductive step) into focused sub-lemmas:
1. **Defined `Nat.Partition.removeOuterCorner`** — removes outer corner from partition of n+1, producing partition of n via YoungDiagram row lengths. `parts_sum` fully proved using new `YoungDiagram.rowLens_sum` helper.
2. **Stated `Nat.Partition.toYoungDiagram_removeOuterCorner`** (sorry'd) — round-trip: removed-corner partition's Young diagram equals direct corner removal
3. **Stated `syt_branching_rule`** (sorry'd) — |SYT(n+1,λ)| = Σ_c |SYT(n, λ\c)| over outer corners
4. **Stated `hook_corner_sum`** (sorry'd) — Σ_c |SYT(n, λ\c)| × hookProd(λ) = (n+1)!
5. **Proved `card_standardYoungTableau_mul_succ`** from branching rule + hook corner sum

The main theorem `card_standardYoungTableau_mul` compiles with the decomposed structure.

## Current frontier

3 remaining sorry'd sub-lemmas in FRTHelpers.lean:
- `toYoungDiagram_removeOuterCorner` (line 186): technical round-trip, needs multiset sort identity for already-sorted lists
- `syt_branching_rule` (line 229): needs branching bijection SYT(n+1,λ) ≃ Σ_c SYT(n, λ\c)
- `hook_corner_sum` (line 246): needs hook quotient identity Σ_c hookProd(λ)/hookProd(λ\c) = n+1

## Overall project progress

- ~194/583 items sorry-free (33.3%)
- Ch3, Ch4, Ch7, Ch8: 100% sorry-free
- New: FRT base case proved, inductive step decomposed into 3 focused sub-problems

## Next step

- Prove `toYoungDiagram_removeOuterCorner` (needs: Multiset.sort on already-sorted list coercion = identity, then ofRowLens_to_rowLens_eq_self)
- Prove `syt_branching_rule` (hardest: construct explicit bijection removing max entry from SYT)
- Prove `hook_corner_sum` (needs hook length recurrence under corner removal + summation identity)

## Blockers

- None — all sub-lemmas are well-defined and independently solvable
