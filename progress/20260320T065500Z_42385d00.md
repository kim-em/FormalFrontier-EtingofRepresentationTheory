## Accomplished

1. **Fixed merge conflicts in PR #1389** (issue #1390 closed)
2. **Proved `finsuppToPartition_toFinsupp`**: roundtrip lemma for partitions
3. **Structured `alternating_kostka_eq_delta` proof**: extracted `sorted_shifted_strict_dominates` helper, decomposed into dominance + inequality sub-goals
4. **Sorry count: 3 → 3** (same count but better structured: 2 in `sorted_shifted_strict_dominates`, 1 in la≠nu case)

## Detailed analysis of remaining sorries

### Sorry 1: Dominance (rearrangement inequality for partial sums)
Show `∀ k, (la.sortedParts.take k).sum ≤ (mu.sortedParts.take k).sum` where mu = sort(la+ρ-e_π). Requires rearrangement inequality infrastructure not in Mathlib/codebase.

### Sorry 2: Inequality (finsuppToPartition v ≠ la)
**Proved sketch**: If multisets match, then `∑ v_j² = ∑ la(j)²`. But `∑ v_j² - ∑ la(j)² = 2(∑ u_j ρ(j) - ∑ u_j e_π(j)) > 0` by rearrangement inequality (u strictly decreasing, ρ strictly decreasing, e_π ≠ ρ). Requires formalizing `∑ a_i b_{σ(i)} < ∑ a_i b_i` for strictly decreasing a, b and non-identity σ.

### Sorry 3: la ≠ nu case (cancellation)
`Σ_π sign(π) K(sort(la+ρ-e_π), ν) = 0`. Deepest sorry. Book uses upper-triangular Kostka matrix argument.

## Current frontier

All 3 sorries require the rearrangement inequality as a prerequisite (directly for #1-#2, indirectly for #3). A dedicated Lean formalization of the rearrangement inequality would unblock all of them.

## Overall project progress

- ~194/583 items sorry-free (33.3%)
- Theorem5_15_1.lean well-structured with clear sorry isolation

## Next step

- Formalize rearrangement inequality: `∑ a_i b_{σ(i)} ≤ ∑ a_i b_i` for sorted sequences, with strict inequality when a is strictly decreasing and σ ≠ id
- This single lemma would unblock sorries #1 and #2

## Blockers

Rearrangement inequality not in Mathlib or codebase.
