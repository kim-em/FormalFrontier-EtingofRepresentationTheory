## Accomplished

Worker session `5bc618ed` claimed and completed issue #2611
(Schur-Weyl L_i part C-4b: GL_N transfer of B-simplicity).

Created new file `EtingofRepresentationTheory/Chapter5/SchurWeylGLTransfer.lean`
(530 lines, zero sorries) containing the full chain from Vandermonde
inversion through the simplicity transfer:

1. **`submodule_coeffs_mem_of_eval_mem`** — Vandermonde inversion: for
   `n + 1` distinct evaluation points `c_j ∈ k`, if
   `∑ i, c_j^i • m_i ∈ W` for each `j`, then each `m_i ∈ W`.
2. **`mixedTensorPow`** + **`tensorPowCoeff`** — explicit "tensor
   coefficient" defined as `(-1)^i • ∑_{|s| = n - i} map(piecewise f 1)`,
   which is the coefficient of `c^i` in `(f - c•1)^⊗n`.
3. **`tensorPow_sub_smul_eq_sum_coeff`** — multilinear expansion:
   `(f - c•1)^⊗n = ∑_i c^i • A_i(f)` with `A_0(f) = f^⊗n`. Proven
   using `MultilinearMap.map_add_univ` + `map_smul_univ` and a
   reindexing bijection on `Finset.range (n + 1)`.
4. **`exists_finset_isUnit_sub_smul_one`** — for finite-dim `V`, the
   set of `c ∈ k` for which `f - c•1` is non-invertible is finite
   (it's a subset of `f.charpoly.roots.toFinset`).
5. **`tensorPow_mem_span_unitsTensorPow`** — `f^⊗n ∈ Submodule.span k
   (Set.range fun (g : Units (End k V)) => g^⊗n)` for every `f`.
   Proof: pick `n + 1` distinct cofinite `c_j` via `Set.Infinite.natEmbedding`,
   apply Vandermonde to `(f - c_j • 1)^⊗n` (each in span by isUnit), extract
   the constant coefficient `f^⊗n`.
6. **`adjoin_unitsTensorPow_eq_diagonalActionImage`** — algebra equality:
   `Algebra.adjoin k {g^⊗n : g ∈ Units(End k V)} = diagonalActionImage k V n`.
   Direct from (5) via `Algebra.span_le_adjoin`.
7. **`submodule_smul_mem_diagonalActionImage_of_unit_smul_mem`** —
   submodule transfer: a k-submodule of a B-module closed under
   `g^⊗n` (for every unit `g`) is closed under all of `B`. Proof:
   `Algebra.adjoin_induction` over `b ∈ B = adjoin k {f^⊗n}`, with
   the generator case dispatched via `Submodule.span_induction` over
   `f^⊗n ∈ span k {g^⊗n}` (from theorem 5).
8. **`submodule_eq_bot_or_top_of_unit_smul_mem`** — simplicity transfer:
   if `M` is `IsSimpleModule (diagonalActionImage k V n) M`, every
   k-submodule of `M` closed under units is `⊥` or `⊤`.

The submodule transfer (#7) requires `maxHeartbeats 800000 /
synthInstance.maxHeartbeats 400000` due to the nested induction with
subtype-membership predicates. All other theorems compile within
default heartbeat limits.

## Current frontier

Issue #2611 closed by PR (forthcoming). Unblocks the Schur-Weyl L_i chain:

- **#2582** (Schur-Weyl L_i C-3, irreducibility of `L_i`) is now ready
  to consume `submodule_eq_bot_or_top_of_unit_smul_mem` to transfer
  B-simplicity (which it builds from
  `Theorem5_18_4_bimodule_decomposition_explicit`'s pairwise-non-iso
  clause) to GL_N-simplicity. The corresponding `IsSimpleModule k (L i).asModule`
  consumer formulation will need a small adapter to bridge between the
  k-submodule form here and the `MonoidAlgebra k (GL_N k)`-module form
  used downstream — the translation is straightforward.
- **#2583** (Schur-Weyl L_i C-4 → SchurModule simplicity) is similarly
  unblocked, modulo the still-open #2610 (algebraic core: image of
  primitive idempotent).

## Overall project progress

Stage 3 formalization. Sorry count unchanged: **7 sorry leaves / 4 files**.
This PR adds a substantial new file (530 lines) with zero sorries that
unblocks the Schur-Weyl L_i programme.

Critical paths:
- **Wall 1** (Ch6): blocked on #2436 human-oversight.
- **Wall 3** (Ch5 SpechtModuleBasis): #2605 (critical-path) still open.
- **Schur-Weyl L_i chain**: #2582 (C-3) and #2610 (C-4a) remain claimed/open;
  #2493 still awaits final assembly. **#2611 done** (this PR).
- **Theorem 2.1.2** (Ch2): blocked on Wall 1.

## Next step

**Workers** (priority order):
1. **#2605** (Wall 3 C.1.c rev2 — critical-path).
2. **#2610** (Schur-Weyl C-4a — primitive idempotent algebraic core).
   Pairs with #2611 (this PR) to close #2583.
3. **#2582** (claimed) — once unblocked, can use `submodule_eq_bot_or_top_of_unit_smul_mem`.
4. **#2591** (summarize wave-57). Refreshes sorry-landscape.
5. **#2601** (heartbeat tightening). Quick hygiene win.
6. **#2602** (extract `glHom` helpers). Lowest priority.

## Blockers

None for this session. PR pending CI.
