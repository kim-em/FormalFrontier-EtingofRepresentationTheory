## Accomplished

- Created `EtingofRepresentationTheory/Infrastructure/IrreducibleEnumeration.lean` connecting Mathlib's Wedderburn-Artin theorem to the group algebra representation theory setting
- **Proved** (no sorry):
  - `MonoidAlgebra.finrank_eq_card`: dim(k[G]) = |G|
  - `finrank_pi_matrix`: dim(Π Mat_{d_i}) = Σ d_i²
  - `IrrepDecomp.sum_sq_eq_card`: |G| = Σ (d_i)² (the dimension formula)
  - `MonoidAlgebra.wedderburnArtin`: existence of the decomposition (direct from Mathlib)
- **Sorry'd** (needs `FDRep k G ≌ FGModuleCat k[G]` which Mathlib doesn't have yet):
  - `IrrepDecomp.n_eq_card_simples`: components biject with simple FDRep objects
  - `IrrepDecomp.d_eq_finrank`: dimensions match irreducible finranks
- Provided `IrrepDecomp` structure bundling n, d, and the algebra isomorphism
- Updated Theorem4_1_1 comment to reference the new infrastructure
- Added Infrastructure module to root imports

## Current frontier

The sorry'd theorems (`n_eq_card_simples`, `d_eq_finrank`) require building the categorical equivalence `FDRep k G ≌ FGModuleCat k[G]`, which is listed as a TODO in Mathlib's FDRep.lean itself. This is a substantial piece of infrastructure that goes beyond this issue's scope.

## Overall project progress

Stage 3.2 (proof filling) is active. Infrastructure for Wedderburn-Artin decomposition is now in place. The proved dimension formula (|G| = Σ d_i²) and the IrrepDecomp structure can be used by downstream issues including regular representation character identity (#644) and column orthogonality (#645).

## Next step

- Issue #644 (regular representation character identity) can proceed using `IrrepDecomp` — it needs the decomposition and dimension formula which are now proved
- The sorry'd FDRep connection theorems should be tracked as a separate infrastructure issue if needed

## Blockers

The `FDRep k G ≌ FGModuleCat k[G]` equivalence is missing from Mathlib. This blocks the full connection between Wedderburn-Artin components and FDRep simple objects. The algebraic side (decomposition, dimensions) is complete.
