## Accomplished

Filled the two definition bodies for Lemma 5.25.3 infrastructure (issue #1038):

1. **`GL2.ellipticSubgroup`**: Defined as the range of a field extension embedding `(GaloisField p (2*n))ˣ →* GL₂(GaloisField p n)` via `MonoidHom.range`. The embedding helper `fieldExtEmbed` (representing the left regular representation on a chosen basis) is sorry'd as a separate private definition.

2. **`GL2.complementarySeriesChar`**: Defined using the virtual character formula:
   - `char(W₁)(g) · char(V_{α,1})(g) - char(V_{α,1})(g) - Ind_K^G(ν)(g)`
   - The induced character piece uses the Frobenius formula inline (sum over conjugates)
   - `α = ν|_{scalars}` via `scalarToElliptic` composition
   - Helper character functions (`charW₁`, `charVα₁`) and `scalarToElliptic` are sorry'd as separate private definitions

Added `[Fintype (GL2 p n)]` instance parameter to `complementarySeriesChar` and `Lemma5_25_3_dimension` (needed for the Frobenius sum over group elements).

## Current frontier

Both target definitions filled. Build succeeds with sorry warnings only from:
- 4 private helper definitions (fieldExtEmbed, scalarToElliptic, charW₁, charVα₁)
- 2 theorem proofs (innerProduct, dimension) — out of scope per issue

## Overall project progress

- ~189/583 items sorry_free (~32.4%)
- Chapters 3 and 4: 100% complete
- Lemma 5.25.3 definitions now concrete (infrastructure for future theorem proofs)

## Next step

- Create PR for this work
- Future issue needed: fill the 4 sorry'd helper definitions, then prove the two theorems

## Blockers

None.
