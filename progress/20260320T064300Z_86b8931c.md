## Accomplished

Issue #1366: Stage 3.2 Ch5 prove normSq_complementaryChar_parabolic (Lemma 5.25.3).

Built proof infrastructure for `normSq_complementaryChar_parabolic` (|χ(g)|² = 1 for parabolic g):

1. **`quadratic_one_root_zero_disc`** (~60 lines): A quadratic with zero discriminant has exactly one root. Key technique: completing-the-square identity `4a(ax²+bx+c) = (2ax+b)² - (b²-4ac)` to avoid field_simp/ring issues with inverses. Char 2 case sorry'd (needs Frobenius surjectivity).

2. **`charW₁_parabolic`** (~30 lines): charW₁ = 0 for parabolic elements (exactly 1 fixed point on P¹). Case splits on M₀₁ = 0 vs ≠ 0.

3. **`normSq_monoidHom_val_eq_one`** (~25 lines, fully proved): |α(g)|² = 1 for any finite group character α. Uses `Complex.norm_eq_one_of_pow_eq_one` and `RCLike.mul_conj`.

4. **`normSq_complementaryChar_parabolic`**: Proved modulo `complementaryChar_parabolic_val`. Rewrites χ(g) = -(alpha a) then applies normSq_monoidHom_val_eq_one.

5. **`complementaryChar_parabolic_val`**: Sorry'd. States χ(g) = -(alpha a) for parabolic g.

Key technical challenges solved:
- `linear_combination` vs `ring` on general fields (GaloisField is not ordered, so `linarith` fails)
- Quadratic identity proved via nonlinear `linear_combination` with `hab * hab` ... didn't work, used step-by-step substitution + ring instead
- Completing-the-square approach avoids `field_simp` leaving `a⁻¹` terms

## Current frontier

- `complementaryChar_parabolic_val` (sorry'd): needs charW₁ = 0 (done), Ind = 0 for parabolic (needs K ∩ parabolic = ∅), charVα₁(g) = α(eigenvalue)
- `quadratic_one_root_zero_disc` char 2 case (sorry'd): needs Frobenius endomorphism surjectivity on finite fields

## Overall project progress

- Sorry count in Lemma5_25_3.lean: 3 → 4 (added new helper infrastructure, 1 new sorry in char 2 case, 1 sorry for complementaryChar_parabolic_val)
- The main target `normSq_complementaryChar_parabolic` has a complete proof structure
- `normSq_monoidHom_val_eq_one` is fully proved (key reusable lemma)
- File builds successfully with only `declaration uses sorry` warnings

## Next step

- Prove `complementaryChar_parabolic_val`: show χ(g) = -(alpha a) for parabolic g. Requires:
  - Ind_K^G ν(g) = 0 for parabolic g (K ∩ parabolic conjugacy class = ∅)
  - charVα₁(g) = α(eigenvalue) for parabolic g
- Or: prove the char 2 case of `quadratic_one_root_zero_disc` (Frobenius surjectivity)

## Blockers

None — remaining sorries have clear proof strategies.
