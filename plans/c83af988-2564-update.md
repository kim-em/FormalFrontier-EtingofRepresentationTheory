## Current state

The Mathlib upstream PR is open:
**https://github.com/leanprover-community/mathlib4/pull/38583** —
adds `MvPolynomial.eq_of_eval_eq_on_gl` to a new file
`Mathlib/LinearAlgebra/Matrix/GeneralLinearGroup/MvPolynomial.lean`.

Step 1 of this issue (open the upstream PR) is **complete** as of
session `3e2d1cff` (`progress/20260427T160426Z_3e2d1cff.md`). This
issue now tracks the post-merge cleanup.

The local copy at `Chapter5/PolynomialRepEmbedding.lean:585-641`
remains in place; downstream Schur-Weyl chain is unblocked. Awaiting
upstream merge before the local copy can be removed.

## Deliverables (post-upstream merge)

1. Bump the project's `lake-manifest.json` to a Mathlib revision that
   includes the new lemma
   (https://github.com/leanprover-community/mathlib4/pull/38583).
2. Remove the local copy at
   `Chapter5/PolynomialRepEmbedding.lean:585-641` (the entire
   `namespace MvPolynomial` block).
3. Verify the call site at
   `Chapter5/PolynomialRepEmbedding.lean:704` (in `hP_mul_of_hP`)
   still works. The upstream signature uses
   `{m : Type*} [Fintype m] [DecidableEq m]`; the local signature uses
   `Fin N : Type`, so the call site already passes `Fin N` and should
   resolve unchanged.
4. `lake build EtingofRepresentationTheory.Chapter5.PolynomialRepEmbedding`
   passes.

## Blocked on

External: https://github.com/leanprover-community/mathlib4/pull/38583
(awaiting Mathlib review/merge — the project's Mathlib pin currently
predates the new file).

## Context

- Local definition: `Chapter5/PolynomialRepEmbedding.lean:585-641`
  (the local lemma now carries a doc-comment forwarding to the upstream
  PR — added in PR #2584 / commit 1a1b8bd).
- Call site: `Chapter5/PolynomialRepEmbedding.lean:704` (in
  `hP_mul_of_hP`).
- Upstream PR opened by session `3e2d1cff`; rationale for placing the
  lemma in `GeneralLinearGroup/MvPolynomial.lean` (rather than
  `Mathlib/LinearAlgebra/Matrix/MvPolynomial.lean` as originally
  suggested) is in `progress/20260427T160426Z_3e2d1cff.md` — moving
  it into `Matrix/MvPolynomial.lean` created an import cycle.
- Audit: #2554 (review),
  `progress/reviews/2026-04-27-schur-weyl-injection-and-hPmul.md`.

## Verification

- After upstream merge: local copy removed, call site updated, build
  green for `EtingofRepresentationTheory.Chapter5.PolynomialRepEmbedding`.

## Notes

This is a low-priority tracker. The local copy works; downstream
chain is unblocked. No urgency — re-open this issue (remove the
`blocked` label) once the Mathlib PR merges and the project bumps to a
Mathlib revision that includes the new file.
