# Meditate #2152: PolytabloidBasis Architecture Assessment

## Deliverable 1: PolytabloidBasis Sorry Assessment

### Current State (7 sorries in PolytabloidBasis.lean, 1 in TabloidModule.lean)

| Line | Name | Status | Assessment |
|------|------|--------|------------|
| 195 | `polytabloid_mem_spechtModule` | **HARD but provable** | Needs κ_T · of(σ_T) · a_λ ∈ ℂ[S_n] · c_λ. Since κ_T = Σ sign(σ_T⁻¹qσ_T)·of(σ_T⁻¹qσ_T), this expands to Σ sign(...)·of(σ_T⁻¹qσ_T·σ_T)·a_λ = Σ sign(...)·of(q)·of(σ_T)·a_λ. Each term of(q)·of(σ_T)·a_λ = of(q·σ_T)·a_λ, and since q ∈ Q_λ, we need of(q·σ_T)·a_λ ∈ V_λ. Since V_λ = ℂ[S_n]·a_λ·b_λ, this requires showing a_λ = a_λ·b_λ·(something). This is non-trivial — it requires the idempotent property of c_λ (Lemma 5.13.3). **Provable but depends on Lemma5_13_3.** |
| 296 | `polytabloid_apply` | **PROVABLE** | Pure MonoidAlgebra coefficient computation. Expand κ_T · of(σ_T) · a_λ using Finsupp multiplication. Issue #2150 targets this. |
| 307 | `polytabloid_self_coeff` | **PROVABLE** | Specialization of polytabloid_apply at σ = σ_T, using P_λ ∩ C_T = {1} (already proved as `row_col_inter_trivial'`). Issue #2150 targets this. |
| 471 | `polytabloid_support` | **PROVABLE** | Contrapositive of coefficient computation. If σ ∉ C_T · σ_T · P_λ then the coefficient is 0. Issue #2150 targets this. |
| 481 | `polytabloid_linearIndependent` | **PROVABLE via TabloidModule** | This is a thin wrapper. The real proof is `polytabloid_linearIndependent'` in TabloidModule.lean (line ~900+). That proof needs `polytabloid_self_coeff` and `polytabloid_syt_dominance`. |
| 889 | `column_standard_in_span'` | **BLOCKED on straightening redesign** | Requires Garnir expansion to produce terms that decrease in a well-founded order. The `columnInvCount'` order was proven FALSE (counterexample in #2104). Needs tabloid dominance order — but that order is not yet defined as a WF relation. |
| 1326 | Garnir general case | **BLOCKED on straightening redesign** | Same issue as column_standard_in_span'. The single-column case is proved; the general case needs the Garnir element expansion to produce tabloid-dominated terms. |

**TabloidModule.lean:**

| Line | Name | Status | Assessment |
|------|------|--------|------------|
| 889 | `polytabloid_syt_dominance` | **HARD, key bottleneck** | Needs: if e_{T₁}(σ_{T₂}) ≠ 0 then tabloid(T₁) dominates tabloid(T₂). The PQ decomposition gives σ₂ = σ₁⁻¹·q·σ₁·σ₁·p, and dominance needs a cross-column argument comparing sorted entry sets. Issue #2088 targets this (difficulty 7-8). Issue #2124 decomposes the core entry comparison lemma. |

### Summary Classification

- **(a) Provable directly in group algebra**: `polytabloid_apply`, `polytabloid_self_coeff`, `polytabloid_support` (3 sorries, issue #2150)
- **(b) Provable with tabloid module**: `polytabloid_linearIndependent` (via TabloidModule), `polytabloid_syt_dominance` (in TabloidModule)
- **(c) Blocked on redesign**: `column_standard_in_span'`, Garnir general case (2 sorries, need WF order)
- **(d) Hard but provable**: `polytabloid_mem_spechtModule` (depends on Lemma 5.13.3 idempotent property)

### Key Question: Should the Specht module be redefined?

**No.** The current definition `SpechtModule = ℂ[S_n] · c_λ` is correct and standard. The `polytabloid_mem_spechtModule` proof IS hard with the T-dependent definition, but it's the mathematically correct statement. The alternative (redefining Specht module as a submodule of TabloidRepresentation) would:
- Require proving the two definitions are equivalent anyway
- Break all downstream code that uses the ℂ[S_n] · c_λ characterization
- Not actually be simpler (you'd need the same idempotent property)

**Recommendation**: Keep the current definition. Prove `polytabloid_mem_spechtModule` using the idempotent property of c_λ when Lemma 5.13.3 is available.

## Deliverable 2: Straightening Algorithm Viability

### PR #2119 Assessment

PR #2119 replaces `columnInvCount'` infrastructure with `straighteningLT` based on tabloid dominance. The diff is moderate (93 additions, 177 deletions) but:

1. **`straighteningLT` is sorry'd** — the new WF relation is not defined, just declared
2. **Circular dependency**: #2104 (this PR's issue) → #2117 (define tabloid dominance order) → #2118 (Garnir respects dominance) → back to #2104
3. **CI failure**: The PR doesn't compile on current main
4. **Stale**: Created before polytabloid definition switch (#2132) and TabloidRepresentation (#2146)

**Recommendation: Close PR #2119.** The approach is directionally correct (tabloid dominance IS the right WF order), but the implementation is incomplete and stale. The straightening algorithm needs:

1. Define tabloid dominance as a WF partial order (in TabloidModule.lean — `tabloidDominance` exists but needs WF proof)
2. Prove Garnir expansion produces tabloid-dominated terms
3. Then rewrite `column_standard_in_span'` using the new WF order

This is a 3-step dependency chain that should be tracked as separate issues.

### Does the tabloid module approach bypass group-algebra straightening?

**Partially.** Linear independence is already handled by the tabloid module approach (unitriangularity). But **spanning** still needs straightening: you need to show every element of V_λ is a linear combination of polytabloids. The straightening algorithm (reducing arbitrary σ · c_λ to a sum of polytabloids) is still required for the spanning direction.

So: tabloid module handles independence, but straightening is still needed for span.

## Deliverable 3: Dependency Graph Cleanup

### Blocked issues with resolved dependencies

Checked all 6 blocked issues:
- **#2143** depends on #2111 (OPEN) — **still correctly blocked**
- **#2125** depends on #2124 (OPEN) — **still correctly blocked**
- **#2118** depends on #2117 (OPEN) — **still correctly blocked**
- **#2117** depends on #2104 (OPEN, has stale PR #2119) — **still correctly blocked**
- **#2112** depends on #2110 (CLOSED) + #2111 (OPEN) — **still correctly blocked** (one dep resolved, one remains)
- **#1571** — old template reconciliation, likely stale

No blocked issues need unblocking. The dependency graph is consistent.

### Newly actionable work (no dependencies)

| Issue | Type | Difficulty | Why actionable |
|-------|------|-----------|----------------|
| #2150 | feature | 5 | Pure group algebra computation, no dependencies |
| #2083 | feature | 9 | Self-contained PID module theory |
| #2124 | feature | 5-6 | SYT entry comparison, no dependencies |
| #2105 | feature | 7-8 | Needs straightening infra — actually BLOCKED on #2104 implicitly |
| #2088 | feature | 7-8 | Needs #2124 for entry comparison — should be blocked on #2124 |

### Recommended issue updates

1. **#2105** should be marked blocked on #2104 (it needs the straightening WF infrastructure)
2. **#2088** should be marked blocked on #2124 (the entry comparison lemma was decomposed out)
3. **#2106** should be marked blocked on #2088 (it assembles polytabloid_linearIndependent from polytabloid_syt_dominance)
4. **Close PR #2119** with explanation about staleness
5. **#2153** (fix/close PR #2119) can be closed once PR #2119 is closed

### Critical path for PolytabloidBasis completion

```
#2150 (polytabloid_apply/self_coeff/support)  [NO DEPS, difficulty 5]
  └→ #2124 (SYT entry comparison)  [NO DEPS, difficulty 5-6]
      └→ #2088 (polytabloid_syt_dominance)  [BLOCKED on #2124]
          └→ #2106 (polytabloid_linearIndependent)  [BLOCKED on #2088]

#2104 (straightening WF redesign)  [OPEN, needs fresh approach]
  └→ #2117 (tabloid dominance WF order)  [BLOCKED on #2104]
      └→ #2118 (Garnir respects dominance)  [BLOCKED on #2117]
          └→ column_standard_in_span' + Garnir general case
              └→ polytabloid_span (already proved from straightening)
```

**Priority**: #2150 and #2124 are the critical path — both are unblocked and feed into the hardest remaining sorry (`polytabloid_syt_dominance`).
