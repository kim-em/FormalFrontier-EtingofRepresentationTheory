## Current state

`etilde6v2Rep_isIndecomposable` (line 2323 of `InfiniteTypeConstructions.lean`)
proves indecomposability of the Ẽ₆ representation for `m ≥ 1`. PR #2414
landed the structural framework with 5 sub-sorries. This issue covers the
`hbot3` sub-sorry at line 2500 — the Γ surjectivity argument.

This sub-sorry is structurally simple (Γ is surjective, so `W'(0) = ⊤`
forces `W(3) = ⊥`) but requires a direct proof of surjectivity of
`etilde6v2Gamma`.

## Deliverables

1. Fill the sorry at line 2500 of `InfiniteTypeConstructions.lean`
   (`have hbot3 : W (3 : Fin 7) = ⊥`). At this point:
   - `hbot0 : W (0 : Fin 7) = ⊥` is available.
   - `hbot4 : W (4 : Fin 7) = ⊥` is available.
   - The invariance hypotheses on `W, W'` and the complement structure
     `hc` are in scope.

## Proof sketch (from the comment at lines 2496-2498)

- Γ = `etilde6v2Gamma` is surjective: given `(u, v) ∈ V(3) = ℂ^{m+1} × ℂ^{m+1}`,
  the preimage `(v, u-v, 0) ∈ V(0) = ℂ^{m+1} × ℂ^{m+1} × ℂ^{m+1}` satisfies
  `Γ(v, u-v, 0) = (v + (u-v), v + N·0) = (u, v)`.
- From `W(0) = ⊥`, `W'(0) = ⊤`, so every `(a, b, c) ∈ V(0)` is in `W'(0)`.
- Hence every `(u, v) ∈ V(3)` is in `W'(3)` (since `W'` is invariant along
  the arrow `0 → 3`).
- Therefore `W'(3) = ⊤`, giving `W(3) = ⊥`.

The direct "surjectivity of Γ" statement is what needs the construction;
once it is in hand, the rest is bookkeeping.

## Context

- File: `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`,
  line 2500 (inside `intro W W' hW_inv hW'_inv hc h24 hbot v`).
- Framework set up by PR #2414.
- Sibling sub-sorries (each a separate issue): `leaf24_containment`,
  `hN₁`+`hN₂`, `hbot0`.
- `etilde6v2Gamma` is defined earlier in the file (around line 2200). Read
  its definition to construct the explicit preimage `(v, u-v, 0)`.
- Difficulty: 5-6. The mathematical argument is straightforward; the work
  is mostly constructing the explicit preimage and unfolding `Γ`.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6.InfiniteTypeConstructions`
  succeeds.
- 1 sorry eliminated (line 2500).
