## Current state

Two PRs landed on the Schur-Weyl chain since the last review pass and
have not been audited:

- **PR #2578** (commit 0cc1dc7) closed issue #2540: filled in the
  sorry on `glTensorRep_equivariant_schurWeyl_decomposition`
  (`Chapter5/FormalCharacterIso.lean:779-949`). This is the
  decomposition-anchor sorry that wave 56's landscape flagged as
  "intentional, claimed". The proof builds:
  1. The bimodule decomposition `e : V^⊗n ≃ ⨁ᵢ ↥(V'i) ⊗ (↥(V'i) →ₗ[A] V^⊗n)`
     via `Theorem5_18_1_bimodule_decomposition_explicit`.
  2. `glHom : GL_N →* centralizer(symGroupImage)` via the centralizer
     identity from `Theorem5_18_4_centralizers`.
  3. Per-summand `ρ i : GL_N →* End_k (Hom_A(V'_i, V^⊗n))` directly
     (bypassing a centralizer-Module instance diamond).
  4. `Module.Finite k` for `↥(V' i)` and the hom space.
  5. Equivariance via `DirectSum.linearMap_ext` + `TensorProduct.ext'`
     reducing to pure tensors.
  Heartbeats bumped to `maxHeartbeats 6400000` (kernel whnf timeout).

- **PR #2579** (commit 6d43568) closed issue #2572: introduced
  `Theorem5_18_4_GL_rep_decomposition_explicit`
  (`Chapter5/Theorem5_18_4.lean:626-...`). This is a refined version of
  `Theorem5_18_4_GL_rep_decomposition` exposing concrete `Submodule`
  summands `S i`, the bundled `L i : FDRep`, an explicit eval formula
  `e.symm (of i (v ⊗ l)) = (L_carrier i l) v`, and an iso identifying
  the FDRep carrier with `↥(S i) →ₗ[A] V^⊗n`. The construction copies
  PR #2578's `glHom` / `ρ` / `Module.Finite` patterns.

Both PRs are on the **Schur-Weyl L_i critical path**: PR #2578's
theorem is consumed by `formalCharacter_tensorPower_eq_sum_character_L`
(C-1 / issue #2580), and PR #2579 is consumed by issue #2589
(`refactor(Ch5): consume Theorem5_18_4_GL_rep_decomposition_explicit
in glTensorRep_equivariant_schurWeyl_decomposition`). An audit before
those downstream consumers stack further is timely.

The audit chain so far has covered (all PASS): #2549 (equivariance
PRs #2538/2539), #2554 (plumbing PRs #2528/2551), #2555 (character
additivity PRs #2516/2534), #2557 (explicit bimodule PR #2509), #2560
(L_i FDRep GL_N PR #2504), #2561 (foundation tier PR #2511). PRs
#2578 and #2579 are the next un-audited links.

## Deliverables

Produce a single review report
`progress/reviews/2026-04-28-schur-weyl-Li-equivariant-and-explicit.md`
covering both PRs with a categorical PASS / PASS-with-followup /
FAIL verdict per question category. Post a consolidated review
comment on **issue #2540 and issue #2572** (or a fresh review issue
linking forward to both) summarising the verdict.

Audit questions:

**For PR #2578 (`glTensorRep_equivariant_schurWeyl_decomposition`):**

- **Q1**: Does the constructed `glHom` correctly send `g : GL_N` to
  `PiTensorProduct.map (fun _ => mulVecLin g.val)`, with image
  provably in `centralizer(symGroupImage)` via `Theorem5_18_4_centralizers`?
- **Q2**: Is the per-summand action `ρ i : GL_N →* End_k (Hom_A(V'_i, E))`
  (as post-composition with `centralizerToEndA (glHom g)`) well-defined
  and avoiding the `centralizerModuleHom` / `Module.toModuleEnd`
  instance diamond described in the PR commentary?
- **Q3**: Are the `Module.Finite k (V'_i →ₗ[A] E)` derivations sound
  (Free + LinearMap finiteness + restrictScalars injection)?
- **Q4**: Does the equivariance proof correctly reduce to the basis case
  `of i (s ⊗ₜ l)` and discharge it via the eval formula?
- **Q5**: Is the heartbeat bump (`maxHeartbeats 6400000`) genuinely
  needed, or could it be tightened with `let`-binding refinements
  (cf. recent #2574 hygiene work)? File an issue if tightening is
  feasible.

**For PR #2579 (`Theorem5_18_4_GL_rep_decomposition_explicit`):**

- **Q6**: Does the theorem signature correctly expose
  `S i : Submodule (symGroupImage) (V^⊗n)` (not just the type),
  the `FDRep`-bundled `L i`, and the `L_carrier i` linear iso
  identifying `(L i : Type u)` with `↥(S i) →ₗ[A] V^⊗n`?
- **Q7**: Is the eval formula
  `e.symm (of i (v ⊗ l)) = (L_carrier i l) v` correctly threaded
  through the `Theorem5_18_1_bimodule_decomposition_explicit` →
  `Theorem5_18_4_bimodule_decomposition_explicit` chain?
- **Q8**: Is the manual `ρ` construction (bypassing `Module.toModuleEnd`)
  identical in shape to PR #2578's manual construction? Could the two
  be deduplicated via a shared helper, or is the duplication justified
  by the different SMul-instance contexts? File a refactor issue if
  worthwhile.
- **Q9**: Does the call site of `Theorem5_18_4_GL_rep_decomposition`
  inside the body of `_explicit` (line 654) actually depend on the
  non-explicit form, or is it dead code? (Issue #2590 flags this as
  a "heads-up: cut to avoid mutual recursion".)
- **Q10** (doc): Does the doc-string accurately describe the eval
  formula, the universe-polymorphic carrier, and the difference vs
  `_GL_rep_decomposition`?

For each finding (F-numbered), file a follow-up issue if non-blocking,
or block on the original issue/PR if the finding is severe enough to
require revert/repair.

## Context

- Source files:
  - `Chapter5/FormalCharacterIso.lean:779-949` (PR #2578 target)
  - `Chapter5/Theorem5_18_4.lean:626-...` (PR #2579 target)
- Closed issues: #2540, #2572.
- Refactor issues that consume these PRs: #2589 (consume `_explicit`
  in `equivariant_schurWeyl_decomposition`), #2590 (delegate
  non-explicit `_GL_rep_decomposition` to `_explicit`).
- Audit pattern reference: `progress/reviews/2026-04-28-schur-weyl-2a-foundations.md`
  (most recent foundation-tier audit, by review session
  `b5a0a349`).
- Progress notes:
  - `progress/20260427T150323Z_c0aa3852.md` — author's session note for
    PR #2578.
  - `progress/20260427T153047Z_8e0b9c38.md` — author's session note for
    PR #2579.

## Verification

- One markdown audit report under `progress/reviews/` with a verdict
  per question (Q1–Q10).
- Either close the audit on #2540 / #2572 with a summary comment, or
  open a fresh `audited` issue linking to the report.
- Each non-blocking finding (F-numbered) has a follow-up issue filed
  via `coordination plan`.
- No code changes in this PR — the audit report is the deliverable.
  If a finding requires immediate code fix, file it as a separate
  feature issue and tag the audit with the issue number.

## Scope

Single-PR review session. Aim for ~200–400 lines in the audit report.
Do not also formalize the refactor follow-ups (#2589 / #2590) — those
are separate feature issues.
