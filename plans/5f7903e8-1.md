## Current state

The most recent sorry-landscape summary is `progress/sorry-landscape.md`
(wave 57, generated via #2591 on 2026-04-27 ~20:07 UTC). Since then
**29 PRs have merged on `main`** — well past the 10-PR summarize
trigger. ~11 of these are substantive (feature/refactor); the rest
are reviews, hygiene, and progress notes.

Headline movements since wave 57:

- **Schur-Weyl L_i chain (Ch5 / `FormalCharacterIso.lean:399`)**:
  major progress. The polynomial identity `(∑ Xᵢ)^n = ∑ d·schurPoly`
  combined-dimension form landed (#2637). The C-4b GL_N transfer
  helper landed (#2646). The MonoidAlgebra-simplicity transfer helper
  landed (#2654). The C-4a-i sub-α block factorization of `c_λ`
  landed (#2665). The B-side → GL_N-rep simplicity bridge wrapper
  `Theorem5_18_4_GL_rep_decomposition_simple` landed (#2670). A
  refactor extracting `glHom` + per-component `ρ` helpers landed
  (#2664). Three review audits have signed off the critical-path
  pieces (#2671 / #2672 / #2673 → PRs #2677 / #2679 / #2680).

  Sub-issue #2643 was decomposed into sub-α/β/γ (#2655 done, #2656
  claimed, #2657 blocked). The C-4a-i / C-4a-ii / C-4b / C-4c
  decomposition under #2612 is now well-articulated.

- **Wall 3 (Ch5 SpechtModuleBasis, 2 sorries)**: the original
  Algorithm A approach hit a dead end on Q_high cancellation. After
  two redesign meditates (#2660 → `algorithm-A-redesign.md`; #2663
  follow-up), the chain was re-routed: R1 bridge
  `in_L_of_in_V_of_supp_bounded` landed (#2669); sub-X bridge
  `polytabloidTab_in_lower_span_of_dominates` landed (#2653); R2
  (Algorithm A core) was escalated to a third meditate **#2676**
  (Q_high cancellation involution, claimed) which is the current
  critical path. Wall 3 D-side / C.2 issues (#2520, #2500) remain
  blocked, semantically waiting on #2676.

- **Wall 1 (Ẽ/T framework, Ch6)**: **unchanged.** 3 sorries; #2436
  still `human-oversight + replan`; still awaiting Kim's option choice.
  The longest-running open item.

- **Theorem 2.1.2 forward bridge (Ch2)**: unchanged; still gated on
  Wall 1.

- **Hygiene**: CharZero hypothesis dropped from
  `formalCharacter_glTensorRep_eq_pow` and supporting lemmas (#2662);
  heartbeat bumps tightened on `Theorem5_18_1_bimodule_decomposition_explicit`
  (#2650) and `glTensorRep_equivariant_schurWeyl_decomposition` (#2640).

Current sorry count: **7 across 4 files** (unchanged from wave 57; the
Schur-Weyl chain and Wall 3 chain are landing infrastructure /
helper PRs, but no sorry-killing top-of-chain closures have merged
yet — those land once #2676 produces output and #2483 final assembly
is reached).

A fresh wave-58 landscape is warranted to take stock and to give the
next wave of workers a clean onboarding document.

## Deliverables

Produce `progress/sorry-landscape.md` (overwrite the existing wave-57
snapshot) covering:

1. **Header**: wave number (58), generation timestamp, source issue.
2. **Executive summary**: total sorry count (7), files (4), delta vs
   wave 57 (0 — chain helpers landed but no top-of-chain closure),
   headline movements per the bullet list above.
3. **Merges since wave 57** (29 PRs, tabulated in chronological order
   with sorry-impact column; mark substantive vs review/progress).
4. **Per-chapter breakdown** (Ch2 = 1, Ch5 = 3, Ch6 = 3 — confirm by
   `grep -nE '^\s*sorry\s*$' EtingofRepresentationTheory/`).
5. **Per-file detail** for each active sorry: file path, line number,
   theorem name, current status, blocking issue (if any), short
   description. Specifically the 3 Ch5 sorries are now:
   - `Chapter5/SpechtModuleBasis.lean:1487` (Wall 3, gated on #2676)
   - `Chapter5/SpechtModuleBasis.lean:1726` (Wall 3, gated on #2676)
   - `Chapter5/FormalCharacterIso.lean:399` (Schur-Weyl, gated on
     #2483 → #2482 → #2493 → #2612 → #2657 + #2644).
6. **Dependency clusters**: refresh the Wall 3 chain status
   (R1 ✓ / R2 = #2676 meditate / R3 will follow); refresh the
   Schur-Weyl L_i (Part C-1/2/3/4) sub-chain decomposition reflecting
   #2655 ✓ / #2656 claimed / #2657 blocked / #2644 claimed / #2612
   blocked / #2493 blocked / #2482 blocked / #2483 blocked.
7. **Active / unclaimed / blocked issues table** with current state of
   the `agent-plan` pool (14 open: #2676, #2657, #2656, #2644, #2612,
   #2564, #2543, #2520, #2500, #2493, #2483, #2482, #2436, #2401).
8. **Next-wave critical path**: which issues, if closed, would unblock
   the most downstream work. After this wave, the critical path is:
   Wall 3 = #2676 meditate → R2.x feature issues → #2520 / #2500
   re-triage → Wall 3 closure (−2 sorries). Schur-Weyl =
   #2656 → #2657 → #2612 → #2493 → #2482 → #2483 closure
   (−1 sorry). Wall 1 = blocked on Kim's #2436 decision.

Also refresh the design-walls snapshot at
`progress/design-walls-wave58.md` (one-page decision sheet, in the
same format as `progress/design-walls-wave57.md`). Wall 1 unchanged
(still awaiting #2436); Wall 2 still closed; Wall 3 in flight on
#2676. If a wall has no movement, a short header noting "no movement
since wave 57" is acceptable, but still create the file for
consistency.

**Do not update `PLAN.md`** (off-limits per project conventions).

## Context

- Previous summarize issue: #2591 (closed 2026-04-27T20:07Z) → PR
  #2635 (the wave-57 landscape doc + design-walls-wave57.md).
- Template to follow: `progress/sorry-landscape.md` (wave 57) —
  preserve the section layout and tone; update all numbers, deltas,
  and per-file content.
- Design-walls template: `progress/design-walls-wave57.md`.
- Input data:
  - `gh api 'repos/{owner}/{repo}/pulls?state=closed&per_page=100&sort=updated&direction=desc' --jq '.[] | select(.merged_at != null and .merged_at > "2026-04-27T20:07:36Z") | "\(.merged_at) #\(.number) \(.title)"'`
    for PRs merged.
  - `grep -rnE '^\s*sorry\s*$' EtingofRepresentationTheory` for current
    sorry locations.
  - `gh issue list --label agent-plan --state open --limit 30 --json number,title,labels`
    for the issue pool.
  - Progress notes between 2026-04-27T20:07Z and now (file-name sort
    in `progress/`).
  - The Algorithm A redesign notes
    `progress/algorithm-A-redesign.md` for Wall 3 architecture
    context.

## Verification

- `progress/sorry-landscape.md` exists, is well-formed Markdown, and
  contains accurate current counts (7 sorries / 4 files).
- Every active sorry is catalogued with a dependency / issue pointer.
- Every open `agent-plan` issue is cross-referenced (state, blocker).
- The summary is readable by a fresh worker as their primary
  onboarding document.
- `progress/design-walls-wave58.md` exists.
- Commit the docs on a feature branch and open a PR; enable
  auto-merge.

## Scope

- Documentation refresh only. No Lean code changes.
- Single PR; auto-merge once CI is green.
