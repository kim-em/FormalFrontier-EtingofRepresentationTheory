## Current state

`etilde6v2Rep_isIndecomposable` (line 2323 of `InfiniteTypeConstructions.lean`)
proves indecomposability of the Ẽ₆ representation for `m ≥ 1`. PR #2414
landed the structural framework with 5 sub-sorries. This issue covers the
`hbot0` sub-sorry at line 2425.

The other sub-sorries (`leaf24_containment`, `hN₁`/`hN₂`, `hbot3`) are
already `sorry`'d in scope — they provide the premises for the proof of
`hbot0` without blocking work on it.

## Deliverables

1. Fill the sorry at line 2425 of `InfiniteTypeConstructions.lean`
   (`have hbot0 : W (0 : Fin 7) = ⊥`). The premises available are:
   - `h24 : W (2 : Fin 7) = W (4 : Fin 7)` (from `leaf24_containment`)
   - `hbot : W (2 : Fin 7) = ⊥` (from applying
     `nilpotent_invariant_compl_trivial`)
   - All invariance hypotheses for `W` and `W'` and the complement
     structure `hc`.

## Proof sketch

- `W(2) = W(4) = ⊥`, so `W'(2) = W'(4) = ⊤` (complements).
- Via `starEmbed1` and `embed2to3_AB`: any preimage of `⊤` at vertices 2, 4
  fills `(x, y) ∈ W'(1)` with `x` ranging over `ℂ^{m+1}`.
- Via arm 3 (vertex 6 → 5 → 0) with `starEmbedNilp` (using `m ≥ 1` for
  non-trivial `N`) and `embed2to3_CA`: the nilpotent coupling forces
  enough of `W'(0)` that all 3 blocks of `V(0) = ℂ^{m+1} × ℂ^{m+1} × ℂ^{m+1}`
  must lie in `W'(0)`, giving `W'(0) = ⊤` hence `W(0) = ⊥`.

## Context

- File: `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`,
  line 2425 (inside `intro W W' hW_inv hW'_inv hc h24 hbot v`).
- Framework set up by PR #2414.
- Sibling sub-sorries (each a separate issue): `leaf24_containment`,
  `hN₁`+`hN₂`, `hbot3`.
- Reference: Ẽ₇ `hbot0` at line 2961 (#2394), T(1,2,5) `hbot0` at line 3425
  (#2400). The Ẽ₆ version is structurally similar but with 7 vertices and
  3 short arms.
- The arrow helpers `hom21`, `hom10`, `hom03`, `hom43`, `hom65`, `hom50`
  and the injectivity lemmas are in scope.
- Difficulty: 7.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6.InfiniteTypeConstructions`
  succeeds.
- 1 sorry eliminated (line 2425).
