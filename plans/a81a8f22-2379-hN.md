## Current state

`etilde6v2Rep_isIndecomposable` (line 2323 of `InfiniteTypeConstructions.lean`)
proves indecomposability of the Ẽ₆ representation for `m ≥ 1`. PR #2414
landed the structural framework with 5 sub-sorries. This issue covers the
two N-invariance sub-sorries `hN₁` (line 2397) and `hN₂` (line 2400).

Note: these sub-sorries depend semantically on the equality `W(2) = W(4)`
produced from `leaf24_containment` (the sibling sub-sorry); since that is
already a `sorry`'d helper in scope, this issue can proceed in parallel
without blocking on that one.

## Deliverables

1. Fill the sorry at line 2397 of `InfiniteTypeConstructions.lean`
   (`have hN₁ : ...`). The goal is:

   ```
   ∀ x : Fin (m + 1) → ℂ, x ∈ W₁ (2 : Fin 7) → nilpotentShiftLin m x ∈ W₁ (2 : Fin 7)
   ```

2. Fill the sorry at line 2400 (`have hN₂ : ...`), the symmetric statement
   for `W₂`.

## Proof sketch

- `heq24 : W₁ (2) = W₁ (4)` and `heq24' : W₂ (2) = W₂ (4)` are already
  derived from `leaf24_containment` and are in scope at this point.
- From leaf 6 via `starEmbedNilp` and `embed2to3_CA`: for `z ∈ W(6)`,
  `(Nz, 2Nz) ∈ W(3)`.
- Combine with the equality `W(2) = W(4)` and the `(x, x) ∈ W(3)` structure
  (from leaf 2 via arm 1) to show nilpotent preserves the common leaf
  subspace.
- Mirror argument for `W₂`.

## Context

- File: `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`,
  lines 2395-2400 (inside `have hN₁` and `have hN₂`).
- Framework set up by PR #2414.
- Sibling sub-sorries (each a separate issue): `leaf24_containment` (the
  equality these depend on — already a `sorry` in scope, not a blocker),
  `hbot0`, `hbot3`.
- Reference: Ẽ₇ `hN₁`/`hN₂` at lines 2922-2927 of the same file (#2394);
  T(1,2,5) `hN₁`/`hN₂` at lines 3398-3403 (#2400). All three cases are
  mechanically similar.
- Difficulty: 7.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6.InfiniteTypeConstructions`
  succeeds.
- 2 sorries eliminated (lines 2397 and 2400).
