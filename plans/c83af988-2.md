## Current state

`Chapter5/Theorem5_18_4.lean:511-587` defines
`Theorem5_18_4_GL_rep_decomposition`, which manually constructs

* `glHom : GL_N → centralizer(symGroupImage)` (lines 548-575),
* `ρ i : GL_N → Module.End k (L' i)` via `Module.toModuleEnd` composed
  with `glHom` (lines 577-582),
* `L i : ι → FDRep k (GL_N k) := FDRep.of (ρ i)` (lines 583-585).

These three chunks duplicate work that now lives inside the explicit
variant `Theorem5_18_4_GL_rep_decomposition_explicit`
(`Chapter5/Theorem5_18_4.lean:626`), introduced by #2579 (issue #2572)
to provide the same bundled `L i : FDRep` together with an explicit
evaluation formula and an `S i : Submodule` realisation.

The non-explicit `Theorem5_18_4_GL_rep_decomposition` can therefore be
re-implemented as a thin wrapper over the explicit form: project to the
weaker existential by forgetting the `S i` `Submodule` structure (use
`↥(S i)` as the carrier `S i : Type u`) and the eval formula. All
downstream consumers of the non-explicit form stay working because the
existential signature is unchanged.

## Deliverables

1. Rewrite `Theorem5_18_4_GL_rep_decomposition` in
   `Chapter5/Theorem5_18_4.lean:511-587` so that:
   - It calls `Theorem5_18_4_GL_rep_decomposition_explicit k N n hN`
     to obtain `ι, S' (as `Submodule`), simp/dist witnesses, L (FDRep),
     L_carrier, e, eval formula`.
   - Re-exposes the existential as `S i := ↥(S' i)` together with the
     induced `AddCommGroup`, `Module k`, `Module (symGroupImage) (S i)`,
     `IsSimpleModule`, distinctness, `Module.Finite k (S i)`, and `L i`.
     All of these are immediate from the corresponding witnesses inside
     `_explicit` (the `Module.Finite k ↥(S' i)` instance via
     `Module.Finite.of_injective ((S' i).subtype.restrictScalars k)`).
   - Returns `⟨e⟩ : Nonempty (TensorPower k (Fin N → k) n ≃ₗ[k]
     ⨁ᵢ S i ⊗[k] (L i : Type u))` from the iso produced by `_explicit`.
2. Delete the duplicated `glHom` / `ρ` / `L` constructions
   (`Chapter5/Theorem5_18_4.lean:548-585`).
3. Verify the consumer chain still type-checks:
   `Theorem5_18_4_GL_rep_decomposition` is invoked at
   `Chapter5/Theorem5_18_4.lean:654` inside the body of
   `Theorem5_18_4_GL_rep_decomposition_explicit` — that call site must
   be cut to avoid mutual recursion (the body of the explicit version
   already constructs everything from `Theorem5_18_4_bimodule_decomposition_explicit`,
   so the dependency runs the other way after this refactor).

## Context

- Source theorem (the now-canonical implementation):
  `Chapter5/Theorem5_18_4.lean:626`
  (`Theorem5_18_4_GL_rep_decomposition_explicit`), introduced by
  #2579 / issue #2572.
- Refactor target: `Chapter5/Theorem5_18_4.lean:511-587`
  (`Theorem5_18_4_GL_rep_decomposition`).
- Heads-up: the current `Theorem5_18_4_GL_rep_decomposition_explicit`
  body (line 654) calls the non-explicit form *internally*. Read that
  body carefully — a naïve refactor will create mutual recursion. The
  cleanest fix is to verify whether `_explicit` actually depends on the
  non-explicit variant or if that line is dead code/an error to be
  removed during this refactor.
- Expected line savings: ~60 lines (the duplicated `glHom` + `ρ` chain).

## Verification

- `lake build EtingofRepresentationTheory.Chapter5.Theorem5_18_4`
  passes.
- The refactored `Theorem5_18_4_GL_rep_decomposition` has no new sorries
  and the existential signature is unchanged.
- All downstream consumers of `Theorem5_18_4_GL_rep_decomposition`
  (e.g. `Chapter5/FormalCharacterIso.lean`) continue to type-check.
  Search via `Grep "Theorem5_18_4_GL_rep_decomposition\b"` and confirm
  each call site still resolves.

## Notes

- This is a pure refactor. No new theorems, no signature changes.
- If the worker discovers that `_explicit` truly depends on the
  non-explicit form (mutual recursion), the right move is to invert the
  dependency: have `_explicit` not call the non-explicit version, and
  have the non-explicit version delegate to `_explicit`. Investigate
  before assuming.
- This issue is independent of and complementary to a separate refactor
  in `Chapter5/FormalCharacterIso.lean` that consumes `_explicit`
  directly. Either can land first.
