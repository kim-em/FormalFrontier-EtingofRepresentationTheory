## Current state

`Chapter5/FormalCharacterIso.lean:779-949` defines
`glTensorRep_equivariant_schurWeyl_decomposition`. The proof manually
reconstructs the same `glHom : GL_N → centralizer(symGroupImage)` and
per-summand `ρ i : GL_N → End_k (Hom_A(V'_i, E))` chains that already
live inside `Theorem5_18_4_GL_rep_decomposition` /
`Theorem5_18_4_GL_rep_decomposition_explicit`
(`Chapter5/Theorem5_18_4.lean:548-585` and
`Chapter5/Theorem5_18_4.lean:660-...`).

PR #2579 (issue #2572) introduced
`Theorem5_18_4_GL_rep_decomposition_explicit` precisely so that
downstream consumers can reuse the bundled `L i : FDRep` together with
the explicit evaluation formula
`e.symm (of i (v ⊗ₜ l)) = (L_carrier i l) v` instead of re-deriving
`glHom` / `ρ` / `Module.Finite k (V'i →ₗ[A] E)` by hand.

The progress note from session `cbaf28df` (`progress/20260427T161046Z_cbaf28df.md`)
explicitly flags this refactor as the natural follow-up to #2572 and
#2573, observing that the manual `Module.Finite` derivation at
`FormalCharacterIso.lean:818-832` becomes redundant once the consumer
uses `_explicit`.

## Deliverables

1. Rewrite `glTensorRep_equivariant_schurWeyl_decomposition` in
   `Chapter5/FormalCharacterIso.lean` to:
   - Invoke `Theorem5_18_4_GL_rep_decomposition_explicit k N n hN`
     to obtain `ι, S (as `Submodule`), L (as `FDRep k (GL_N k)`),
     `L_carrier`, `e`, and the eval formula
     `e.symm (of i (v ⊗ₜ l)) = (L_carrier i l) v`.
   - Expose the same existential signature as before
     (`ι, S : ι → Type u, AddCommGroup, Module k, Module.Finite k,
     L : ι → FDRep k (GL_N k), e, equivariance`) by passing
     `S i := ↥(S' i)` (the underlying type of the submodule); the
     `Module.Finite k ↥(S' i)` instance is automatic from
     `Module.Finite.of_injective ((S' i).subtype.restrictScalars k)`.
   - Discharge equivariance by reducing to pure tensors via
     `DirectSum.linearMap_ext` + `TensorProduct.ext'` exactly as in the
     current proof, then using the explicit eval formula:
     `e.symm (of i (v ⊗ₜ l)) = (L_carrier i l) v` — both sides evaluate
     to `glTensorRep g ((L_carrier i l) v)` after a `LinearEquiv` chase
     through `L_carrier`.
2. Delete the now-unused manual constructions inside the body of
   `glTensorRep_equivariant_schurWeyl_decomposition`:
   - the `glHom` `let` (lines 834-862) — already lives inside `_explicit`;
   - the `ρ` `let` (lines 867-893) — `(L i).ρ` is now what the explicit
     theorem returns;
   - the `hSi_fin` / `hLi_fin` `haveI`s (lines 818-832) — `Module.Finite`
     for the new `S i := ↥(S' i)` is one line via `of_injective`;
3. Verify downstream consumer
   `formalCharacter_tensorPower_eq_sum_character_L`
   (`Chapter5/FormalCharacterIso.lean:971`) still type-checks against
   the new signature (existential output is unchanged in shape, only
   the proof body is refactored).

## Context

- Source theorem to consume:
  `Chapter5/Theorem5_18_4.lean:626` (`Theorem5_18_4_GL_rep_decomposition_explicit`),
  introduced by #2579 (issue #2572).
- Refactor target:
  `Chapter5/FormalCharacterIso.lean:779-949`
  (`glTensorRep_equivariant_schurWeyl_decomposition`).
- Downstream consumer to keep working:
  `Chapter5/FormalCharacterIso.lean:971`
  (`formalCharacter_tensorPower_eq_sum_character_L`).
- Progress note flagging this refactor:
  `progress/20260427T161046Z_cbaf28df.md` (lines 49-56) and
  `progress/20260427T153047Z_8e0b9c38.md` (final paragraph of
  "Current frontier").
- Expected line savings: ~120 lines (manual `glHom` + `ρ` chain plus
  the two `Module.Finite` derivations).

## Verification

- `lake build EtingofRepresentationTheory.Chapter5.FormalCharacterIso`
  passes.
- The refactored `glTensorRep_equivariant_schurWeyl_decomposition` has
  no new sorries.
- `formalCharacter_tensorPower_eq_sum_character_L` continues to type-check
  unchanged (its `obtain` pattern matches the same existential).
- Heartbeat bumps may need adjustment; document any change.

## Notes

- This is a pure refactor. No new theorems, no signature changes to the
  existential output of
  `glTensorRep_equivariant_schurWeyl_decomposition`. Downstream behaviour
  is identical.
- The `L_carrier` clause from `_explicit` is needed inside the proof to
  bridge `(L i : Type u)` (universally quantified bundled FDRep carrier)
  and the concrete hom-space `↥(S' i) →ₗ[A] V^⊗n` so that `l` can be
  applied to `v`. Use `LinearEquiv.refl` defeq (`FGModuleCat.of_carrier`)
  if the proof needs it.
