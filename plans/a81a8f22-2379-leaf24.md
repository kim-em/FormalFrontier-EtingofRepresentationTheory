## Current state

`etilde6v2Rep_isIndecomposable` (line 2323 of `InfiniteTypeConstructions.lean`)
proves indecomposability of the Ẽ₆ representation for `m ≥ 1`. PR #2414
landed the structural framework with 5 sub-sorries. This issue covers the
first (and hardest) sub-sorry: `leaf24_containment` at line 2380.

## Deliverables

1. Fill the sorry at line 2380 of `InfiniteTypeConstructions.lean`
   (inside `have leaf24_containment ...`). The goal is:

   ```
   ∀ x : Fin (m + 1) → ℂ, x ∈ W (2 : Fin 7) → x ∈ W (4 : Fin 7)
   ```

   where `W, W'` is an arbitrary invariant decomposition of the Ẽ₆
   representation with each pair `(W v, W' v)` a complement pair (i.e. the
   data coming from a hypothetical splitting of `etilde6v2Rep m`).

## Proof sketch (from the issue body / PR #2411)

- For `x ∈ W(2)`, push forward along `hom21 : 2 → 1` giving
  `(x, 0) ∈ W(1)`, then along `hom10 : 1 → 0` giving `(x, 0, 0) ∈ W(0)`,
  then along `hom03 : 0 → 3` (which is `etilde6v2Gamma`) giving
  `(x, x) ∈ W(3)`.
- Meanwhile from vertex 4 via `starEmbed1`: any `y ∈ W(4)` sends to
  `(y, 0) ∈ W(3)`.
- The complement `W'(3)` contains nothing mixing "both blocks equally" in
  the sense we need; combined with the leaf-6 `(Nz, 2Nz) ∈ W(3)` structure,
  this forces `x ∈ W(4)`.
- The `m ≥ 1` nilpotent coupling from leaf 6 is essential — without it,
  the Ẽ₆ representation is genuinely decomposable (see #2388).

## Context

- File: `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`,
  line 2380 (inside `have leaf24_containment`).
- Framework set up by PR #2414.
- Related sibling sub-sorries (each a separate issue): `hN₁`+`hN₂`
  (N-invariance), `hbot0` (W(0) = ⊥ from W(2) = ⊥), `hbot3` (W(3) = ⊥ via
  Γ surjectivity).
- The analogous leaf-containment argument in D̃₅ is at lines ~1759-1807
  and in Ẽ₇ at the `leaf_containment` helper (search for it in the same
  file); use those as templates.
- Reference: `m ≥ 1` hypothesis is `hm` in scope.
- Difficulty: 7-8. This is the core mathematical step that isolates the
  Ẽ₆ indecomposability proof from the other root-system cases.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6.InfiniteTypeConstructions`
  succeeds.
- 1 sorry eliminated (line 2380).
